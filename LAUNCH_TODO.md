# Launch TODO — Living Checklist

**Launch day: TBD — deferred until Android app is feature-complete and tested.**
Launching iOS-only with a buggy or missing Android app risks long-term reputational damage. The June 1, 2026 date is paused. The original day-by-day calendar lives in `docs/LAUNCH_PLAYBOOK.md`; treat it as a *template* now, not a deadline. Many tasks (legal, social accounts, screenshots, App Store assets) can still be completed in parallel and will compress the post-Android timeline.

This document is the single source of truth for what's still open before launch. Update it every session. Items are grouped by category, not by order. Within each category, items are roughly high → low priority.

**Legend:** `[ ]` open · `[~]` in progress · `[x]` done · `[!]` blocked on external dependency

---

## 🗺 Doc Map — where everything lives

This file is the **only** task tracker. Everything else is reference (no tasks tracked there):

- **Tasks / launch status:** this file (`LAUNCH_TODO.md`).
- **Scaling thresholds + AlphaVantage plan upgrades (150→300/min):** `SCALING_TRIGGERS.md`.
- **Current system design:** `CURRENT_ARCHITECTURE.md` (+ `ARCHITECTURE_CHANGELOG.md` for history).
- **Onboarding / build & deploy:** `DEVIN_HANDOVER.md` (+ front door `README.md`).
- **Performance %, snapshots, cash tracking:** `docs/PERFORMANCE_AND_SNAPSHOTS.md`.
- **Feature specs:** `PENDING_TRADES_DESIGN.md`, `XERO_PAYOUT_INTEGRATION.md`, `docs/PER_CREATOR_SUBSCRIPTION_SLOTS.md`, `POLL_ADMIN_GUIDE.md`.
- **Env / setup:** `ENV_VARIABLES.md`.
- **Launch calendar template + store copy:** `docs/` (`LAUNCH_PLAYBOOK.md`, `ASO_STRATEGY.md`, `PLAY_STORE_LISTING_GUIDE.md`).
- **Superseded planning/implementation docs:** archived under `_legacy/docs_web_era/` + `_legacy/docs_debug_history/` (2026-06 cleanup — `DEPLOYMENT_CHECKLIST`, `MASTER_REFERENCE`, `IMPLEMENTATION_CHECKLIST/PHASES/RECOMMENDATION/STATUS/SUMMARY`, `TOMORROW_GAMEPLAN` — the last was a May 20 day-plan whose Steps 1/2/3/5/6 all shipped; only its Phase F hardening survived into Section A below). **Session 18 (2026-06-22) doc consolidation** archived `MOBILE_ARCHITECTURE_PLAN`, `FINAL_REQUIREMENTS`, `GHOST_SUBSCRIBER_VISIBILITY`, `QUICK_START`, `README_UPDATES`, `SETUP_GUIDE`, `PRODUCTION_MIGRATION_GUIDE` to `_legacy/docs_web_era/`, and merged `DESIGN_DECISIONS_EXPLAINED` + `PERFORMANCE_CALCULATION_AUDIT` + `PORTFOLIO_CLEANUP_PLAN` → `docs/PERFORMANCE_AND_SNAPSHOTS.md`.

---

## 🎯 MASTER PRE-LAUNCH TASK LIST (rebuilt Session 18, 2026-06-22)

The authoritative, deduplicated checklist of **everything** required for public launch — consolidated from this file *and* every `docs/*.md` (PLAYBOOK, CONTENT, OUTREACH, ASO, PLAY_STORE_LISTING_GUIDE, PRICING, IAP_WEBHOOK_SETUP, XERO_W9_DEPLOY_CHECKLIST, PER_CREATOR_SUBSCRIPTION_SLOTS, bot_agent_*). Detail/history for each item lives in the dated sections below and in the referenced docs. Status reflects sessions 1–18.

### 0. 🚦 Critical path (do roughly in this order)
1. [x] **Run both Supabase migrations — DONE (Session 18, USER); both ran clean, `mobile_subscription.slot` verified integer/nullable.** — `2026_06_14_subscription_slot.sql` + `2026_06_17_payout_integrity.sql` (full SQL: §"Migration SQL to paste" below). Gates the already-deployed slot + payout code.
2. [x] **Payout-integrity + monthly-cron + 1099/W-9 code PUSHED** — commits `e630f13` + `1c9eaa5` on `origin/master` (verified Session 18; no uncommitted money-file changes).
3. [x] **Slot 2–20 store products CREATED** — 19 iOS subscription groups (monthly+annual) + 38 Play products (USER, Session 18).
4. [ ] **Verify Apple ASSN V2** via a real sandbox subscription (no test-button in ASC). Google RTDN already [x] verified (Session 18).
5. [~] **Ship updated builds** — **iOS Build 45 uploaded to ASC (2026-07-10, repo at `60b7233`)**; **Android `versionCode 4` AAB built + uploaded (2026-07-10)**. Remaining: next Android build (v5) carries the fused login wordmark (Session 25 fix); start closed-testing rollout.
6. [~] **App Links / AASA — backend DONE + verified live (2026-06-24); on-device autoVerify remains.** Both endpoints confirmed serving correct content at `https://apestogether.ai/.well-known/{assetlinks.json, apple-app-site-association}`: iOS `appID = M8R8YVW472.com.apestogether.ApesTogether` (`APPLE_TEAM_ID` already set on Vercel — the `TEAM_ID` placeholder only lived in the unused static file, now also corrected); Android now serves **both** SHA-256 fingerprints. **Fixed a real bug:** assetlinks was shipping an EMPTY fingerprint list (Vercel `includeFiles` doesn't bundle the static file into the Python lambda → route fell back to an unset env var), which silently disables `autoVerify`; now backstopped by an embedded code constant + `Cache-Control: no-store` (commits `13c65b2`, `3a0b14d`). **Remaining:** on-device `adb shell pm get-app-links com.apestogether.app` after installing a build signed with the matching Play key.
7. [ ] **E2E money tests** — paid subscribe → validate → Xero bill → 1099; cancel-path (iOS StoreKit + Android license tester); W-9 hold/release. Both platforms.
8. [ ] **Generate real store assets** → fill both listings → **submit for review**.
9. [~] **Attorney privacy policy + ToS** — **Privacy Policy PUBLISHED (2026-07-15, attorney text verbatim + GPC support). Creator-deletion cancellation Q&A ANSWERED (Session 30) — flow built + behavior summary sent to attorney.** Remaining: attorney redrafts ToS §5.2(a) (85%-of-net); disclaimers audit still ours.
10. [~] **Google Play 14-day Closed-Testing gate — likely NOT APPLICABLE (2026-07-20).** The Dashboard offers the direct “Create and publish a release → production track” flow with NO 14-day/12-tester requirement card — consistent with **organization** accounts (Family Apps LLC) being exempt from the personal-account closed-testing mandate. Treat the 14-day wall-clock as OFF the critical path (confirm at first release review); still run a closed test for quality, but with no tester minimum or clock.

### 1. 🔒 Security & Backend
- [~] **Full pre-launch security audit — DONE (Session 18) → `docs/SECURITY_AUDIT.md`.** Verdict: **no SQL injection, no IDOR, strong authz** — raw SQL is uniformly parameterized (the one f-string DDL at `api/index.py:7105` is hardcoded constants + `@admin_2fa_required`); per-user resources scope to `g.user_id` (`/unsubscribe`, `/subscriptions/<id>/scale`, `/pending-trades/<id>` all checked); 90/97 mobile routes auth-gated; Apple webhook JWS verified + root-pinned; OAuth `enforce`; no CORS wildcard; secrets via env. **Open hardening:** ~~S-1~~ **RESOLVED** (Session 19 W1 — shared Postgres rate-limiter live + `/auth/token` throttled 10/min/IP); ~~S-2~~ **RESOLVED** (`RTDN_WEBHOOK_TOKEN` shared-secret set on Vercel + Pub/Sub URL); ~~S-3~~ **RESOLVED** (`app.py` debug gated behind `FLASK_DEBUG=1`); **S-4 still open** — no `pip-audit`/Dependabot in CI (see §10/CI hardening); ~~S-5~~ **RESOLVED** (`MAX_CONTENT_LENGTH = 1 MB`).
- [~] **Rate-limit / abuse controls (audit finding S-1) — core DONE (Session 19 W1).** `rate_limit` now uses a shared Postgres fixed-window counter (works across serverless instances); `/auth/token` throttled 10/min/IP; trade execution 60/min/user. ~~Remaining: `/leaderboard` + Trader API~~ **DONE (Session 19):** mobile `/api/mobile/leaderboard` was already `@rate_limit(60)`; added the same to the legacy public web `/leaderboard/` + `/leaderboard/api/data` (commit `46668ea`, verified live 200). UC-A rate-limit design written into `docs/TRADER_API_SCOPING.md` (per-user keying, read/write buckets, per-day quota, tiered limits resolving D-6, quota headers).
- [ ] Load-test `/api/mobile/feed` at 100 concurrent users (§A).
- [x] OAuth ID-token verification hardened + verified live; admin-route auth sweep complete; dead admin stubs deleted.

### 2. 🤖 Automated-input API & Bot-research adequacy
- [~] **Public Trader API — SCOPED (Session 18) → `docs/TRADER_API_SCOPING.md`.** Two products: **UC-A Algo/AI Creator API** (inbound WRITE — external algos/AI run a public portfolio via API key + scopes; mostly a hardened wrapper over the *proven internal bot trade logic*; trades are virtual = low regulatory risk) and **UC-D Subscriber Auto-Execution API** (outbound READ + trade events — a follower's bot mirrors creators' trades into a *real brokerage* via SnapTrade/Alpaca; heavy legal/compliance). Recommendation: **UC-A = v1 / fast-follow (HARD-depends on the audit S-1 shared-store rate-limiter landing first); UC-D = post-launch, compliance-gated (start legal review now, design UC-A's trade-event model UC-D-ready).** Open product decisions **D-1..D-6** listed in the doc. *(Distinct from the internal bot system and from the AI-model research below.)*
- [x] **#2 Per-sector data adequacy incl. real estate & healthcare — DONE (Session 19) → `docs/BOT_DATA_ADEQUACY.md`.** Verdict: stack is **adequate for the product's purpose** (realistic varied virtual trading; per-ticker dead-leg reweighting keeps decisions sane), but **structurally thin in exactly the two flagged sectors** — Real Estate (no rates/FFO inputs) and biotech-within-Healthcare (no FDA/trial-catalyst feed). Also confirmed **two of four Finnhub alt-data legs are DEAD on the free tier** (social + analyst 403 → skipped); only insider works. **Highest-ROI fix is ~free:** wire in 4 endpoints already in AV Premium (`EARNINGS_CALENDAR` — the `earnings` archetype currently has NO earnings-date input!, `DIVIDENDS`, `OVERVIEW` fundamentals, `TREASURY_YIELD`/`FEDERAL_FUNDS_RATE` for the REIT rates gap). Then revive Finnhub analyst/social (paid tier) + openFDA→Benzinga biotech catalysts. Tiered provider recommendations (FMP/Polygon/Tiingo) in the doc.
  - [x] **Bot Layer A — SHIPPED (Session 20, commit `4e010ea`).** Market-wide `EARNINGS_CALENDAR` + `TREASURY_YIELD` (2 AV calls/wave, no new infra): adds pre-earnings drift timing to the `earnings`/`news_reactor` archetypes (which previously had NO earnings-date input) and a REIT rates tailwind/headwind to Real Estate names (the missing rates signal). New signals are backward-compatible flat addends — existing bots' 7-key weights untouched. Surfaced in the admin Market Research card + trade attribution.
  - [x] **Bot Layer B — SHIPPED (Session 20, commit `80b65f0`).** Per-ticker fundamentals cache from AV `OVERVIEW` (weekly cron `refresh-fundamentals`, part/of split; `get-fundamentals` CI read endpoint; `stock_fundamentals` table). Adds **valuation** (P/E+PEG) + **dividend-yield** signals and **revives the dead analyst leg** via real `AnalystTargetPrice` upside (keeps the insider/analyst weight live on the free Finnhub tier instead of zeroing it). Degrades to 0 if the table/cache is absent.
  - [x] **DONE (2026-07-03, USER) — `scripts/migrations/2026_06_25_stock_fundamentals.sql` applied**, verified via `SELECT to_regclass('public.stock_fundamentals')` → returns `stock_fundamentals` (table exists). **Optional next:** warm the cache once — `GET /api/cron/refresh-fundamentals?part=1&of=3` (×3) with the cron secret, or just wait for the Sunday 23:00–23:04 UTC cron.
- [ ] **#3 External AI stock-picking model API research (owed write-up).** Evaluate feeding bots an LLM/signal API vs. the rules engine; scope providers, cost, latency, integration into `bot_data_hub.py` / `bot_strategies.py`.
- [ ] **#1 Bot creation/batch rules verification** — trades stay in assigned sector; research method + `max_cash_deployed` wired (`bot_executor.py`/`bot_strategies.py`/`bot_data_hub.py`).

### 3. 🍎 iOS app + App Store
- [x] iOS build archive — **Build 45 uploaded to App Store Connect (2026-07-10**; Mac pulled to `60b7233`, ships slots + CTA + store-cancel + recent-trades-15 + trade icons + login legal links + deleted-creator UI**)**.
- [ ] Listing (`docs/ASO_STRATEGY.md`): Title `ApesTogether: AI Stock Trader`, Subtitle `Top Picks Alerts Tracker Live` (v4 keyword string), Keywords (v4 rebalance + es-MX loc), Promo text, Description.
- [x] **6 iOS screenshots DONE (Session 21, 2026-07-03)** — real app captures composited @ **1290×2796** (6.9") via `scripts/compose_appstore_screenshots_v2.ps1` → `~/Downloads/AppStore_Screenshots_v2` (`01_leaderboard`…`06_earnings`); captions/disclaimers from `docs/ASO_STRATEGY.md`. Upload straight into ASC Media Manager (PNGs are outputs, not committed). **Still:** app icon; app-preview video optional.
- [ ] Age rating **17+**, Privacy nutrition labels, Export-compliance answers.
- [ ] In-App Events drafted; review-prompt after follow + 3rd session.
- [ ] Visual-verify charts every period; verify pending-trade render on device.
- [ ] **Submit for review.**
- [x] FCM push, Apple/Google sign-in, billing verified on-device (§C).

### 4. 🤖 Android app + Play
- [x] Android **rebuild** — **`versionCode 4` AAB built + uploaded (2026-07-10)** (slots + CTA + recent-trades-15 + trade icons + pending render + ad-ID fix + login legal links + deleted-creator UI + scale-input commas).
- [x] **Next Android build (~~v5~~ v6):** fused login wordmark (Session 25 fix) — **`versionCode 6` AAB built + uploaded + rolled out to Internal AND Closed testing (2026-07-20, Session 31)**; verify commas + stable A/B/C slot order on-device (v6 now on the Pixel's internal track).
- 🐛 **Bugs from the USER's 2026-07-10 screenshot session (Session 25 triage):**
  - [x] Login screen "Apes Together" spaced — `LoginScreen.kt` wordmark appended " Together" with a leading space (missed by the Session 20 sweep because it's split across two `append()` spans); **fused in code, ships in v5**.
  - [x] Scale input missing commas-as-you-type — **already shipped** (`ThousandsSeparatorVisualTransformation`, commit `6c6a9db` 2026-06-27, included in v4); the device showing the bug ran an older build — update from the testing track and re-verify.
  - [x] Subscriptions reshuffled A, C, B — **root cause backend:** `GET /subscriptions` had no ORDER BY → Postgres heap order reshuffles when a row is updated (push toggle/renewal). **Fixed with `ORDER BY slot NULLS LAST, id`** (+ deterministic subscribers order); fixes BOTH apps, no rebuild — **needs deploy**.
  - [ ] Android notification screenshot — fire the admin-panel **Test Push** (same as iOS): sign in on the Pixel (registers FCM token), background the app, `/admin` → Test Push card, capture the tray notification.
- [ ] LegalText port + Settings linkouts status check.
- [~] Listing per `docs/PLAY_STORE_LISTING_GUIDE.md`: title/short/full desc (v4.1 slate locked in `docs/ASO_STRATEGY.md` — short desc rewritten after Play flagged `top` as ranking language), **feature graphic DONE + uploaded (2026-07-08)**, **listing text fields (title / v4.1 short / v4.2 full desc) + 512×512 icon ENTERED IN CONSOLE (2026-07-10)**, ~~8 screenshots~~ **6 screenshots UPLOADED in order (2026-07-20** — final set is 6, see `PLAY_STORE_LISTING_GUIDE.md`**)**, content rating (Reference/Educational, **not** gambling); **Data safety DONE (2026-07-07)** — Advertising ID = **No** (after the AD_ID fix below); declared Name/Email/User IDs/Address/Other-info (Tax ID)/Purchase history/Other-financial (holdings)/App interactions/Device IDs, all **Collected, not Shared**; **Financial features DONE (2026-07-07) = "Other" → "Simulated (paper) stock-portfolio tracking"** — the old UI's "Personal investment research and management" no longer exists and we deliberately avoid "Stock trading and portfolio management"; **Play confirmed no additional documentation required** (see rewritten `docs/PLAY_STORE_LISTING_GUIDE.md §2.9`); target audience **18+**, privacy-policy URL 200, reviewer login.
- [ ] Subscriptions **Active** + **Small Business Program (15%) enrolled**; payments/bank/tax profile complete.
- [~] **14-day Closed Testing** (≥12 opted-in testers) → Production rollout **@20%** → ramp. **Session 22 (2026-07-06):** signed release **AAB built + uploaded** to *Closed testing – Alpha* (`versionCode 2` — bumped from 1, which a prior internal-testing upload had already claimed; `versionName 1.0`, target SDK 35). Play App Signing **confirmed enrolled** (upload key `apestogether-upload` = assetlinks cert A; Google's app-signing key = cert B). **⚠ `versionCode 2` is now SUPERSEDED** — the Advertising-ID fix (commit `c86b89f`, `versionCode 3`) must be **rebuilt (`bundleRelease`) + uploaded before rollout** so the shipped build actually drops the ad ID; do NOT roll out the stale v2 AAB. **Next: rebuild v3 → upload → add ≥12 testers + start rollout — the 14-day clock begins at first rollout.** **UPDATE 2026-07-10 (Session 25): `versionCode 4` AAB rebuilt + UPLOADED (supersedes v3; carries ad-ID fix + login legal links + deleted-creator UI + scale commas). Remaining: add ≥12 testers + start rollout.** **UPDATE 2026-07-20 (Session 31): `versionCode 6` release ROLLED OUT to Closed testing – Alpha + SUBMITTED FOR REVIEW** (12-tester/14-day gate does NOT apply — org account; USER is sole tester). Internal testing also superseded to v6 (was pre-ad-ID-strip v1 — see Session 31 notes). Next: await review approval → promote the SAME release to Production.
- [x] Slot 2–20 products created (Session 18); RTDN verified; on-device sign-in/billing/deep-link verified.

### 5. 🎨 Shared store assets & review prep
- [~] Generate **app icon** — **Play 512×512 DONE + uploaded (2026-07-10**; `android/play_store_icon_512.png`, untracked — commit it**)**; iOS ASC icon rides the build — ~~feature graphic (1024×500)~~ **feature graphic DONE (2026-07-08)** — Gemini art + Option-A text, normalized to exactly 1024×500 24-bit PNG at `assets/store/play_feature_graphic_1024x500.png`, uploaded to Play — **8 screenshots** (Gemini prompts in `docs/ASO_STRATEGY.md`; no text in icon).
- [~] **Android screenshots — generator ready (Session 22, 2026-07-03).** Reuse the iOS captures behind an Android frame: `compose_appstore_screenshots_v2.ps1 -FrameFile pixel_frame.png -AndroidStatusBar -OutputDir …\Play_Screenshots` — the new `-AndroidStatusBar` mode overpaints the iOS status bar + Dynamic Island with a synthetic Android status bar (clock/wifi/battery) and auto-uses a **2:1 canvas** (Play caps aspect at 2:1). Generated a first pass to `~/Downloads/Play_Screenshots`. **Note:** these are iOS UI behind a Pixel frame — fine for a first listing given the near-identical custom UI, but **recapture the 6 screens on the real Pixel 8a** before/soon after launch for full Play-policy fidelity — **Pixel 8A now in hand (2026-07-06); native capture unblocked.** ~~Still need the **1024×500 feature graphic** (Play-only).~~ **Feature graphic DONE + uploaded (2026-07-08, Session 24).**
- [ ] Create **reviewer demo account(s)** (`reviewer@apestogether.ai`) with followed traders, for Apple + Google reviewers.
- [ ] **Brand-spelling sweep** — "ApesTogether" (fused) everywhere: iOS/Android strings, push sender label, share-sheet, landing, emails, social display names.
- [ ] **UGC pre-submission scrub** — review all trader display names + shared content (one bad string rejects the whole app).

### 6. ⚖️ Legal / Compliance
- [~] Privacy policy final — **attorney ENGAGED (USER sent requirements 2026-07-09; drafting now)**.
- [~] ToS final — **attorney drafting (same engagement)**; reconcile `ATTermsOfService03APR2026.docx` ↔ `/legal/terms-of-service.md` when it lands.
- [ ] Investment-disclaimers audit (`LAUNCH_PLAYBOOK §11` — never imply guaranteed returns; "informational/educational only").
- [ ] Per-store disclaimer: "We do not offer real brokerage services. All trades are virtual using real market data."

### 7. 🌐 Website / Landing
- [ ] Refresh landing screenshot (old pricing); OG-image test (opengraph.xyz); live waitlist counter.
- [x] Hero/nav CTAs, waitlist segmentation, trust bar, © 2026, real screenshot.

### 8. 📣 Marketing / Launch execution  *(deferred until Android is "fairly bug-free", then run `docs/LAUNCH_PLAYBOOK.md`)*
- [ ] **Now:** register + lock social handles (X `@ApesTogetherApp`, TikTok/IG `@apestogether`, LinkedIn, YouTube, Reddit) + UTM bio links.
- [ ] Press kit (screenshots, bio, one-pager) — playbook Day 16.
- [ ] Content calendar X/TikTok/LinkedIn/Reddit (`docs/LAUNCH_CONTENT.md`); influencer + journalist outreach incl. WSJ exclusive (`docs/LAUNCH_OUTREACH.md`); email sequences; $2-bill stunt (launch week); stand up KPI tracker.

### 9. 🛠 Ops / Data / Infra
- [ ] AlphaVantage News/Movers/Prices `no_calls` — verify after next bot wave; cold-start ping cron `*/4 → /api/health` (status unclear); verify weekly drift-cron fires.
- [ ] After first monthly close: confirm Xero revenue post + payout sync; confirm ghost/bonus-subscriber tracking matches month-end payouts.
- [x] Xero reconnected; `ADMIN_API_KEY` + `ADMIN_TOTP_SECRET` confirmed; `GOOGLE_PLAY_CREDENTIALS_JSON` set (on-device billing worked).

### 10. 🧹 Post-launch cleanup (non-blocking)
- [ ] Delete stray iOS Firebase app + resolve Android SHA-1 duplicate warning.
- [ ] `DELETE` 33 orphan uppercase `stock_transaction` rows.
- [ ] CI/security hardening (branch protection, SHA-pin Actions, OIDC, Dependabot/CodeQL, git-history secret scan).
- [ ] Consolidate the two `SQLAlchemy(app)` instances; remove the legacy OAuth decode shim.
- [ ] Email deliverability (`notifications@` → spam) diagnosis; later tighten DMARC → `p=reject`.

### 11. 🚀 Post-launch feature backlog (not built — tracked so they aren't lost)
- [ ] **Trader API (UC-A: bots submit trades via API key)** — scoped in `docs/TRADER_API_SCOPING.md` (open decisions D-1..D-6); rate-limiter dependency already shipped (Session 19 S-1). No routes, no docs page yet.
- [ ] **Outbound trade-event feed** (UC-B: machine-readable JSON/CSV poll + webhook for subscribers) — same scoping doc.
- ~~**Waitlist 2.0** (queue-jump referrals)~~ **DROPPED (Session 26 verdict)** — waitlist stays a simple email collector; revisit only if pre-launch traffic materializes.
- ~~**Invite-gate at signup**~~ **REJECTED (Session 26 verdict, USER + product-fit review)** — AT is a marketplace where every user adds leaderboard supply and the core asset (track-record time) can't be gated without starving it; r/wsb audience is allergic to velvet ropes. → MARKETING_PLAN §Verdict.
- [x] **Founding Trader badge (100 slots) — BUILT (Session 28)** — the ONE scarcity mechanic kept. **Rules defined Session 27** (→ MARKETING_PLAN §"Founding Trader badge — RULES"): first 100 human traders by first-trade timestamp, beta counts, bots/admin/test accounts NEVER qualify, permanent, status-only (no fee break / placement boost). **Shipped:** idempotent award sweep (`mobile_api._award_founding_trader_badges`, frozen at 100) triggered on first live trade + queued-trade settle + `POST /admin/founding-trader/award` backfill; `founding_trader` on leaderboard `user` + portfolio `owner` payloads; gold FOUNDER chip (leaderboard row stats line) + "Founding Trader" pill (profile badge row) on **Android and iOS** (iOS ships with the next Mac build). **Remaining:** web UI chip (post-launch); public "N/100 claimed" counter (deferred until ~20+ claimed per rules).
- [ ] **Play pre-registration + Apple pre-order** setup — converts waitlist → day-one install spike.
- [ ] **"ApesTogether vs Dub vs eToro" comparison page** on the site (GEO/AI-search play).
- [ ] Settings v1.1 screens (Payment History, Tax Info / W-9 sheet, FAQ link) — both apps.

---

## 🛠 SESSION 19 WORK ITEMS (opened 2026-06-22, from USER review)

Tracked here so nothing is dropped; checked off as resolved. Detail/answers land in the linked docs.

- [x] **W1 — Security fixes (audit S-1..S-5) — DONE + DEPLOYED (2026-06-24).** Chose **Postgres-backed** limiter (Supabase; no new infra). (a) **S-1**: `rate_limit` uses a shared Postgres fixed-window counter with in-memory fallback, and `@rate_limit(10,60)` on `/auth/token` — migration `2026_06_22_rate_limit.sql` **applied** (cross-instance limiting live); (b) **S-2**: Google RTDN webhook honors `RTDN_WEBHOOK_TOKEN` — **env var set on Vercel + `?token=…` appended to the Pub/Sub push URL** (USER, confirmed); (c) **S-3**: `app.py` debug requires `FLASK_DEBUG=1` (no-op in prod WSGI); (d) **S-5**: `MAX_CONTENT_LENGTH = 1 MB`. S-4 (`pip-audit`/Dependabot) stays in §10. → `docs/SECURITY_AUDIT.md`.
- [x] **W2 — Anti-abuse caps (DONE).** `mobile_api.py` constants `MAX_SHARES_PER_ORDER=1M`, `MAX_ORDER_NOTIONAL_USD=$10M`, `MAX_HOLDINGS_PER_PORTFOLIO=200`, enforced in `execute_trade` (both live + after-hours paths). Ships on next backend deploy.
- [x] **W3 — Price-integrity guarantee (DONE).** Found + fixed a real hole: `execute_trade` previously trusted the client's `price`. Now the live price is fetched **server-side** (`PortfolioPerformanceCalculator().get_stock_data`) and any client `price` is ignored. After-hours trades already settle at the cron's open price. (`bot_execute_trade` is internal/`@require_cron_secret` — our backend; the future inbound API will inherit `execute_trade`'s no-price contract.) Invariant documented in the docstring.
- [x] **W4 — Renamed "obfuscate/obfuscation" (DONE). Chosen term: `bot_multiplier` (identifier) / "privacy multiplier" (prose).** Renamed `obfuscation_multiplier`→`bot_multiplier`, `scripts/obfuscate_bot_holdings.py`→`scripts/scale_bot_holdings.py`, and all prose across `api/index.py` + `mobile_api.py`. **Left intact (not ours):** Google Billing's real `obfuscatedAccountId` field (LAUNCH_TODO §C note) and ProGuard "deobfuscation" in `docs/PLAY_STORE_LISTING_GUIDE.md`.
- [x] **W5 — Subscription SLOT bug — DONE + DEPLOYED (2026-06-24).** (a) `bot_subscribe` now assigns the lowest free slot via `subscription_slots.lowest_free_slot()` (fixes all FUTURE comped subs); (b) **comped-sub backfill** `2026_06_22_reslot_comped_subs.sql` **applied** — returned 0 rows (no other admin-comped subscribers exist); (c) **bobford00 reslot** `2026_06_24_reslot_bobford_test_subs.sql` **applied** — his 3 real-platform FREE-TEST subs (all on the legacy slot-1 product) renumbered → slots 1/2/3, **verified** A=CoastHillBear / B=candle3873 / C=divi51. **Remaining launch dependency (tracked, not blocking this item):** real future multi-creator subs only get distinct slots once the `com.apestogether.sub.s02..s20` products are live in App Store Connect + Play Console — until then every real purchase buys the legacy slot-1 product and would collide the same way.
- [x] **W6 — iOS Subscriptions UI (DONE).** `SubscriptionsView.swift`: slot pill now `lineLimit(1)` + `fixedSize` (no mid-word wrap) and "Renews …" moved to its own line below the badges. Ships on next iOS build.
- [x] **W7 — "Allow New Subscribers" — FULLY IMPLEMENTED (backend + both apps).** Was frontend-only (gated nothing); now real. **Done:** (a) flag stored in `User.extra_data['accepts_new_subscribers']` (default True, no migration) via `_get_accepts_new_subscribers`; (b) persisted through `GET/PUT /settings/portfolio-preferences`; (c) server-authoritative gates — `slot_for_creator` returns `not_accepting_subscribers` and `validate_purchase` 403s a brand-new sub (existing subscribers' renewals still validate — store auto-renews until the subscriber cancels; we never revoke a payer); (d) flag now exposed on the public portfolio (`get_portfolio`) and every leaderboard row; (e) Settings toggle **renamed → "Allow New Subscribers"** and wired to persist on **both** iOS (`SettingsView.swift`) and Android (`SettingsScreen.kt` + `SettingsViewModel`). User stays on the leaderboard / portfolio viewable when OFF. (f) **CTA swap DONE on both apps:** when `accepts_new_subscribers` is false the Subscribe button is replaced with *"Not accepting new subscribers right now"* on the leaderboard expanded card, the public portfolio detail (Share button stays), and the blurred-holdings teaser — on **both** iOS and Android.
- [x] **W8 — Inbound/outbound API design answers (DONE)** → `docs/TRADER_API_SCOPING.md` "Design clarifications": (a) interface = no GUI, just documented REST endpoints + an in-app-generated API key; (b) Apple/Google friction preserved by construction — API access requires an existing signed-in app account, no separate signup, keys scoped to one account; (c) price integrity guaranteed — the API has no `price` param and reuses the server-priced `execute_trade` path; (d) outbound = non-personalized machine-readable trade-event feed (JSON + CSV, poll + webhook), same event as the email but structured + subscriber-gated.
- [x] **W9 — Price-cache + AlphaVantage scaling (analysis DONE + shared cache IMPLEMENTED).** Analysis in `docs/PRICE_CACHE_AND_SCALING.md`. **Implemented:** `portfolio_performance.py` now layers a **shared Postgres L2 cache** behind the per-instance in-memory **L1** (`_shared_cache_get/_get_many/_set_many`), so every serverless instance reuses the same prices instead of each re-fetching (this was the real scaling blocker, not user count). Added a **tiered TTL** (`PRICE_CACHE_TTL`=90s general / `PRICE_CACHE_TTL_HOT`=30s for "hot" tickers = those traded in the last 15 min + optional `PRICE_CACHE_HOT_TICKERS` env list). All shared-cache ops use a **separate engine connection** (never touch the request's ORM txn) and are fully defensive (missing table → in-memory fallback). **USER must run** `scripts/migrations/2026_06_24_stock_price_cache.sql`. Projection: 50k users @ 30s TTL ≈ 40 calls/min, well under the 150/min tier. Also bumped the trade rate limit **20 → 60/min per user** (`/portfolio/trade`). **Migration `2026_06_24_stock_price_cache.sql` applied + backend DEPLOYED (2026-06-24).** Verified the cache is *consumed*, not just defined: `get_stock_data` (L1→L2→fetch→write both) and `get_batch_stock_data` (L1→batched L2→fetch misses→write both) are the only live-price paths; historical-bar/S&P backfills + admin AV diagnostics intentionally don't use this live cache. **Cache is backend-only** — the iOS/Android apps never call AlphaVantage; they render prices from backend API responses, so both apps benefit transparently (no app-side cache exists or is appropriate).

---

## 🧾 Session 21 (2026-07-03) — W-9 polish, month-end check-run, USPS decision, 1099 filing

**Shipped + pushed (commit `30f9841`, verified live):**
- **Month-end payout check-run email** — the existing `/api/cron/monthly-payouts` (3rd @ 09:00 UTC) now emails a "check run" (payee + mailing address + amount + **real/gift** sub counts) to **bobford00@gmail.com** at the end of `run_monthly_payout_pipeline`. Verified via `GET /api/mobile/admin/tax/payout-check-run?sample=1` → `email: sent`. `?sample=1` returns a fabricated 3-payee report (incl. a business row + a missing-address warning row); `?email=1` uses real data.
- **In-app W-9 form polish (iOS + Android)** — "Powered by Xero" logo, softer trust copy, USPS state dropdown, ZIP `^\d{5}(-\d{4})?$` regex, server-error surfacing + a "Submit anyway" override.
- **`services/address_validation.py`** — present but **DORMANT by decision** (see below).

**Decisions logged:**
- **USPS address validation = NOT used.** The USPS Address API goes paid (license + tier fees) **2026-07-12**; not worth it for our low mailing volume. The module is **fail-open** — with `USPS_CLIENT_ID/SECRET` unset it's a no-op; we rely on client+server format/state/ZIP checks + human review of the check-run email. Detail in `docs/XERO_W9_DEPLOY_CHECKLIST.md` §G.
- **1099 filing direction (was never logged — now decided):** Xero **tracks payments + generates** the 1099-NEC report (payout bills post to `6010`), but Xero does **NOT** transmit to the IRS itself. At January close, two paths: **(a) e-file via a Xero App Store partner** (Track1099/Avalara, Tax1099, TaxBandits — per-form fee, files IRS **+ states**, e-delivers recipient copies) or **(b) export the Xero 1099 report as CSV and file ourselves** (IRS only *requires* e-file at **10+** total info returns; under 10 we may paper-file). **Recommendation: Track1099 from within Xero** (cheap at our volume; handles state filing + recipient copies). Nothing to build — it's a once-a-year January task; our year-round prep (W-9 gate + `1099-readiness` dashboard + 6010 bills) already feeds it. The 1099 report is available on the **Early** plan.

**USER-confirmed done:**
- **`W9_TEST_EMAILS` deleted on Vercel** (owner accounts no longer look payout-eligible). ✅
- **iOS App Store screenshots ready** (6 @ 1290×2796).

**Still open (unchanged):** ship builds (iOS Build 41 on Mac + Android **release AAB** — needs `keystore.properties` on the build machine); E2E money tests; Play listing + 14-day closed test; attorney privacy policy.

---

## 🚀 Session 22 (2026-07-06) — Android release AAB built + uploaded; both device-pins cleared

**Devices back in hand** — USER has the **MacBook** (iOS / App Store Connect work unblocked) and the **Pixel 8A** (native Android capture + on-device closed-test install unblocked). The two items we'd pinned are no longer blocked:
- *Was pinned on the MacBook:* iOS screenshots upload + ASC listing metadata + iOS **Build 43** archive.
- *Was pinned on the Pixel 8A:* native Android screenshots + installing the closed-test build on-device.

**Shipped + pushed:**
- **Android signed release AAB — BUILT + verified + uploaded to Play.** `commit 4db84bf` bumps `versionCode 1→2` (Play rejected 1 as already used by a prior internal-testing upload). Built on Windows via `android/gradlew bundleRelease` using the existing `android/keystore.properties` + `C:\Users\catal\keys\apestogether-upload.jks` (alias `apestogether-upload`). **Play App Signing confirmed enrolled** — upload-key cert = `assetlinks.json` fingerprint A (`64:47:B5…`); Google's app-signing cert = fingerprint B (`0A:3A:A0…`). AAB: `android/app/build/outputs/bundle/release/app-release.aab` (8.26 MB, `versionCode 2`, target SDK 35).
- **Screenshot compositor `-AndroidStatusBar` mode** (`commit 27962e0`) — reuses the iOS captures behind `pixel_frame.png`, overpaints the iOS status bar + Dynamic Island with a synthetic Android status bar, auto-uses a 2:1 canvas (Play caps aspect at 2:1). First pass in `~/Downloads/Play_Screenshots`. Native Pixel captures now preferred for policy fidelity.
- **Docs:** October **1099 filing decision** (Xero partner vs self-file via IRS **IRIS TCC — up to 45-day lead**, FIRE retires end-2026) in `docs/XERO_W9_DEPLOY_CHECKLIST.md` §I; fundamentals migration marked applied.

**Play compliance forms + web policy (2026-07-07):**
- **Data safety DONE** — Advertising ID = **No** after the AD_ID fix (below); data types + Collected-not-Shared answers recorded in `docs/PLAY_STORE_LISTING_GUIDE.md` walkthrough.
- **Financial features DONE** — declared **"Other" → "Simulated (paper) stock-portfolio tracking & social sharing"** (exact label + description in `docs/PLAY_STORE_LISTING_GUIDE.md §2.9`, rewritten for the current UI); **Play confirmed no additional documentation is required.**
- **⚠ Advertising-ID fix → `versionCode 3` (commit `c86b89f`).** Firebase Analytics had auto-injected `com.google.android.gms.permission.AD_ID` + `ACCESS_ADSERVICES_AD_ID` and collected the GAID; now stripped via `tools:node="remove"` + `google_analytics_adid_collection_enabled=false`, **verified gone from the merged release manifest**. **The v2 AAB in Play is stale — rebuild + upload v3 before rollout.**
- **Web policy pages (deployed):** public no-login `/delete-account` page (retention wording fixed `1a9a5f8`) + footer link (`1b653d6`); privacy policy switched to Apple/Google IAP + Xero and links the delete page (`fb73076`); removed site-wide Stripe.js (`0ad9908`).

**In progress:** walking USER through the Play Console **Closed testing – Alpha** release + the full store-listing / compliance forms (exact field values sourced from `docs/PLAY_STORE_LISTING_GUIDE.md` + `docs/ASO_STRATEGY.md`).

**Still open:** **rebuild + upload the `versionCode 3` AAB** (ad-ID fix — the v2 bundle is stale) to *Closed testing – Alpha*; add ≥12 closed testers + start the 14-day clock; finish remaining Play **store-listing** fields (Data safety + Financial features **DONE**); iOS **Build 43** archive + ASC listing on the Mac; E2E money tests; attorney privacy policy.

---

## 🔁 Session 23 (2026-07-07) — copy-trade pause switch, creator-deletion notifications, login links, deleted-creator UI, AI-vs-human store copy

**Shipped (backend — needs a deploy to take effect):**
- **Public copy-trade PAUSE SWITCH.** `_is_email_trade_paused`/`_set_email_trade_paused` (`mobile_api.py`) + early-returns in `bot_email_trade` and `bot_process_pending_trades` (both return `status:'paused'`, HTTP 200, so the GAS parser still marks each email processed and nothing replays on resume). Admin toggle at `GET/POST /api/mobile/admin/bot/email-trade-pause`; the **GET now also accepts `?set=on|off`** so an admin can flip it by simply navigating in a logged-in browser (`require_admin_2fa` accepts the admin session). Also honors env `BOT_EMAIL_TRADE_PAUSED`. **Purpose:** liquidate the personal Public.com brokerage account without mirroring those confirmation emails into Wolff's copytrade bot.
  - **Pause ON:**  `https://apestogether.ai/api/mobile/admin/bot/email-trade-pause?set=on`
  - **Pause OFF:** `https://apestogether.ai/api/mobile/admin/bot/email-trade-pause?set=off`
  - **Status:**    `https://apestogether.ai/api/mobile/admin/bot/email-trade-pause` (must be logged into the admin panel `/admin` first).
- **Creator-deletion subscriber notifications.** `_notify_subscribers_creator_deleted` fans out **push + email** to a deleted creator's active subscribers, with a **platform-specific "How to cancel" CTA** (App Store vs Google Play, inferred from each subscriber's active `DeviceToken.platform`; both if unknown). Wired into **BOTH** the mobile `DELETE /auth/account` and web `/settings/delete-account` (best-effort, never blocks deletion). `GET /subscriptions` now returns a **`creator_deleted`** flag.

**Shipped (apps — ride in the pending Android `versionCode 3` rebuild + iOS Build 43):**
- **Login-screen legal links FIXED (both apps).** "Terms of Service" / "Privacy Policy" on the login screen are now **tappable** → `apestogether.ai/terms-of-service` + `/privacy-policy` (iOS SwiftUI markdown auto-link `LoginView.swift`; Android Compose `LinkAnnotation` `LoginScreen.kt`). They were previously plain text.
- **Subscriptions tab deleted-creator state (both apps).** New "*{creator} has left*" card (replaces View Portfolio / push controls) with a billing warning + **"How to cancel"** CTA — iOS opens the App Store manage-subscriptions sheet (`SubscriptionsView.swift`), Android opens Google Play subscriptions (`SubscriptionsScreen.kt`). Driven by the new `creator_deleted` field; models updated (iOS `SubscriptionMade.creatorDeleted`, Android `@SerialName("creator_deleted")`, both backward-compatible/default-false).

**Store copy:**
- **Full description REWORKED** (`docs/ASO_STRATEGY.md`) → framed as a **best-AI-models vs best-humans competition** throughout (opening, HOW IT WORKS, THE LEADERBOARD, trader benefits). Play closing longtail is now "verified **AI-vs-human** performance"; disclaimer broadened to "ApesTogether and the creators … are not investment advisers."
- **Feature graphic (1024×500) — Gemini prompt drafted** (in chat this session). Still needs generating + upload.

**Still open (unchanged priorities):** rebuild + upload the Android **`versionCode 3`** AAB (now carries the ad-ID fix **+** login links **+** deleted-creator UI); generate + upload the **feature graphic**; iOS **Build 43** archive + ASC listing on the Mac; add ≥12 closed testers + start the 14-day clock; E2E money tests; attorney privacy policy. **Deploy the backend** so the pause switch + creator-deletion notifications go live.

---

## 🔁 Session 24 (2026-07-08) — ASO v4/v4.1 slate locked + Play feature graphic shipped

- **ASO v4 locked** (`docs/ASO_STRATEGY.md`, now versioned v4): iOS Subtitle = keyword string `Top Picks Alerts Tracker Live` (29 char; subtitle renders tiny/truncated → pure keyword play, comma-free); iOS keyword field rebalanced (`pick`/`alert` promoted to subtitle, `gainer` dropped, `portfolio` + `screener` added — 95 char); Play short description = keyword string.
- **v4.1 correction:** Play Console flagged `top` in the short desc (*"Should not use keywords that indicate store performance or ranking"* — costs promotion eligibility, not publishing). Final: `Verified stock picks, trade alerts, portfolio tracker, AI trading, leaderboard` (78 char). **Rule recorded: no `top`/`best`/`#1` in ANY Play listing text or graphics** — automated checks don't parse intent.
- **Feature graphic DONE + UPLOADED.** Display-dynamics research (in ASO doc): without a promo video it NEVER shows on our own listing page — only on Google promo surfaces (collection cards / editorial / ads), so guideline compliance (minimal text, no wordmark, safe zone) gates its only exposure. Final: Gemini "human vs AI head" art + `The stock-picking leaderboard.` / `AI vs humans.` (7 words), normalized to exactly 1024×500 24-bit PNG → `assets/store/play_feature_graphic_1024x500.png`.
- Migrated kicker copy for screenshot captions/description: `See trades live. Traders keep 85%.` (ranking-language-safe).

---

## 🔁 Session 25 (2026-07-10) — Play listing fields live, v4 AAB + iOS Build 45, Android bug pass, desc v4.2

**USER-completed (Console/store actions):** Play **512×512 icon** generated + uploaded (`android/play_store_icon_512.png`, untracked — commit it); **Play listing text fields entered** (title + v4.1 short + v4.2 full desc); **Android `versionCode 4` AAB rebuilt + uploaded**; **iOS Build 45** archived + uploaded to ASC (Mac pulled to `60b7233`, bumped 44→45); **attorney engaged** — requirements sent 2026-07-09, Privacy Policy + ToS in progress.

**Backend deploy verification (USER asked):** Session 23 work IS live — `10052a1` (pause switch + creator-deletion notifications + login links + deleted-creator UI) sits on `origin/master` two commits before HEAD `60b7233`, so today's deploys included it, plus `14a50b5` (email idempotency) and the Wolff reversal.

**Android bug pass:** see the 🐛 list in §4 — (1) login wordmark space fixed in code (ships v5); (2) scale commas already shipped in v4 (`6c6a9db`, stale build on device); (3) A/C/B reshuffle root-caused to missing ORDER BY in `GET /subscriptions`, fixed server-side (deploy pending); (4) notification screenshot → admin Test Push.

**Store copy v4.2:** "not investment advice" strengthened in BOTH full descriptions (opening + explicit second line + "do not provide investment advice" in the closing disclaimer); Play version live in Console; iOS master updated in `docs/ASO_STRATEGY.md` (keeps Apple-specific cancel line + disclaimer paragraph).

**Trader API status (USER asked):** NOT BUILT — scoped only (Session 18 → `docs/TRADER_API_SCOPING.md`). UC-A (bots submit trades for an authenticated account via API key + scopes over the proven internal bot trade logic) is the v1 candidate; no routes or instructions exist yet; open decisions D-1..D-6. Its hard dependency (S-1 shared rate limiter) shipped Session 19, so it's buildable when prioritized.

**OPEN (new this session):**
- [x] Commit + push today's fixes; **deploy backend** (subscriptions ORDER BY). → Done Session 26 (`cf8b2b1`).
- [x] Android **v5 build** (fused wordmark) — built Session 26; AAB upload to Play closed testing still on USER.
- [ ] Capture the Android **notification screenshot** — now via the **Sample Trade** button (Session 26), not plain Test Push.
- [x] **Outreach language rewrite — DONE (Session 25).** Both docs now open with a style-rules banner; every message is paste-ready with `[bracketed]` slots. Fixed across ~70 messages: killed AI-flattery openers ("Your macro insights bring institutional clarity…") + "resonates/aligns" filler; plain direct register; brand fused everywhere; stale `June 1`/`Sunday`/`Memorial Day` → `[launch date]`/`[launch day]` placeholders; **removed the fabricated #X-26 stat** ("underperformed by 12%") and softened the unsourced "85%/20%" stats to defensible phrasing; scrubbed `copy trading` from press pitches (playbook §11) and added the virtual-trading disclosure to every journalist pitch; "report trades" → trades placed in-app. (Tester-recruitment email lives in `PLAY_STORE_LISTING_GUIDE.md` §4.5.)

---

## 🔁 Session 26 (2026-07-13) — commit/deploy, v5 build, Sample Trade push, scarcity strategy

- **Committed + pushed `cf8b2b1`** (12 files) → Vercel backend deploy carries: subscriptions ORDER BY fix (A/B/C reshuffle), admin Sample Trade preset, landing FAQ/urgency fixes, outreach rewrite docs. **No iOS action needed** — nothing in this commit touches Swift; next Mac pull/bump/archive is only when iOS code changes.
- **Android v5 built** (versionCode 5, fused login wordmark, comma formatting confirmed in-source): APK + AAB at `android/app/build/outputs/{apk/release/app-release.apk, bundle/release/app-release.aab}`. Direct `adb install` to the Pixel 8a for the scale-popup comma screenshot (uninstall first — Play-signed vs upload-key signature mismatch); **USER: upload the v5 AAB to Play closed testing** so the test track stops lagging the source (root cause of the "comma still broken on device" confusion — the fix shipped in v4 source but the Pixel was running an older build).
- **Sample-trade push for screenshots:** `/api/mobile/admin/test-push` now takes `preset: 'trade'` → production-formatted alert (🟢 Wolff's Flagship Fund BUY / 25 NVDA @ $184.10, `trade_alert` payload); admin panel Test Push card gained a **Sample Trade** button. This is the answer to "how did we do the iOS screenshot" — we never had a trade-formatted sender; the ASO doc's old tip pointed at the generic Test Push (now corrected in `docs/ASO_STRATEGY.md`).
- **Landing page fixes (live on deploy):** stale hero urgency `Beta opens June 1 — limited spots` → honest weekly-invite-wave line; two FAQ answers still describing the old *report-trades* model rewritten to the in-app execution model.
- **Demand & Scarcity strategy written** → `docs/MARKETING_PLAN.md` new top-level section (Doublespeed-article prompt): scarcity-vs-fabrication bright line, store policy answer (invite-gating allowed on both stores w/ reviewer access), Waitlist 2.0 referral mechanics, weekly invite drops, soft invite gate, founding-trader cap, pre-registration/pre-order, GEO/founder-led notes. Build items tracked in new **§11 backlog**.
- **§11 Post-launch feature backlog added** — Trader API + outbound feed + scarcity builds + v1.1 settings now have a permanent tracked home (previously only in session narratives/scoping docs).
- **Strategy verdict (after USER pushback):** invite-gate REJECTED — marketplace dynamics (every user = supply), track-record time is the ungateable core asset, scarcity amplifies attention but generates none, r/wsb receipts-culture fits the product but hates gatekeeping. **Adopted: open access + product-native urgency (track-record clock + Founding Trader badge) + USER's simple sequenced plan** — soft-launch open, harden during the closed-test window (Labor of Love lesson: never point the one-shot WSJ spike at a buggy app), then earned media with the AI-vs-human hook + micro-influencers + founder-led social. → MARKETING_PLAN §Verdict.
- **v5 installed on the Pixel 8a via adb** (uninstall + install, Play-signed→upload-key signature swap) and launched. Sample-trade preset default changed Wolff→divi51; landing urgency line neutralized ("Now in private beta — join the waitlist for early access") — USER correctly flagged that strategy language shouldn't have shipped to the site unprompted.
- **OPEN:** comma screenshot (scale popup, v5 now on device); Sample-Trade push → notification screenshot (needs the divi51 deploy, ~2 min after push); USER uploads v5 AAB to Play closed testing.

---

## 🔁 Session 27 (2026-07-13 PM) — sign-in SHA-1 root cause, Founding Trader rules, launch-docs alignment

- **Google Sign-In broken on the Pixel — ROOT-CAUSED, fix is a 2-min USER console action (no code, no rebuild).** The side-loaded v5 APK is signed with the **upload key**, whose SHA-1 was never registered for Google Sign-In. "It worked before" because previous installs were Play-delivered (re-signed with the **Play App Signing key** — registered) or debug builds (debug SHA-1 — registered). **FIX (USER):** Firebase Console → Project Settings → Your apps → Android `com.apestogether.app` → **Add fingerprint** → SHA-1 `49:2E:A2:EB:A3:CF:6D:6B:69:3C:17:66:A1:7A:56:FB:E5:31:79:FF` (optionally also SHA-256 `64:47:B5:21:9B:F5:9F:95:12:E1:5D:8C:1E:50:F8:CB:35:60:EA:61:45:A4:7D:8B:51:37:C5:44:7D:10:2B:57`). Propagates in minutes; then retry sign-in. ⚠️ If the console errors "OAuth client already exists," that's the §10 legacy-GCP-project SHA-1 conflict — delete the duplicate from the legacy project first. (Fingerprints via `keytool` against `C:/Users/catal/keys/apestogether-upload.jks`; debug SHA-1 for reference: `29:56:AA:D7:69:76:0E:66:57:69:FF:C2:C6:A8:31:58:C4:36:CC:1B`.)
- **v5 AAB uploaded to Play closed testing** ✅ (USER). Test track now matches source.
- **Founding Trader badge — STATUS CLARIFIED (USER asked "did you implement it?"): NO — it was docs-only.** Rules now formally defined in MARKETING_PLAN §"Founding Trader badge — RULES": first **100 human traders** by first-trade timestamp (beta trades count); **bots/house/admin/test accounts never qualify**; permanent + irrevocable; status-only (badge on leaderboard + profile, no perks that would corrupt leaderboard neutrality); honest public counter allowed once ~20+ claimed. Implementation tracked in §11 (still unbuilt).
- **Launch docs aligned to the adopted approach** (open access, no artificial scarcity, product-native urgency, harden-then-press sequencing, AI-vs-human hook): `LAUNCH_PLAYBOOK.md` got a governing **MARKETING APPROACH banner** + the stale `"Beta opens June 1 — limited spots"` landing rec marked superseded; `LAUNCH_CONTENT.md` got an approach note in its style banner + reworded **#X-19** ("limited group" → honest small-first-group + "everyone gets in at public launch"), **#TT-08** ("Limited spots." → factual beta-cap line), **#E-WELCOME** ("looking for founding traders" → concrete badge offer + track-record-clock line); `LAUNCH_OUTREACH.md` got an approach note (press/creator *early access* offers stay — time-based, not gated; badge = legitimate recruitment offer). Journalist "exclusive" offers and #E-CAMPUS founding-trader language reviewed — fine as-is.
- **OPEN:**
  - [ ] USER: add the upload-key SHA-1 fingerprint in Firebase → re-test Google Sign-In on the Pixel.
  - [ ] Then: scale-popup **comma screenshot** + Sample-Trade (divi51) **notification screenshot** (deploy is live).
  - [x] Founding Trader badge build (§11) — **DONE Session 28** (below).

---

## 🔁 Session 28 (2026-07-13 late PM) — Founding Trader badge BUILT, debug-build screenshot path, package-name explainer

- **Screenshot workaround (USER dislikes SHA juggling): use the DEBUG build — no Firebase change needed.** The debug keystore's SHA-1 (`29:56:AA:…`) has been registered since May 10 (§ANDROID setup) and debug shares the `com.apestogether.app` package + prod API (`build.gradle.kts` — no `applicationIdSuffix`). So: uninstall the sideloaded release v5 → `gradlew :app:installDebug` → sign-in works → device token registers → Sample-Trade (divi51) push arrives. The notification shade renders identically (same app name + icon) — fine for the ASO screenshot. The upload-key fingerprint add (Session 27 OPEN) is now OPTIONAL — only needed if we ever test sideloaded *release* APKs again; installing from the Play closed-testing track needs nothing.
- **Package-name confusion resolved:** `com.apestogether.app` = the **Android** package (hardcoded in Play Console, `google-services.json`, assetlinks — CORRECT, keep forever). `com.apestogether.ApesTogether` = the **iOS** bundle ID. The Firebase entry pending deletion is the stray **iOS** app that shipped in builds 1-33 with the wrong `BUNDLE_ID=com.apestogether.app` (§FCM-iOS fix log). ⚠️ **Do NOT delete the Android `com.apestogether.app` Firebase app** — that kills Android sign-in + FCM. The sign-in error naming `com.apestogether.app` is expected/correct: it's the Android app reporting its own package.
- **Founding Trader badge — IMPLEMENTED end-to-end** (rules → MARKETING_PLAN §"Founding Trader badge", now marked BUILT):
  - **Backend (`mobile_api.py`):** `FOUNDING_TRADER_CAP=100` + `_award_founding_trader_badges()` — one aggregate query (`MIN(timestamp)` over buy/sell Transactions), walks in first-trade order, awards `extra_data['founding_trader']={rank, first_trade_at, awarded_at}` to eligible humans only (`role=='user'`, not `is_company_owned`, not copytrade-bot, not soft-deleted); existing holders never touched (irrevocable); process-lifetime cap short-circuit. Triggers: (a) `execute_trade` post-commit when the COUNT says first trade (non-blocking), (b) `process_queued_trades` after the market-open settle batch (`services/trading_email.py` — covers a user whose FIRST trade was queued after-hours), (c) `POST /api/mobile/admin/founding-trader/award` (admin 2FA) for backfill/verification — **run this once after deploy to backfill beta traders**, response reports `{awarded, total_holders, cap_reached}`.
  - **Payloads:** `founding_trader: bool` on the leaderboard `user` object + `get_portfolio` `owner` object (additive — old clients unaffected).
  - **Android:** `LeaderboardUser`/`PortfolioOwner` DTO fields; `FoundingTraderChip` (gold `WorkspacePremium` + 8sp "FOUNDER", pill ≈ SubBadge line height) on the leaderboard row **stats line** — deliberately NOT next to the username so long names never truncate and row height is unchanged; `FoundingTraderPill` ("Founding Trader", 10/6dp + 20dp corner — dimensions match `LeaderboardBadgePill`) leads the profile badge row, which now also renders for founder-without-medals.
  - **iOS (needs Mac build):** same two surfaces, mirrored styling (`medal.fill`, gold FFD700, matching paddings) in `LeaderboardView.swift` + `PortfolioDetailView.swift`; DTO fields decode via `.convertFromSnakeCase`. Verified no manual `LeaderboardUser(`/`PortfolioOwner(` constructions anywhere, so the added fields can't break memberwise inits.
  - **Layout audit:** leaderboard chip sits inline with the 10sp sub-badges (star-count, trades/wk) inside the `weight(1f)` name column — worst case it clips off the right edge of that column, never pushes the sparkline/alpha cluster; profile pill row is a horizontal scroller on both platforms, so founder + multiple medals overflow by scrolling, not wrapping.
- **Award endpoint 405 — EXPLAINED + admin-panel button added.** USER pasted the URL into the browser → browser sends **GET**, endpoint is **POST-only** (it mutates — awards badges), hence `405 Method Not Allowed`. That's the endpoint working as designed, not a bug. Supported path: admin panel → **System Health** tab → new **"Founding Trader Badges" card → Run Award Sweep** button (`templates/admin_panel.html`, mirrors the Test Push card; renders the `{awarded, total_holders, cap_reached}` JSON — doubles as the "N/100 claimed" readout). Idempotent, safe to re-click.
- **Debug build installed on the Pixel 8a via `gradlew :app:installDebug`** (USER had uninstalled the sideloaded release v5) and launched via adb. Debug SHA-1 is registered → Google Sign-In works with zero Firebase changes. Build includes the badge UI (compiled from current source).
- **"Push sent but never arrived" — ROOT-CAUSED: FCM zombie socket, delivered late (NOT an app bug, no code change).** Sample Trade + Test Push both returned FCM success for all 4 tokens (incl. the fresh debug token, id 4) but nothing appeared on the Pixel. Diagnosis via adb: (a) on-device token store (`run-as com.apestogether.app cat shared_prefs/com.google.android.gms.appid.xml`) **exactly matches** DB token id 4 — registration chain correct; (b) POST_NOTIFICATIONS granted, `trade_alerts` channel exists at IMPORTANCE_HIGH; (c) `dumpsys activity service GcmService` showed **`Seen good heartbeat in last connection? false`** + socket reconnected only ~08:31 ET — AFTER the ~08:25 sends. FCM queued server-side and flushed on reconnect: `dumpsys notification` shows the trade alert POSTED on `trade_alerts` (`tag=FCM-Notification:…`, `seen=false` — sitting unseen in the shade). **Takeaway:** FCM "success" = accepted, not delivered; a zombie socket can hold pushes up to the ~28-min heartbeat. If a push lags during screenshot work, toggle Wi-Fi/airplane (forces reconnect) and resend — once the socket is fresh, delivery is ~instant.
- **OPEN:**
  - [ ] USER: after the Vercel deploy — sign in on the Pixel (debug build, installed + launched) → admin panel → System Health → **Sample Trade** push (bobford00) → notification-shade screenshot; **Run Award Sweep** (same tab) → verify FOUNDER chip on the leaderboard; scale-popup comma screenshot while in there.
  - [ ] Build Android v6 (badge UI) when convenient — badge is additive, so v5 keeps working meanwhile.
  - [ ] iOS: next Mac session — build + verify badge chips, then archive.
  - [ ] Web UI founder chip (post-launch, §11).

---

## 🔁 Session 29 (2026-07-14 PM) — earnings screenshot fixture, Android framing workflow, USER app-review fixes

- **Notification + scale/comma screenshots captured** ✅ (USER, via the debug build — Session 28 zombie-socket explanation held: resend after the reconnect landed instantly).
- **Earnings screenshot (the missing one) — how it works:** it's the **Screenshot Mode fixture** (aspirational-but-self-consistent Earnings card: 467 subs × $6.50 ≈ $3,036/mo), never real data — iOS = `forceScreenshotMode`/`-screenshotMode` in `SubscriptionsView.swift`; Android = `SHOW_SCREENSHOT_FIXTURE` in `SubscriptionsScreen.kt` (both `#if DEBUG`/`BuildConfig.DEBUG` gated, never ship enabled). **Flipped the Android flag, installed the fixture build on the Pixel, then reverted the source flag (uncommitted-flip workflow)** — the installed APK keeps rendering the fixture until the next install. USER: Subscriptions tab → capture → save as `06_earnings.png`.
- **Android screenshot-framing workflow — SEPARATE folder (USER decision: preserve the iOS set for future tweaks):** created `Downloads\raw_screenshots_android` pre-seeded with copies of the 6 iOS captures. USER overwrites the Android-native ones (`02_alerts.png` notification, `05_scale.png` comma scale, `06_earnings.png` earnings) + drops a Gemini **Pixel mockup with solid-magenta screen** as `frame.png` IN THAT FOLDER (`-FrameFile` defaults to `InputDir\frame.png`), then runs `compose_appstore_screenshots_v2.ps1 -InputDir "$env:USERPROFILE\Downloads\raw_screenshots_android" -AndroidStatusBar -OutputDir "$env:USERPROFILE\Downloads\Play_Screenshots"` (auto 2:1 canvas for Play's cap; synthetic Android status bar overpaints iOS chrome on reused captures). Script header updated with this workflow.
- **USER app-review batch (6 items):**
  1. **Leaderboard vs private portfolio gains mismatch intraday (1W/3M) — BY DESIGN, USER self-confirmed:** leaderboard serves pre-computed `LeaderboardCache` (populated at market close from EOD snapshots) while the private portfolio computes live intraday values; they converge after close. No fix.
  2. **RESOLVED (was truncated):** USER gifted the high-performing `fund.finance2024` bot 3 subs to rank it higher in Top Creators (Wolff underperforming). That gifting is also what surfaced item 5's error flash.
  3. **Daily Signups spike — EXPLAINED, label fix:** 'signups' counts EVERY new User row; in-app bot creations are `created_by='system'` (+ `apestogether_review`), so making bots spikes the purple line. Human growth = 'New Real Accounts'. Relabeled → **"Daily Signups (all accounts)"** in `admin_panel.html`.
  4. **Activity Feed (Command Center) dates — FIXED:** timestamps now render `MMM d, hh:mm` via `toLocaleString` (was time-only, ambiguous across days).
  5. **Top Creators error/retry after gifting subs — FIXED on both apps:** `/top-influencers` never 500s (returns empty on error), so the flash was a transient client-side failure (cold Vercel instance / network blip) surfaced as the full-screen error. Both `TopInfluencersViewModel`s (Android `TopInfluencersScreen.kt`, iOS `TopInfluencersView.swift`) now **retry once after 1.5s** before surfacing any error and **keep already-loaded entries on failed refreshes** (error screen only when there's nothing to show). Industry-filter change still resets to Loading (content invalid).
  6. **iOS login wordmark "Apes Together" → fused "ApesTogether" — FIXED** in `LoginView.swift` (space removed, two-tone kept; matches Android v5's fused wordmark). Ships with the next Mac build.
- **Earnings screenshot captured** ✅ (USER, via the fixture build) — clean debug build reinstalled on the Pixel afterwards (fixture cleared; flag was already reverted in source).
- **Play set now visually matches the iOS reference (USER flagged font-size/position/padding drift).** Root cause: fixed pixel constants + cover-cropping on the shorter 2:1 Play canvas (vs 1290x2796 reference) → text read ~8% bigger and the phone proportionally taller at equal display heights. Script fixes (`compose_appstore_screenshots_v2.ps1`): (a) `$vScale=$H/2796` scales caption/subtitle/disclaimer sizes, `CaptionTop`, and vertical margins (no-op at reference height → iOS output byte-identical; explicit params respected); (b) frame drawn **fit-height with 1px-column edge-extension** for horizontal deficit (cover-cropping ate ~200px of the mockup's breathing room) — Android screen band now lands at the same 19.8%–90.2% fractions as iOS; (c) per-frame `sb` flag: `02_alerts` uses `sb='noclock'` — strip overpainted (hides "No SIM — Emergency calls only") + right icons, but no 9:41 clock contradicting the lock screen's 7:08. All 6 Play frames regenerated + visually verified (incl. comma in 05, fixture card in 06).
- **Parity pass 2 (USER: line spacing still loose + text hugging the frame).** Two more root causes: (1) line pitch used `Font.GetHeight()` — a FAMILY metric: iOS set was rendered with SF Pro (lineSpacing/em 1.1934, fonts in `Downloads\sfpro`), Android fell back to Segoe UI (1.3301, +11.5%) → looser caption/subtitle/disclaimer pitch and a taller header block. Now pitch = fontSize × `$RefLine` (1.1934) — layout is font-independent, SF runs unchanged. (2) header clamp + disclaimer gap measured from the keyed SCREEN bbox, but the Pixel mockup's visible shell extends ~2x further above the screen than the iPhone's → subtitle butted the bezel. Script now scans the screen's center column for the outermost bright (lum>110) shell pixel → `bezelTop`/`bezelBottom` (prints `shell y=…`), and text gaps (56px @ ref height) measure from the shell edge. Regenerated + verified: pitch, subtitle-to-frame air, and disclaimer placement now match the iOS reference.
- **ATTORNEY BATCH (2026-07-15):**
  - **Ad pixels (attorney Q1): NONE** — full scan of `templates/`+`static/` found zero Meta/TikTok/X/GA/ad-tech scripts (only a TikTok social LINK + copy mention on landing). The "no sale/share" statement is TRUE; no CCPA opt-out build-out required.
  - **GPC (attorney Q2): NOW IMPLEMENTED** — `api/index.py` (+`app.py` dev mirror): `Sec-GPC: 1` recognized per 11 CCR §7025; persists a first-party `gpc_opt_out` cookie via `after_request` (nothing to disable today — recorded + enforceable if practices change); `/privacy-policy` shows an on-page "GPC detected & honored" banner.
  - **Revenue split (attorney Q3): CONFIRMED 85/15 after store fees** (`iap_validation_service.PRICING`): monthly $9.00 → $1.35 store (15%, Small Business Program) → $6.50 creator / $1.15 platform; annual $69 → $49.85 / $8.80. **ToS §5.2(a) "70% of gross" is WRONG** (actual = 85% of net ≈ 72.2% of gross) → attorney to redraft; fix needed in `templates/terms_of_service.html`, `legal/terms-of-service.md`, AND the iOS in-app copy `ios/ApesTogetherApp/LegalText.swift` (embeds full ToS + an old privacy policy — consider swapping to web links next Mac session). Landing footnote "85% of net proceeds after app store fees" is accurate.
  - **New CCPA/CPRA privacy policy PUBLISHED** — `templates/privacy_policy.html` fully rebuilt with the attorney's text verbatim (17 sections, both 11.1/11.2 tables with navy-header Word-style formatting, TOC anchors). Effective Date set to July 15, 2026. Accuracy fixes made so the policy is true: **both apps' Settings renamed "Account" → "Account & Privacy" + added "Request My Data" (mailto w/ subject) and "Delete My Account" rows** (§9.1/§11.7 describe exactly this); delete dialogs + iOS FAQ rewritten to describe the REAL 30-day-grace soft delete (old copy claimed "permanent, cannot be undone"). Flagged to attorney: §2.2 "location derived from IP" isn't actually derived (we store raw IPs only); §6 "app uses analytics and crash-reporting tools" = Firebase Analytics on Android only (iOS has no analytics SDK; crash data comes from Play vitals / Apple with user consent).
  - **"instant" → "real-time" sweep (attorney wording): DONE** — `landing.html` (×2), Android `PortfolioDetailScreen.kt` paywall, iOS `PortfolioDetailView.swift` paywall, and 9 outreach/store drafts in `docs/LAUNCH_CONTENT.md`. Intro carousels were ALREADY "real-time" (identical copy both platforms). Zero occurrences remain in user-facing copy.
  - **Unfiled-W-9 payout holding: CONFIRMED SAFE** — no-W-9 payouts get `payment_status='held'` (`mobile_api` monthly payout run); W-9 submission flips held→pending; NOTHING sweeps/expires/forfeits held balances or books them as platform revenue; purge cron retains `TaxpayerProfile`/`XeroPayoutRecord`/`InAppPurchase` even after account anonymization. (Held-forever is bookkeeping-only; bank-account segregation is operational, not in code.)
  - **Creator-deletion subscription cancellation: attorney is RIGHT about Google** — Play Developer API v3 `purchases.subscriptions...:cancel` stops auto-renew while keeping paid access to period end; feasible with our slot setup (per-purchase `product_id` + purchase token stored in `InAppPurchase.receipt_data`/`original_transaction_id`; same service-account scope already used for subscriptionsv2 validation). Apple has NO cancel API → deep-link `https://apps.apple.com/account/subscriptions` per attorney's fallback. Gap analysis vs his 8 steps logged in session notes; steps 3 (Google cancel), 4 (Apple deep link in email/push), 6 (explicit unavailability date), 7/8 (access through paid period; annual subs outlive the 30-day purge) NOT yet implemented — awaiting USER go-ahead + design decision on content availability.
- **OPEN:**
  - [~] USER: review `Downloads\Play_Screenshots` → upload to Play Console listing. **(Set regenerated Session 30 — `01_leaderboard` recaptured without Wolff.)**
  - [x] ~~USER: confirm go-ahead on the creator-deletion cancellation flow~~ — **BUILT + attorney answered (Session 30):** Google revoke + prorated refund (`_cancel_google_purchases`), Apple manage-subscriptions deep link fallback.
  - [ ] Attorney: redraft ToS §5.2(a) to 85%-of-net-after-store-fees; then update web ToS + `legal/terms-of-service.md` + iOS `LegalText.swift`.
  - [ ] iOS: next Mac session — build + verify badge chips, wordmark, Top Creators retry, **Account & Privacy settings rows, updated delete dialog/FAQ, paywall copy**; then archive.

---

## 🔁 Session 30 (2026-07-16/17) — attorney creator-deletion reply, MBGL phantom-sell incident, email-pipeline hardening, leaderboard screenshot redo

- **ATTORNEY — creator-deletion cancellation ANSWERED (closes the Session 29 open item).** Sent the platform-behavior summary he requested for the ToS: **Google Play** = on creator deletion the server programmatically **revokes each subscriber's subscription with a prorated refund** via Play Developer API (`_cancel_google_purchases(revoke=True)` → `revocationContext.proratedRefund`; Google fires `SUBSCRIPTION_REVOKED` RTDN which `iap_webhooks` books as the refund), plus email + push confirmation per subscriber. **Apple** = standard StoreKit, **no developer-initiated cancel/refund API exists** → per his fallback: subscribers get email + push with the `apps.apple.com/account/subscriptions` manage link, cancellation steps, and `reportaproblem.apple.com` refund instructions. Still awaited from attorney: ToS §5.2(a) redraft (85%-of-net).
- **MBGL INCIDENT (2026-07-17) — anatomy + resolution.** Wolff entered MBGL (first-ever position) 9:52 ET, but **Public's confirmation email mislabelled the entry buy as "You sold $24.00 of MBGL"**; the parser faithfully recorded a sell → no bot holds MBGL → deferred; the cluster resolver DID attribute it to bot 14 via the 5 same-minute sell anchors, but the executor's unheld-sell guard refused (correctly — mirroring only an exit fabricates cash) → `unroutable` #136. Then the admin's own **forward** of that email at 1:16 PM was re-ingested (GAS `GmailApp.search` returns threads → parses EVERY message incl. forwards; new msg-id defeats dedupe) → second phantom sell #144. **Resolution:** entry buy backfilled via explicit-bot `/admin/bot/email-trade` (1.16967 @ $20.52, ts `2026-07-17T13:52:54Z` → ×1.52 multiplier = 1.777898 sh, push fanned out); **#136 + #144 dismissed (USER)**; ledger `Fwd:` row proved the source.
- **PIPELINE HARDENING SHIPPED** (commits `3428f45` `9167e70` `76895b9` `96b4238`):
  - Sells of unheld tickers → distinct `skipped_not_held` status + admin email (was generic `unroutable`), with backfill-the-entry-then-Assign instructions.
  - **Manual Assign flow actually works now** — the described UI **never existed** (`setAssignData` had no caller = no button) AND the param was mismatched (UI `bot_id` vs API `bot_user_id` = guaranteed 400). Assign now executes through the shared executor **at the email's original timestamp** (a manual attribution IS a correct backfill), multiplier + guards applied, per-trade errors surfaced.
  - **Dismiss button** + `POST /admin/bot/dismiss-pending-trades` — close mislabelled/never-mirror rows without executing (status `dismissed`, drops out of the assignable pool).
  - **Duplicate-email guard** (`_is_duplicate_trade_suspect`) — identical ticker/action/qty within 24h **never auto-executes**: deferred `duplicate_suspect` at ingestion (would otherwise anchor-execute + liquidate), `skipped_duplicate` in the resolver, admin emailed. Protects the now-held MBGL position from further phantom "sold" emails.
  - **Forward/reply guard** — GAS now skips non-Public senders + `Fwd:`/`Re:` subjects inside threads; server backstop records `skipped_forward` in the ledger. **Ledger detail now lists ticker/action/qty** per email (old rows keep opaque detail).
  - Also this session (pre-MBGL): pipeline tab UX overhaul — all-statuses view, `Z`-suffix UTC timestamp fix, idempotency-ledger table (which is what identified the `Fwd:` culprit).
- **Play screenshot 01 REDONE** — USER recaptured the 1W leaderboard without Wolff (`raw_screenshots_android/01_leaderboard.png`), full 6-frame set regenerated deterministically → `Downloads\Play_Screenshots` (visually verified: `apestogether_review` on top, frame/captions clean). Upload to Play Console still open (master list §0.8).
- **OPEN:**
  - [x] **USER: paste the updated `scripts/public_email_parser.gs` into Apps Script — DONE (2026-07-17 3:43 PM).** Manual `checkForTradeEmails` run clean ("0 new, 325 previously seen" — the 1:16 PM forward is dedupe-filtered before the guard, so no skip line; guard applies to all future first-seen messages). Script Properties + trigger unchanged.
  - [x] USER: upload the `Play_Screenshots` set — **DONE (2026-07-20, all 6 in order)**. Beforehand (07-17 PM) **`02_alerts` was recaptured on BOTH platforms**: 3 fresh 🟢/🔴 alerts stamped “now” sent via `/admin/test-push` `preset:'trade'` overrides (the old frame's lone “· 10h” alert contradicted “Real-time”); Play frame = lock-screen group + count badge, iOS frame = expanded 3-alert stack **replacing the old Gemini mockup** — both sets recomposited (iOS with SF Pro fonts). iOS `02_alerts` still needs its ASC upload (ride the Build 46 listing session).
  - [ ] Attorney: ToS §5.2(a) redraft → then update web ToS + `legal/terms-of-service.md` + iOS `LegalText.swift` (carried from Session 29).

---

## 🔁 Session 31 (2026-07-20) — v6 AAB, advertising-ID declaration loop root-caused, closed testing SUBMITTED FOR REVIEW

- **v6 AAB built + committed (`fc8b270`).** v5's code was already consumed by an earlier upload (drag-drop puts the bundle in the app's **bundle library** the instant the upload completes, even if the draft is never saved — v5 AND v6 were both sitting there). Lesson: on "version code already used", don't rebuild — **Add from library**.
- **"Completely shadowed" errors** — the draft release had accumulated all 6 bundles; removed 1–5 from the release (⋮ → Remove from release), kept only 6 (1.0).
- **Advertising-ID declaration loop ROOT-CAUSED.** "No" wouldn't save ("Incomplete advertising ID declaration" persisted) because Play validates the declaration against **every active artifact in every track** — and Internal testing still served **v1**, built BEFORE the 07-07 AD_ID strip (`c86b89f`). Bundle-explorer proof: v1 lists `com.google.android.gms.permission.AD_ID` + `ACCESS_ADSERVICES_AD_ID` (12 perms); v6 lists neither (10 perms). **Fix: rolled v6 out to Internal testing (review-exempt, supersedes v1 immediately) → "No" then saved cleanly.**
- **Closed testing – Alpha: v6 release rolled out + all changes SENT FOR REVIEW** (release + App content declarations). First review can take up to ~7 days. Debug-symbols warning ignored (native libs from deps; optional `ndk.debugSymbolLevel = "FULL"` with the next real build — don't burn a versionCode on it).
- **OPEN:**
  - [ ] USER: smoke-test v6 from the internal track on the Pixel 8a (login, portfolio, push) — same binary going to production.
  - [ ] Await Play review verdict → **promote the approved release to Production** (Closed testing → Promote release).
  - [ ] Reviewer demo account (`reviewer@apestogether.ai`) — still needed for App access declaration follow-ups + Apple review (master list §5).

---

## 🔁 Session 32 (2026-07-21) — Play monetization setup, trial-offer correction, founder pill move, ToS 85/15

- **15% SERVICE FEE: ENROLLED (2026-07-21).** Account group created via the Payments-profile banner; no associated developer accounts. Payout math in `iap_validation_service.PRICING` now matches reality.
- **Primary payout method SET** — existing (already-verified) business checking selected as Primary; no micro-deposit wait.
- **⚠ TRIAL OFFER CORRECTION — trial belongs ONLY on Slot A.** Per `docs/PER_CREATOR_SUBSCRIPTION_SLOTS.md` §"One free trial per user": the 7-day intro offer goes ONLY on the Slot-1 products (`com.apestogether.subscription.monthly` / `.annual`); slots B–T bill immediately BY DESIGN (one lifetime trial, store-enforced — first sub always lands in Slot A). Cascade's Session-31 advice to add `trial7` to every product was WRONG; USER added it to Trader Subscription B (eligibility "Never had any subscription") → **REMOVE/deactivate that offer**, and instead **verify the two Slot-A products have an ACTIVE 7-day offer** (search `com.apestogether.subscription` in the Subscriptions list; offers column should show 1). iOS mirror: intro offer only on the original Slot-A subscription group; B–T groups have none (once-per-group enforced by Apple automatically).
- **Trial offers VERIFIED (2026-07-21 pm):** Slot-A products both carry ACTIVE offers (`monthly-7day-free` / `annual-7day-free`, eligibility "Never had any subscription") — matches the one-lifetime-trial design. USER's stray `trial7` on Trader Subscription B annual: **deactivated** (Play won't delete activated offers; deactivated = unredeemable, harmless).
- **Dynamic trial CTA — IMPLEMENTED (both platforms, same session).** "Try Free for 7 Days, then $X" now renders only while the store confirms eligibility; otherwise "Subscribe for $X". Android: `BillingService.trialEligible` StateFlow — Play pre-filters `subscriptionOfferDetails` per account, so a zero-price phase on the Slot-A pair ⇔ trial will apply (threaded through `PortfolioDetailScreen` + `LeaderboardScreen`; LeaderboardViewModel now prefetches products in init). iOS: `SubscriptionManager.trialEligible` via StoreKit 2 `isEligibleForIntroOffer` on the Slot-A group + shared `subscribeCtaText()` helper used by all 3 CTA sites (portfolio subscribe row, locked-holdings card, leaderboard expanded card). Optimistic default (true) until the store answers; billing itself was always correct — this is copy-honesty only. `ReferralPreviewScreen/View` "Sign in to start your free trial" left as-is (pre-auth, eligibility unknowable; audience is overwhelmingly new users).
- **Resizer bullet DECIDED + APPLIED:** "Adjust the portfolio size instantly" (USER's pick — info-only framing, no follow-the-trades implication).
- **Founder pill MOVED (both platforms, `de96edc`)** — public view: pill now renders inside `PortfolioHeroCard` under the portfolio value (permanent status on the identity block); medals row keeps only earned leaderboard badges. Owner view (no hero card): pill stays in the badges row.
- **iOS locked-holdings CTA fixes (`f940e65`)** — plan-pill highlight now equal height both sides (`fixedSize` + `maxHeight: .infinity`); middle benefit bullet de-duplicated to "Full position details". Third bullet (resizer) copy still pending — USER wants info-only framing (no "follow these trades" implication); 50 new candidates delivered, decision open. **Ships in Build 46.**
- **ToS 70/30 → 85/15 (numbers only, USER-approved)** — attorney's sentence structure kept verbatim; swapped seventy/70% → eighty-five/85% and thirty/30% → fifteen/15% in `templates/terms_of_service.html` §5.2(a), `legal/terms-of-service.md` §5.2, `ios/ApesTogetherApp/LegalText.swift`. NOTE: "of the gross subscription revenue" is still technically wrong (actual = 85% of net after store fees) — attorney redraft still owed.
- **Console-navigation findings (2026-07 console):** "App access" no longer appears as its own declaration — the reviewer-credentials declaration is **"Sign in details"** (app → Monitor and improve → Policy and programs → App content → Actioned → Sign in details → Manage). License testing remains ACCOUNT-level (All apps → Settings → License testing).
- [x] USER: `apestogether.review@gmail.com` added to License testing → "Internal testing" list — **DONE 2026-07-21.**
- [x] USER: "Sign in details" declaration confirmed to carry `apestogether.review@gmail.com` — **DONE 2026-07-21.**

### 📦 PENDING BUILDS — contents + WHEN to deploy (single source of truth)

- **Android v7 (versionCode 7)** — *client fixes riding `de96edc` + `fa95e73` + this session*: dynamic trial CTA (trial vs "Subscribe for $X" per store eligibility) + founder-pill move to hero header + **Play In-App Review prompt** (`ReviewPrompter.kt`, 3rd trade) + **acquisition survey** (`AcquisitionSurvey.kt` one-tap chips, shows once ~1.5s after authed launch — on-device check: dialog appears, tap posts to `/user/acquisition-source`, never re-shows).
  - **When:** build + upload ONLY after the v6 review verdict lands and v6 is **promoted to Production** (do NOT touch the Closed-testing release or App content before then — edits can reset the review queue). Flow: build AAB → Internal testing smoke on the Pixel 8a → Production update.
- **Android v8 — 📅 HARD DEADLINE Aug 31, 2026 (Play policy warnings 2026-07-21):**
  - [ ] `targetSdk` 35 → **36** (+ `compileSdk` 36, on-device sanity pass).
  - [ ] Play Billing Library `7.1.1` → **8.x** (breaking API changes — dedicated session; re-verify `BillingService.trialEligible` offer-filtering behavior after migration).
  - [ ] `ndk.debugSymbolLevel = "FULL"` (native-symbols warning).
  - [ ] **#6 referral-loop "happy-moment" surfaces** (approved 2026-07-22): share/referral prompt after badge award or a green day + influencer comp tied to referral codes — design next session, rides v8.
  - **When:** separate from v7 on purpose — don't couple a billing-library migration to simple copy fixes. If the v6 review drags into August, fold v7's fixes into v8 instead and ship one update before 8/31.
- **iOS Build 48 (future)** — acquisition survey (`AcquisitionSurveyView.swift`, registered in pbxproj — file is on master, rides whatever the next archive is) + #6 referral surfaces once designed. Build 47 was already uploaded before these landed.
- **iOS Build 47 — ✅ UPLOADED to ASC by USER (2026-07-22).** USER pulled master on the MacBook, bumped build 46→47, archived + uploaded. Carries everything since 45: dynamic trial CTA (`SubscriptionManager.trialEligible` + `subscribeCtaText()`), founder-pill move, locked-holdings CTA fixes (equal-height plan pills, de-duped bullet, resizer bullet "Adjust the portfolio size instantly"), `LegalText.swift` 85/15. ("Build 46" was never uploaded — 47 supersedes 45 directly.)
  - **Still open before SUBMITTING 47 for App Store review:** (a) verify the ASC Slot-A subscription group carries the 7-day intro offer; (b) spot-check the resizer bullet at 240pt on a device/simulator; (c) create the Apple reviewer demo account (`reviewer@apestogether.ai`, with followed traders) and put its credentials in App Review notes; (d) then Submit for Review.
- **OPEN:**
  - [ ] Attorney: §5.2(a) gross→net redraft — USER approved sending the before/after (2026-07-21) but is holding it until the attorney answers his current outstanding question; send as the follow-up.

### ▶ NEXT SESSION (planned 2026-07-22)

1. **Play Billing E2E on-device (the long-blocked money test #4) — now UNBLOCKED:** v6 is live on the Internal track and license testers are set (`bobford00`, `apestogether.review@gmail.com`). On the Pixel 8a via the Internal opt-in link, with a license-tester account: subscribe **monthly** (confirm the Play sheet shows the 7-day trial from `monthly-7day-free`), then **annual** on a second creator (slot B — confirm NO trial + immediate [test] charge), confirm backend `MobileSubscription` rows + 200 from `/purchase/validate`, then cancel via Play + verify downgrade. License-tester purchases are free/fake — no real money moves.
2. **Verify the v7 client fixes on-device (sideload debug build over USB):** founder pill in the hero header, and the dynamic trial CTA — after step 1 burns the test account's "never had any subscription" eligibility, the CTA should flip to "Subscribe for $69/yr" (live regression of `BillingService.trialEligible`).
3. **If the Mac is available: iOS Build 46 session** — archive + upload (supersedes 45); verify ASC Slot-A intro offer; check resizer bullet at 240pt; create/confirm the Apple reviewer demo account (`reviewer@apestogether.ai`, master list §5); **submit for App Store review**.
4. **Passive:** watch for the Play verdict → on approval, **promote v6 to Production** (= Android launch), then build/upload v7 per the PENDING BUILDS block.
5. **Parked:** attorney §5.2(a) follow-up (after his current answer); Android v8 compliance build (by 8/31).

---

## 🔁 Session 33 (2026-07-22) — marketing full-plan review, trigger-based launch sequencing, USER: "no rush — do it right"

- **Pixel was updating → yesterday's on-device items CARRIED to tomorrow (2026-07-23), unchanged:** (1) Play Billing E2E via Internal opt-in link (money test #4 — monthly w/ trial sheet check, annual on 2nd creator = slot B no-trial, backend rows + `/purchase/validate`, cancel); (2) sideload debug build → verify founder pill + dynamic trial CTA flip after step 1 burns eligibility; (3) Mac if available → iOS Build 46.
- **MARKETING FULL-PLAN REVIEW — DONE** (USER asked; directive: no launch rush, do it right). Reviewed `MARKETING_PLAN.md`, `LAUNCH_PLAYBOOK.md`, `LAUNCH_CONTENT.md` (100+ content pieces), `LAUNCH_OUTREACH.md` (influencer/journalist templates + named targets) + fresh channel research. **Verdict: plan is date-broken, not content-broken** — the 49-day calendar assumed 7 pre-launch audience-building weeks that never ran. Fix = take the Session-26 quiet-launch verdict seriously: **store availability ≠ launch moment.** Full write-up appended to `MARKETING_PLAN.md` §"Full-Plan Review & Trigger-Based Sequencing (Session 33)": Phases 0–4 with EXIT GATES instead of dates (quiet availability → trader-supply hunt → proof accumulation → one-shot WSJ-first spike → post-spike engine). Quiet weeks BUILD the moat (verified track-record history compounds); "no rush" is strategically correct.
- **Product Hunt DEPRIORITIZED** (2026 research: 500+ launches/day, AI-tool audience drift, B2C converts poorly, skip-if-pre-testimonials guidance) — never the launch moment; optional side-beat later.
- **12-item gap register** logged in MARKETING_PLAN — biggest: **no trader-supply pipeline** (journalists are name-listed, traders aren't; playbook's own "supply first" principle). Others: waitlist rot (needs monthly build-log nurture email), unsustainable posting cadence (adopted 3 X + 1 TikTok/wk floor), install-attribution hole ("how did you hear" onboarding survey), **Android in-app review prompt MISSING** (iOS has it in `TradeSheetView.swift`; → **added to v7 scope**), GEO comparison page unbuilt, press kit + App Preview video unstarted (now Phase-2 exit gates).
- **PHASE 0 ACTION ITEMS (can start immediately, no launch dependency):**
  - [ ] USER: **register social handles** (X `@ApesTogetherApp`, TikTok/IG `@apestogether`, LinkedIn, YouTube, Reddit joins) — squatting risk, overdue since Session 13; ~2h once. Click-by-click: `LAUNCH_EXECUTION_PLAN.md` §0.1.
  - [x] Cascade: **trader-recruitment pipeline** → `docs/TRADER_RECRUITMENT.md` (sourcing queries, 3 DM templates, follow-up rule, white-glove onboarding, 25-row tracker). USER fills the names Sat 7/26 per the day-by-day.
  - [x] Cascade: **waitlist nurture email #1** → `LAUNCH_CONTENT.md` #E-BUILDLOG-01 (+ trader-segment variant). Send AFTER both stores quietly live + deliverability verified (`LAUNCH_EXECUTION_PLAN.md` §1.2).
  - [x] Cascade: **Android Play In-App Review prompt** — `ReviewPrompter.kt` (3rd successful trade, mirrors iOS `TradeSheetView`), wired in `TradeSheet.kt`, `review-ktx 2.0.2` added. **Rides v7.**
  - [x] Cascade: **GEO comparison page** — approved + BUILT same day (`/compare`; see gap-plan status below).
- **NEW DOC: `docs/LAUNCH_EXECUTION_PLAN.md`** — Phases 0–4 click-by-click (handle registration, Build-47 ASC submission steps, v6 staged-rollout promotion, waitlist send mechanics, Phase-2 exit-gate table, spike-week run-of-show, post-spike engine) + **day-by-day calendar for 7/23–8/11** folding in every open ENG box (billing E2E, v7, v8-by-8/31, Build 47 submission, deliverability bug, attorney follow-up).
- **Social content pass (Session 33, this model):** `LAUNCH_CONTENT.md` — added global re-anchoring banner (posts run during quiet availability → "live now" replaces "beta opens [date]"; AI-vs-human standings become the headline hook once real data exists); rewrote the 4 highest-stakes pieces (#X-01 pinned thread, #X-LAUNCH spike thread, #TT-01, #R-LAUNCH wsb — now leads with AI-vs-human standings, self-deprecating, includes removed-post fallback to the Daily thread). Remaining ~95 pieces: solid after the Session-25 rewrite; treat as menu per the banner.
- **v7 scope now:** dynamic trial CTA + founder pill + **Play In-App Review prompt** + **acquisition survey** (added after all-7 gap approval).
- **GAP PLANS — ALL 7 APPROVED by USER (2026-07-22), status:**
  - [x] **#7 attribution survey — BUILT (backend + both clients).** Backend: `POST /api/mobile/user/acquisition-source` (first-write-wins into `User.extra_data`, 8 allowed sources) + admin rollup `GET /api/mobile/admin/acquisition-sources` (2FA-gated counts). Android: `AcquisitionSurvey.kt` host wired in `RootApp` (DataStore-gated, rides **v7**). iOS: `AcquisitionSurveyView.swift` overlay on `MainTabView` (UserDefaults-gated, registered in pbxproj, rides **Build 48** — 47 already uploaded).
  - [x] **#10 GEO comparison page — BUILT.** `templates/compare.html` ("ApesTogether vs Dub vs eToro": TL;DR verdict box, 10-row table, per-app deep dives, 6-item FAQ + FAQPage JSON-LD), routes in `api/index.py` + `app.py`, footer link on landing. Competitor claims kept qualitative + "check their current pricing" hedges + trademark disclaimer.
  - [ ] **#6 referral surfaces** — scoped into v8/Build 48 (see PENDING BUILDS).
  - [ ] **#8 press kit + `/press` page** — Cascade drafts next session; App Preview video stays USER's (Phase-2 gate).
  - [ ] **#3 cold-start seeding** — operational: weekly admin check that all AI bots traded in the last 5 sessions; no build.
  - [ ] **#11 store featuring** — post-spike only (Apple nomination form + Google pitch); no build.
  - [ ] **#12 KPI cadence** — Friday metrics note (installs / signups / subs / trader count) appended here weekly; USER owns the 10-min review.

---

## ▶ RESUME HERE — Sunday (AI tokens reset)

**Last worked: Session 11, 2026-06-04. Committed `7a21b67` (8 files, +254/−95).** Stopped because AI tokens ran low.

**Done this session (all committed):**
- **Android deep-link navigation bug — FIXED + VERIFIED on the physical Pixel 8a.** `MainActivity` now handles `onNewIntent` (warm start) and reads the slug from both the data Uri (App Links) and the `portfolio_slug` intent extra (FCM system-tray tap). All 4 cases (cold/warm × Uri/extra) passed on-device via adb; admin Test Push also delivered + displayed while backgrounded. → Pre-launch tests **#2 (FCM) and #3 (App Link handling) are confirmed on-device.** Details in **Section C**.
- **Google IAP backend — FIXED.** Migrated to `purchases.subscriptionsv2.get` (token-only), forwarded `product_id` end-to-end, per-product pricing (fixes annual subs 404'ing / being priced as monthly). Unit test updated, logic verified locally. **Not yet runnable on-device** (see #4 gates below). Details in **Section C**.

**Next steps, in priority order:**
1. **Play Billing (test #4) setup** — the only remaining on-device test, and it can't run over USB. Need: (a) set Vercel **`GOOGLE_PLAY_CREDENTIALS_JSON`** + **redeploy backend** (Section H); (b) publish both subscription products + base plans + the 7-day trial in Play Console; (c) add `bobford00` as a License Tester; (d) upload to an **internal-testing track** and install via that opt-in link. Then subscribe (monthly + annual) on the Pixel and confirm a `MobileSubscription` row + 200 from `/purchase/validate`. *(Ask Cascade to walk through this step by step.)*
2. **App Link `autoVerify`** — swap the placeholder SHA-256s in `public/.well-known/assetlinks.json` for the real debug + Play App Signing certs (see "USER ACTIONS" / Section C). Handling already works; only OS verification is pending.
3. **`/admin-panel` → API Usage tab shows all zeroes** (reported this session) — investigate. Logged in **Section H**.
4. *(Optional/low-risk)* real-device Google Sign-In (#1) and a real trade-alert FCM tap (rides along with #4 once a live subscription exists).

**Re-test the deep-link fix anytime (Pixel plugged in, logged in as bobford00):** clear logcat, launch the app plain to the feed first (clean baseline), then `adb shell am start -W -a android.intent.action.VIEW -d "https://apestogether.ai/p/<slug>" com.apestogether.app` — success = a `GET /api/mobile/portfolio/<slug>` appears in `adb logcat` (proves navigation). adb lives at `%LOCALAPPDATA%\Android\Sdk\platform-tools\adb.exe` (not on PATH).

---

## 🐛 Notification & Trade-Pipeline Bugs — triaged Session 13 (2026-06-08)

Five issues reported after Wolff's rebalance + a Public alert for a BRK.B buy. Root-caused against code; **fixes not yet applied** (triage only). Priority order for today is at the bottom.

- [x] **#3 (RESOLVED — DO NOT REVISIT) — Dotted tickers (BRK.B) dropped by the copytrade-bot email parser. FIXED + DEPLOYED + BACKFILLED (USER-confirmed complete).** **CORRECTION to first triage:** Wolff (user_id 14) is a copytrade bot whose trades are mirrored from **Public.com via the Google Apps Script `scripts/public_email_parser.gs`** → `/admin/bot/email-trade` — NOT the human `trade@trade.apestogether.ai` path. All **8** ticker regexes in that GAS used `([A-Z]{1,5})` (letters only), so `BRK.B` either **failed to match** (patterns 0b/1: dropped → nothing reached `/admin/bot/email-trade` → no `PendingTrade` → empty Email Trade Pipeline, matching the report) or **mis-captured as `BRK`/`B`** (patterns 0a/4). The backend (`bot_email_trade`) itself accepts dotted tickers fine. **Fixes applied:** (1) `scripts/public_email_parser.gs` — all 8 ticker groups now `([A-Z]{1,5}(?:\.[A-Z]{1,2})?)` (verified: captures `BRK.B`, no-op for `AAPL`); (2) `portfolio_performance.py` — added `_to_av_symbol()` so AlphaVantage gets the hyphen form `BRK-B` on **all three** call sites: bulk quote (`:148`+response map), single quote (`:278`), and historical `TIME_SERIES_DAILY` (`:402`) — the last one is exactly the "symbol-format mismatch like BRK.B" daily-close gap; also lets bot BRK.B trades get a price; (3) **bonus/defensive** `services/trading_email.py:20,44` — the *human* email parser now also accepts dotted tickers + normalizes to dot form (separate latent bug, not this incident). **REMAINING:** (a) **deploy the updated `.gs` into the live Apps Script project** (script.google.com as `bobford00`) — the repo copy is not the running code; (b) **backfill Wolff's missed BRK.B buy** (see below); (c) post-deploy, confirm a live `BRK.B` price comes back from AV. *RESOLVED 2026-06-14 (USER-confirmed, 3x): updated `.gs` deployed to the live Apps Script project, Wolff's missed BRK.B buy backfilled, and a live BRK.B price confirmed from AV. Closed — do not re-add.*

- [x] **#2 (HIGH) — Subscribers get email but no push. RESOLVED (Session 13). ACTUAL root cause was OPERATIONAL, not a code bug.** **What it actually was:** the iPhone's **iOS notification permission had been turned off** at the system level in the prior ~5 days. Re-enabling it in Settings was NOT enough — revoking iOS notification permission invalidates the APNs device token, and re-granting only re-registers a fresh token on the NEXT app launch (`AppDelegate.didFinishLaunching → requestAuthorization → registerForRemoteNotifications`, `ios/ApesTogetherApp/ApesTogetherApp.swift:35-48`). The DB still held the **stale** pre-revocation token, so FCM returned `success`+`message_id` while APNs silently dropped it. **Fix:** force-quit + reopen the iOS app → fresh token registered → push now delivers on BOTH iPhone + Android (verified via admin `Test Push` card, 3/3 tokens). **Diagnostic facts proven:** push-send code (`push_notification_service.py`) unchanged since 2026-05-10; iOS push reg unchanged since 2026-02-26; Trade Alerts commit `8a10115` touched ONLY `notification_history` (the in-app feed), zero push code; Test Push bypasses all fan-out/feed code, so no backend change could cause it. **Defensive code changes shipped anyway (commit `ead9dc6`, still valid):** (a) `mobile_api.py:3872` comp/bot subscribe no longer hardcodes `push_notifications_enabled=False` (now `True`); (b) `/admin/migrate-push-notification-defaults` exists to flip legacy subs (ran dry-run → 0 affected, so no existing sub was actually mis-flagged). **In-app Trade Alerts feed confirmed one-per-trade** regardless of push/email (commit `8a10115`, Transaction-sourced). **Possible future nicety:** surface token age / `last_registered_at` in Test Push output so a stale token is obvious at a glance. *Original triage below.* —
- [ ] **(orig triage) Subscribers get email but no push, despite "on by default".** Push + email both fan out from the single call site `cash_tracking.process_transaction` (`:154` push, `:180` email) — so email firing proves the path ran. Push is additionally gated by `sub.push_notifications_enabled` (`push_notification_service.py:426`) + an active `DeviceToken` + FCM availability. **Root cause (primary):** the admin/bot/comp subscription creator at `mobile_api.py:3867` hardcodes `push_notifications_enabled=False`, contradicting the model default `True` (`models.py:910`). The user's Wolff sub was almost certainly comped via this path → push disabled at the DB level even though the UI/expectation is "on." **Fix:** (a) stop hardcoding `False` at `mobile_api.py:3867` (use default `True`); (b) one-time data migration to set `push_notifications_enabled=True` for active subs currently `False`/`NULL`; (c) **confirm with a DB check** the user's sub flag value AND that an active `DeviceToken` exists (push was last verified via admin Test Push, which may bypass the per-sub filter). *Effort: low code + small migration; needs DB confirmation to rule out a missing token.*

- [~] **#5 (QUICK WIN) — Recent Trades shows only 5; want ~15. CODE SHIPPED (commit `ead9dc6`); needs app rebuild.** Cap was client-side display only; changed iOS `PortfolioDetailView.swift:296,298` (`prefix(5)`→`prefix(15)` + divider bound) and Android `PortfolioDetailScreen.kt:409` (`take(5)`→`take(15)`). Backend already returns up to 20 (`mobile_api.py:920`), so 15 is within range. **REMAINING (user):** rebuild iOS (on Mac/Xcode) + Android apps; needs ≥6 recent trades on a portfolio to visibly confirm.

- [x] **#1 (LOW) — Queued-trade email used the wrong icon (📈 chart instead of branded 🟢/🔴). FIXED + DEPLOYED (commit `df04d73`).** Extracted `build_trade_confirmation_email()` in `services/notification_utils.py` as the single source of truth for trader confirmation emails (green/red-dot subject + HTML table body). Both the inbound-email path (`trading_email._execute_single_trade`) and the queued-at-open path (`trading_email.process_queued_trades`) now use it instead of plain-text `send_email` with 📈/📉; `send_trade_confirmation_email` also delegates to it. Verified output: BUY→🟢 + HTML table, SELL→🔴 + position-%, custom heading/footer for the queued variant. Deployed to Vercel.

- [ ] **#4 (DEFER — real feature) — "Allow Subscribers" slider is not wired to anything.** iOS `SettingsView.swift:8` `@State private var allowSubscribers = true` with a `Toggle` at `:97` — purely local state, **no persistence**, unlike `preferFractional` which loads/saves server-side (`:293-317`). No backend field/endpoint exists (`allow_subscribers`/`subscription_enabled` absent from `mobile_api.py`/`models.py`), and nothing enforces it. **Full fix = a feature, not a wire-up:** new `User` column + migration + PUT endpoint + load-on-appear + **enforcement at subscribe time** (block `/subscribe`, hide/disable the Subscribe CTA on leaderboard/portfolio) + Android parity. *Effort: medium. Recommend scheduling separately, not bundling with today's bug pass.*

**Recommended order for today:** **#3** (silently dropping real trades is the worst — it breaks the core copy-trading promise) → **#2** (push is a core subscriber feature; small fix + migration, but confirm DB first) → **#5** (trivial, bundle in) → **#1** (cosmetic, bundle if time) → **#4** (defer; real feature work).

---

## 💳 Subscription Lifecycle & Billing — opened Session 14 (2026-06-14)

Cohesive cluster opened after a code audit of the cancel / trial / billing path (triggered by USER questions: “is cancel fully wired?”, “is the trial-end reminder wired?”). Findings + the work they spawned. Recommended order at the bottom.

- [x] **CTA copy standardized across both apps (Session 14).** All subscribe buttons now read **“Try Free for 7 Days, then $69/yr”** (annual) / **“…then $X/mo”** (monthly), toggle-aware. Research-backed (front-loads “Free”; keeps price on the button = Apple 3.1.2 disclosure). Changes: iOS `PortfolioDetailView.swift` below-chart button (`:104-106`) + bottom locked-overlay button (was “Start Free Trial”, `:1299-1301`); Android `PortfolioDetailScreen.kt` (`:750-751`). Removed the redundant grey **“7-day free trial, then $69/year ($5.75/mo)”** subtext from the iOS overlay (the button now carries the price; Terms/Privacy links remain — the grey “Cancel anytime…” subtext was later removed too; see the trial-end reminder item). iOS + Android **leaderboard** buttons already matched. **Ships on the next iOS archive / Android build. NOT yet committed.**

- [x] **In-app cancel is now a SINGLE store-routed action — REWORKED (Session 14, client; uncommitted).** The row button is relabeled **“Manage”** and opens ONLY the store’s own subscription screen — iOS `SubscriptionManager.openManageSubscriptions()` (StoreKit 2 in-app sheet); Android deep-links `https://play.google.com/store/account/subscriptions?package=<pkg>`. **Removed the local `/unsubscribe` fake-cancel entirely** (no more DB `status='canceled'` flip, no premature access/payout removal): the store is the single source of truth, matching Netflix/Spotify/etc. Alerts reworded to non-destructive (“Canceling there stops future billing; you keep access until the period ends”). The app reflects the real cancel/expire/refund state via the ASSN V2 + RTDN webhooks (next item) on next resume — so there is **no scenario where the app says “canceled” while the store keeps billing**. `mobile_api.py:607 /unsubscribe` is now unused by mobile (left in place, harmless). *(Original finding: in-app “Cancel” only marked our DB canceled and never took the user to the store → charge-after-cancel + App Store rejection risk.)*

- [~] **Per-creator subscription SLOTS — CODE BUILT (Session 15), store products CREATED by USER (Session 18); migration + app builds + on-device test pending.** Root problem found while answering “with multiple subs, which one is the user canceling on the Manage page?”: the app used **one shared product for every creator**, so the stores (one active sub per group/product, products must be pre-approved) physically **blocked a 2nd concurrent creator-sub** (Android `ITEM_ALREADY_OWNED`; iOS would crossgrade within the group) and showed **no creator** on the Manage page. Fix = **Twitch-style bounded slot pool**: 20 generic, same-price products (**Slot A..T**), each its **own iOS subscription group** (so they’re independently cancelable); the backend maps **slot→creator per-user** via new `MobileSubscription.slot`. New `subscription_slots.py` (Slot 1 = the existing product IDs) + `GET /subscriptions/slot-for-creator` (returns lowest free slot, or `already_subscribed` / `max_reached`); `/purchase/validate` derives the slot from the **authoritative** `product_id`; `/subscriptions` returns `slot_label`. Clients resolve the slot product **before** purchase (iOS `SubscriptionManager.subscribe`; Android **both** `PortfolioDetail` + `Leaderboard` flows + new `BillingService.queryProduct`), show a **“Subscription A” chip** per row, and the **Manage dialog names the exact store entry** to cancel. **One free trial per user, lifetime** = intro offer ONLY on Slot 1 (the first sub always lands in Slot 1; stores refuse a 2nd group-1 trial forever, even after slot reuse — zero app logic). S2S webhooks already match by store token, so **slot reuse is safe**. Android built+installed on Pixel; backend `py_compile` OK. **REMAINING (config, user):** (1) run `scripts/migrations/2026_06_14_subscription_slot.sql` in Supabase; (2) create Slots 2–20 — ✅ **DONE (Session 18, USER)** — 19 iOS subscription **groups** (monthly+annual) + 38 Play products created, all same price as Slot 1 (`docs/PER_CREATOR_SUBSCRIPTION_SLOTS.md`); (3) iOS archive + Android rebuild pick up the client side. **Then on-device test: subscribe to 2 creators → 2 independent store entries + correct A/B labels + cancel-one-keep-other.** *(Design + constraints: uniform pricing required; max 20 concurrent/user.)*

- [~] **Server-to-server subscription notifications — CODE BUILT (Session 14), external config pending.** New `iap_webhooks.py` + two public routes: `POST /api/mobile/webhooks/apple/notifications` (verifies Apple’s `x5c`-signed JWS, decodes the notification + nested `signedTransactionInfo`, updates the matching InAppPurchase/MobileSubscription by `originalTransactionId`) and `POST /api/mobile/webhooks/google/rtdn` (decodes the Pub/Sub message, re-fetches authoritative state via `validate_google_purchase`, updates by `purchaseToken`). Handlers only UPDATE existing rows (never create); refund/revoke → `expired`. Both files compile. **Full step-by-step config guide: `docs/IAP_WEBHOOK_SETUP.md`.** **REMAINING (config, user):** Apple — set the ASSN V2 URL in App Store Connect (prod **and** sandbox) and pin Apple’s root via `APPLE_ROOT_CA_G3_PEM` or `certs/AppleRootCA-G3.cer` (else it falls back to chain-structure + leaf-signature verification with a logged warning). Google — create a Pub/Sub topic, grant the Play system service account Publisher, set it in Play Console → Monetization setup, add a push subscription POSTing to the RTDN URL. Then test via App Store Connect “Request a Test Notification” + Play Console “Send test notification”. *(Original gap: backend only learned of renew/cancel/expire/refund when the client next called `POST /purchase/validate`.)*

- [x] **Trial-end reminder — RESOLVED by removing the promise (Session 14).** Decided NOT to build a reminder. Research finding: **Apple does not send a pre-conversion (“trial ending”) notice in the US** — only a sign-up receipt (renewal date + price) and price-increase notices; dedicated trial reminders are driven by non-US consumer-protection law. Apple’s omission is conversion-friendly, and we want conversions too. So we **removed the grey “Cancel anytime…” subtext** from the iOS overlay (`PortfolioDetailView.swift`) and confirmed no other surface promises a reminder (web `public_portfolio.html` only says “Cancel anytime”, which is accurate and stays). The button’s price/trial disclosure + Terms/Privacy links keep us Apple-3.1.2 compliant. *(A server-side reminder via `push_notification_service.py` remains a future option if conversion testing ever favors it.)*

- [ ] **(MED) Cancel-path testing (no real payments).** **iOS:** use the new `Configuration.storekit` + Xcode **Debug ▸ StoreKit ▸ Manage Transactions** to simulate cancel / refund / expire / renew, then reopen the app → confirm `/purchase/validate` flips the status + access gating; Sandbox subs also auto-renew fast and auto-cancel after ~6 renewals. **Android:** license tester cancels via **Play ▸ Subscriptions**, reopen the app to re-validate. *(Observed Session 14: Google test subs renewed several times then auto-canceled with emails — expected Play license-test behavior, not a bug.)* Confirm `MobileSubscription.status` + that access is actually revoked in both apps.

- [x] **Accounting/payout test harness — DONE + RUN (Session 14, committed `33a903b`).** RUN 2026-06-14: lifecycle math correct ($6.50 influencer / $1.15 platform / $1.35 store while active; $0 once canceled/expired/refunded); live dry-run showed **0 active subscribers** right now; **Xero `token_valid:false` (expired 2026-03-23) — reconnect before any payout sync (see Section H).** New `POST /admin/bot/simulate-subscription-lifecycle` (`@require_admin_2fa`, DRY-RUN, **no DB writes / no Xero sync**): (1) walks one sub through PURCHASED → RENEWED → CANCELED → EXPIRED → REFUND and shows, at each step, whether it’s still payable + the payout breakdown using the SAME `XeroPayoutRecord.calculate_totals()` math and the same `status=='active'` rule as `generate-payout-records`; (2) read-only live count of current active subs per creator → what this period’s payouts WOULD be (company bots = $0), with grand totals; (3) optional `include_xero:true` → `get_xero_status()`. Body: `{ "base_active_subs": 0, "include_xero": false }`. **Note surfaced:** flipping a sub to `canceled` immediately drops it from the payable count (matches the active-only query). (Actual Xero money-movement still needs sandbox→prod transactions — overlaps Section F “E2E paid subscription flow”.)

- [x] **Payout integrity overhaul — transaction-driven payouts + refund reversal + carry-forward clawback (Session 16, code; uncommitted, tested).** Audit of the month-end payout path found three money bugs, all now fixed in code (`tests/test_payout_integrity.py`, 9 passing). **(1) KEYSTONE — renewals were never booked.** The ASSN V2 / RTDN handlers only *updated* the existing `InAppPurchase` row on a renewal, so every renewal month produced **no revenue row and no payout** → creators underpaid from month 2 onward. New `iap_webhooks.py::_record_renewal` now inserts a fresh `InAppPurchase` row per verified renewal (Apple `DID_RENEW`, Google renewal/recovered), cloning the prior period's economics, deduped by renewal `transaction_id`. **(2) Annual subs overpaid.** `bot_generate_payout_records` summed `active count × $6.50` every month, so an **annual** sub paid the creator 12×. Now sums real earnings directly from the period's non-refunded `InAppPurchase` rows; `create_bill_for_payout` bills the record's actual $ (not count×rate). **(3) Refunds/chargebacks never reversed.** New `xero_service.py::reverse_refunded_purchase[s]` issues idempotent credit notes (revenue 4010 `ACCRECCREDIT` + store fee 6100 `ACCPAYCREDIT`) keyed by `XeroSyncLog`; best-effort hook in the webhooks + authoritative sweep `POST /admin/xero/reconcile-refunds`. Refunds landing *after* a creator was already paid net against the next month via `mobile_api.py::_compute_creator_clawback` (greedy, bounded so the check is never negative, carry-forward) tracked by new `in_app_purchase.payout_reversed_at`. Idempotency unique index `uq_payout_user_period` added. **Known limit (OK for launch):** renewal rows clone the prior period's price, so a consented store price change would book the old amount until re-validation (no price changes planned). **REMAINING (USER):** (a) run the migration below before deploy; (b) commit; (c) reconnect Xero (Section H) before any sync.
- [ ] **USER ACTION: run `scripts/migrations/2026_06_17_payout_integrity.sql`** in Supabase before deploying the Session 16 payout work (adds `in_app_purchase.payout_reversed_at` + the `uq_payout_user_period` unique index).
- [x] **1099 readiness dashboard + W-9 retry/reconcile (Session 16, code; uncommitted, tested).** `GET /api/mobile/admin/tax/1099-readiness?year=YYYY` buckets every creator by YTD payout vs the $600 1099-NEC threshold: `action_required` (paid ≥ $600, **no W-9** — chase it), `reportable` (paid ≥ $600, W-9 on file — file a 1099-NEC), `at_risk` (obligation ≥ $600, no W-9), `below_threshold`. `POST /api/mobile/admin/tax/w9/retry-sync` retries failed Xero W-9 pushes — since the full TIN is never stored locally it **reconciles** against Xero (`xero_service.reconcile_w9_on_file`): if the contact now has a `TaxNumber` it marks the creator on file + releases held payouts; the rest land in `needs_reprompt` to re-collect in-app. Monthly-close runbook: generate-payout-records → reconcile-refunds → review 1099-readiness → pay → mark `paid`.
- [~] **Track 3 store/webhook config — env vars + ASSN V2 + RTDN + cert-pin + slot products (Session 16, walkthrough started).** No new code — console + env tasks. **VERIFIED Session 16:** `GET /api/mobile/health` returns all green — `firebase: configured`, `iap.apple: configured` (`APPLE_SHARED_SECRET` on Vercel), `iap.google: configured` (`GOOGLE_PLAY_CREDENTIALS_JSON` on Vercel), `jwt: configured` (`JWT_SECRET` set). **Apple root-cert pin DONE** — `certs/AppleRootCA-G3.cer` is git-tracked + verified genuine (self-signed `CN=Apple Root CA - G3`), so `verify_apple_jws` pins to the real root without needing `APPLE_ROOT_CA_G3_PEM` (confirm post-deploy by sending an App Store Connect Test Notification and checking the log has no "not pinned" warning — confirms the file is in the Vercel bundle). **STILL OPEN:** (a) confirm `ADMIN_API_KEY` + `ADMIN_TOTP_SECRET` on Vercel (Section A); (b) reconnect Xero (Section H, token expired); (c) store-console config — set the ASSN V2 URL in App Store Connect (prod+sandbox), create the Google RTDN Pub/Sub topic + push subscription, and create Slot 2–20 products in both stores (see the SLOTS + ASSN/RTDN items above).
- [~] **Track 3 store/webhook config — Session 17 progress.** **Apple:** ASSN V2 URL `…/api/mobile/webhooks/apple/notifications` entered in App Store Connect (prod+sandbox). **No "Send Test Notification" button is present in this App Store Connect view**, so the endpoint will be verified by a **real sandbox subscription** instead (see the discrete test item below). **Google RTDN:** Pub/Sub topic `play-rtdn` being created in GCP project `apestogether-play-api`; STILL TODO on the topic — (a) grant `google-play-developer-notifications@system.gserviceaccount.com` the **Pub/Sub Publisher** role, (b) create a **Push** subscription → `…/api/mobile/webhooks/google/rtdn`, (c) paste `projects/apestogether-play-api/topics/play-rtdn` into Play Console → Monetization setup → RTDN. **Verify the project is right:** it must match the `project_id` inside the Vercel `GOOGLE_PLAY_CREDENTIALS_JSON` AND Play Console → Settings → Developer account → API access "linked project" (the `apes-together`/bobford00 project is the Firebase/FCM one, a different concern).
- [ ] **USER: verify the Apple ASSN endpoint via a real sandbox subscription** — the "Send Test Notification" button is absent in App Store Connect, so this is the test path. Subscribe with a sandbox Apple ID → confirm the webhook returns **HTTP 200** and Vercel logs show an `[ASSN]` line. (Rides along with the on-device IAP testing.)
- [x] **Monthly payout cron — SHIPPED (Session 17, code; uncommitted).** New `/api/cron/monthly-payouts` (`api/index.py`, cron-authed via `verify_cron_request`) runs the **3rd of each month @ 09:00 UTC** (`vercel.json`) and calls `mobile_api.run_monthly_payout_pipeline()`: generates the **PRIOR** month's `XeroPayoutRecord` rows (extracted helper `_generate_payout_records_for_period`) **and** syncs them to Xero as ACCPAY bills. **Creates bills only — never marks anything paid**, so after the 1st the operator just opens Xero → Bills to pay and cuts the checks. End-to-end idempotent (unique `(creator, period)` index + sync skips already-synced / W-9-held), so an accidental re-run is harmless. The manual `POST /admin/bot/generate-payout-records` now shares the same helper (behavior unchanged).
- [x] **Google RTDN — VERIFIED end-to-end (Session 18, 2026-06-22).** Topic `projects/apestogether-play-api/topics/play-rtdn` created; **Pub/Sub Publisher** granted to `google-play-developer-notifications@system.gserviceaccount.com`; push subscription `play-rtdn-push` → `https://apestogether.ai/api/mobile/webhooks/google/rtdn` (no OIDC auth — handler re-fetches authoritative state; **Never-expire**; exponential-backoff retry); Play Console → Monetization setup → RTDN **enabled**, content = **"Subscriptions and voided purchases only"** (app is subscription-only). **Play Console "Send test notification" → Vercel logged `[RTDN] test notification received` + HTTP `200`.** *(GCP project assumed `apestogether-play-api` — the Vercel `GOOGLE_PLAY_CREDENTIALS_JSON` is marked Sensitive so its `project_id` couldn't be re-confirmed; if RTDN ever stops arriving, that mismatch is the first thing to check.)* Apple ASSN still pending its sandbox-subscription test (item above).
- [x] **Google `SUBSCRIPTION_STATE_PAUSED` — CONFIRMED handled (Session 18, no code change).** Play Pause is Enabled (Google default). `iap_validation_service.py:538-543` maps `SUBSCRIPTION_STATE_PAUSED` (also `ON_HOLD`/`PENDING`) → `SubscriptionStatus.EXPIRED` = **access gated OFF during the pause**; on resume Google fires a `RENEWED`/`RECOVERED` RTDN → re-validate → `expires_date > now` → back to `ACTIVE` (and `_record_renewal` books the new period for payouts). Correct entitlement behavior, no money risk. *(Minor: our DB stores the paused interval as `expired` rather than the distinct `paused` status the model allows — cosmetic only; access + payouts are right.)*
- [~] **Android App Links `autoVerify` — SHA-256s WRITTEN to `public/.well-known/assetlinks.json` (Session 18); pending deploy + on-device verify (supersedes the RESUME-HERE #2 note).** Both real fingerprints from **Play Console → Protected with Play → Play Store protection → Manage Play app signing** are now in the file: **slot 1 = Upload key** `64:47:B5:…:2B:57`, **slot 2 = Play App signing key** `0A:3A:A0:…:1C:CA` (the latter also matches the new *Android developer verification* page, confirming it's the production cert). Vercel already serves it at `https://apestogether.ai/.well-known/assetlinks.json` (explicit route in `vercel.json`, mirrors the iOS AASA route). **Remaining:** deploy, then verify the live URL returns the JSON + on-device App Link verification. *(Serving was broken pre-Session-18: `vercel.json` routed `/.well-known/*` to `/public/...` static dests that had no static builder → Vercel 404, shadowing the existing Flask routes at `api/index.py:12003/12019`. Fixed `8bad07d`: removed those routes, `android_asset_links()` now serves the committed file from disk via `includeFiles`, AASA falls through to its Flask route.)*
- [x] **Google Sign-In project-of-record CLARIFIED + already production-verified (Session 18 audit; cross-ref the 2026-06-09 Pre-launch-testing entry).** Google Sign-In does NOT run through Firebase Auth. Android uses Credential Manager with serverClientId = `GOOGLE_WEB_CLIENT_ID` (`654567882865-4skl...`, the "ApesTogether Web Client" in GCP project `654567882865` / bobford00); the backend (`mobile_api._verify_google_id_token`) checks the ID token `aud` against Vercel `GOOGLE_ANDROID_CLIENT_ID` (= that same web client ID). The **Play app-signing SHA-1 `8F:A7:83:2D...`** is ALREADY registered as the Android OAuth client for `com.apestogether.app` in project `654567882865`, and sign-in was VERIFIED on a Play-signed build (Pixel 8a, 2026-06-09). **Nothing needs adding to Firebase** -- the Firebase project `apestogether-2c749` (#1096138595577) is push (FCM) + bundled config only, so `google-services.json` having `oauth_client: 0` is expected. **Only open confirm:** Vercel still has `GOOGLE_ANDROID_CLIENT_ID=654567882865-4skl...`, `APPLE_BUNDLE_ID=com.apestogether.ApesTogether`, and `STRICT_OAUTH_VERIFICATION=enforce` (without the last, `/auth/token` uses the INSECURE decode-without-verification fallback). **[CONFIRMED LIVE]** A forged-but-well-formed Google ID token POSTed to `/api/mobile/auth/token` returns `401 {"error":"invalid_google_token"}` (the strict signature-verify path) rather than `400 email_required_for_new_user` (the legacy decode path) — so `enforce` is genuinely active, not merely present (the var is marked Sensitive in Vercel so the value can't be eyeballed). `/api/mobile/health` reports `iap.apple`, `iap.google`, `jwt`, and `firebase` all `configured`.
- [x] **Firebase debug-SHA "duplicate OAuth client" warning -- EXPECTED / harmless (Session 18; corrects an earlier mis-diagnosis in this list).** It fires because GCP project `654567882865` (the Sign-In project, bobford00) legitimately owns the (debug SHA-1 `29:56...`, `com.apestogether.app`) Android OAuth client, so Firebase project `apestogether-2c749` cannot also claim that exact combo. **Do NOT delete the client in `654567882865` -- it is the functional Sign-In client; removing it breaks Google Sign-In.** Firebase here is push-only and FCM needs no SHA-1, so the warning has zero functional impact. *Optional cosmetic silence:* remove the debug SHA-1 from the Firebase Android app. **Real (optional) cleanups:** (a) stray iOS Firebase app 'Apes Together iOS' (bundle `com.apestogether.app`, appID ...7f2b...) is a mistake -- the shipping app uses 'ApesTogether iOS (correct)' (`com.apestogether.ApesTogether`, appID ...ef605...); safe to remove the stray. (b) iOS uses **Apple Sign-In only** (no Google), so the plist having no CLIENT_ID is correct. *(The saved SHA you held = the debug keystore; safe to discard; not needed in assetlinks.json.)*
- [x] **Bot username pool — CONFIRMED expanded + wired (Session 14).** `bot_personas.generate_username()` produces **5 styles** — CamelCase compounds (e.g. `CoastHillBear`), lowercase-fused, short handles, real-ish names, letters+number — for **~1M+** unique combinations, no underscores/corporate suffixes. Wired into creation via `generate_bot_batch` → `bot_agent.py:133/135` + admin `POST /admin/bot/batch-seed` (`mobile_api.py:8094`) and `/admin/bot/auto-create-run` (`:8386`). **Existing bots keep their pre-expansion names** — generate a fresh batch to see the variety.

**Status (Session 14):** CTA copy, in-app cancel deep-links + reword, trial-reminder removal, ASSN V2 + RTDN endpoint *code*, and the accounting/payout test harness are all DONE (uncommitted). **Remaining:** commit the client + backend changes → next iOS archive / Android build; do the store-side config for ASSN V2 + RTDN (App Store Connect URL + root-cert pin; Play Console RTDN topic + Pub/Sub push); then USER runs the on-device cancel-path tests.

---

## ⏰ Monday market-open checklist (next trading day)

Things to verify when the market is open and the bot pipeline is running. Hit each URL and report back.

- [ ] **Drift detector still clean.** `/admin-panel` → System Health → Cash-Tracking Drift + Snapshot-Drift cards. Click *Run now* on each. After commit `7549f80` (May 31) the audits bucket trades by **UTC calendar date** (matching the snapshot writer's `func.date(timestamp)`) and tolerate **all post-market-close trades (>= 20:00 UTC)** — so the EOD bot-trade wave (~20:05-20:20 UTC) no longer produces false-positive drift. Local validation on May 31 showed **0 users / 0 bad snapshots** fleet-wide. Copytrade bots (`CoastHillBear`, `marblethehill72`) are now skipped entirely (`copytrade_bots_skipped` in the response). If genuine drift reappears, it's an INTRADAY (<20:00 UTC) discrepancy worth investigating.
- [ ] **AlphaVantage logging populated.** `/admin-panel` → Bot Management → Market Research Data Sources. After the 9:45 AM ET wave finishes, News (NEWS_SENTIMENT) and Movers (TOP_GAINERS_LOSERS) rows should show non-zero call counts in the last 24h. If still showing `no_calls`, the HTTP fallback isn't firing — check Vercel logs for `/admin/bot/log-av-calls` POSTs.
- [ ] **Bot trades show realistic prices.** `/admin-panel` → System Health → Recent Trades. Verify trades from this morning's wave have plausible prices (not $0.01, not $99999) and the right `Source` (`bot_research` or `bulk_api`). **ALSO**: this doubles as a numpy-on-Vercel sanity check — commit `04811e7` (May 12) revealed numpy is missing from the Vercel bundle for the admin Finnhub probe. If today's bot-trade-wave cron also lacks numpy, no new trades will appear and Vercel logs will show `NameError: name 'np' is not defined` from `bot_data_hub.py` (RSI/ADX/EMA functions). If that's the case, options are: (a) add `includeFiles: ["**/numpy/**"]` to `vercel.json`'s build config to force-bundle it, or (b) move bot-trade-wave to GitHub Actions per `bot_data_hub.py:8-13` docstring's original intent.
- [x] **#4 intraday spike/dip anomaly (apex1575 spike / divi51 dip) — ROOT-CAUSED + FIXED (session 9).** NOT an open-time/price bug. It's a **read-skew race** in `cash_tracking.calculate_portfolio_value_with_cash` (the intraday snapshot writer): it reads `user.cash_proceeds` and the `Stock` holdings in **two separate queries**. `_execute_bot_trade_wave` commits holdings + cash atomically, but when a trade lands **between** those two reads, the row stores post-trade `stock_value` with pre-trade `cash` (spike on a buy) or vice-versa (dip on a sell), then the next 15-min tick reads cleanly → "snaps back". Confirmed from intraday data: both anomalies at **2026-06-01 15:45 UTC** (apex1575 a ~$20.5k buy: stock +$20.7k at 15:45 but cash didn't drop until 16:00; divi51 a ~$3.1k sell: mirror). **Daily snapshots are clean** (they replay cash from transactions) — only the **1D intraday chart** and the ~15-min **1D leaderboard** rebuild are affected. This is the intraday twin of the daily race documented in `/admin/audit-snapshot-cash-drift` (panther2585 May 7-8). **Fix:** live path now reads cash → computes stock → re-reads cash; if cash moved mid-read, it recomputes holdings so both sides reflect the same committed state (converges immediately when no trade is concurrent). **New fleet audit:** `/admin/audit-intraday-anomalies` (read-only; `?fingerprint_only=true` isolates the cash-lag signature; `?fix=true` deletes phantom rows). **RESOLVED Jun 1 (session 9):** deployed `16435b9`; fleet scan checked **3,211 intraday points across 14 users** and found **only 2** phantom rows — `apex1575` #15590 (spike) + `divi51` #15596 (dip), both `2026-06-01 15:45 UTC` — confirming this was NOT systemic. Both deleted via `?fix=true` (`rows_deleted: 2`); stale 1D/5D `user_portfolio_chart_cache` rows for users 3 & 9 cleared so the live charts (`/api/portfolio/intraday/*`) regenerate clean. **Phase 2 (session 9 — BUILT, awaiting fleet run):** new endpoint `/admin/audit-snapshot-stock-value` validates the one field no other audit checks — `PortfolioSnapshot.stock_value`. For each daily snapshot it replays holdings as-of-date (buy/initial +qty, sell −qty) and reprices from the `MarketData` daily close (exact-date, else weekend carry-forward), then buckets issues: **drift** (fully priced but stored value wrong — bad live-day price/qty), **missing_price** (held ticker with no `MarketData` close → `calculate_portfolio_value` silently skips it, undervaluing the snapshot — needs a MarketData backfill, never auto-fixed), **stale_weekday_price** (weekday MarketData gap), **absurd_price** (close ≤ 0). Copytrade bots skipped. Read-only by default; `?fix=true` repairs ONLY clean drift rows (`stock_value=expected`, `total_value=expected+cash_proceeds`). Params: `?threshold=0.01&abs_floor=1.00&days=N&role=&limit_users=N`.

**Phase 2 fleet-run finding (session 9):** the transaction-replay oracle is **only valid for real users** (`role='user'` — e.g. `bobford00` validated clean, `expected≈actual`). For **bots (`role='agent'`)** it produces massive FALSE POSITIVES: most bots were seeded via the legacy `/admin/bot/add-stocks` path which created `Stock` holdings **without `Transaction` rows** (the `add_stocks` cost-basis bug — `@/Users/catal/CascadeProjects/stock-portfolio-app/bot_executor.py:471`; the newer `_seed_bot_portfolio` in `mobile_api.py` fixed it by routing through `process_transaction`), and bots' pre-creation `PortfolioSnapshot` history was **synthetically backfilled** (uniform `first_bad_date` 2026-03-19). So replay yields 0/partial holdings → bogus `-100% drift`. **DO NOT** run `?fix=true` on agents — it would zero their portfolios. **Guardrail added:** the stock-value `?fix` now refuses any row with `expected<=0` or `holdings_count==0`.

**Bot-aware validator (session 9, BUILT):** `/admin/audit-bot-portfolio-integrity` — READ-ONLY, no transaction replay. Per bot: (1) **ledger coverage** — current `Stock` holdings vs txn replay → `untracked_holdings` (the add_stocks signature) + qty mismatches; (2) **latest snapshot vs live holdings** — current `Stock` × latest `MarketData` close vs newest snapshot `stock_value` (catches a corrupted value on the row users see TODAY); (3) **series plausibility** — `total_value != stock_value+cash_proceeds` and spike/dip-and-revert (daily analog of the #4 intraday corruption); (4) **synthetic-history** count (snapshots predating first txn). Params: `?holdings_threshold=0.10&spike_pct=0.25&limit_users=N&include_copytrade=true`.

**Audit results (session 9, RAN):** Both audits = good news for what users see. **Real user `bobford00`: clean** — `snapshots_with_drift: 0`, `max_abs_drift 0.0`; all 41 flags are `stale_weekday_price` on `AAPL` where `expected≈actual` (MarketData coverage gaps, not snapshot errors). **Bots: current values + charts clean** — `bots_with_latest_snapshot_drift: 0`, `bots_with_series_anomalies: 0` (no chart corruption; live writer reads `Stock` directly). The only real defect = **28 `untracked_holdings` across 6 bots** (candle3873, apex1575, divi51, zen1889, panther3765, chart1658) — the confirmed `add_stocks` seed bug. Smoking gun: `divi51` `VYM` current 104.9051 vs replay 14.9051 (= 90.0 missing) and `panther3765` `VRTX` 64.0793 vs 19.0793 (= 45.0 missing) — round-number seed lots missing, fractional later trades present. Large unflagged `drift_pct` (candle3873 33%, chart1658 65%) is purely **MarketData coverage** (DXCM/ARKK/ABT/RSG/HON/SBUX/ADBE/CVX/ITW have no close → `live_holdings_value` undercounts), correctly not flagged. **Secondary finding:** MarketData daily-close coverage is broadly sparse (even AAPL stale 41/65 of bobford00's dates) — separate infra item.

**Untracked-holdings backfill (session 9, BUILT):** `/admin/backfill-bot-untracked-holdings` — DRY-RUN by default. For each (bot,ticker) with `current_qty > replay_qty`: INSERT `Transaction(type='initial', qty=current-replay, price=Stock.purchase_price, timestamp=first_snapshot_date, price_source='backfill_seed')` and `max_cash_deployed += seed_value`. Deliberately does NOT use `process_transaction` (it would consume current `cash_proceeds` from later sells). Provably correct: later trades' max increments depend only on the `cash_proceeds` series (unchanged by the seed), so `current_max + seed_value == correct_max`. Params: `?commit=true&user_id=N&include_copytrade=true`. On commit, purges affected users from `user_portfolio_chart_cache`. Heads-up: raising `max_cash_deployed` LOWERS the previously-inflated displayed return % for these bots.

**MarketData daily-close coverage (session 9, BUILT):** two endpoints to close the sparse-`market_data` gap (the source `get_historical_price` reads to value/validate snapshots — it's populated only on-demand, so sparse). Key insight: the `daily_price_bar` table is refreshed daily for the whole ~145-name bot universe (`bot_data_hub.get_all_tickers`), so it already holds most of the closes `market_data` lacks — fill from there, no API calls. (1) `/admin/audit-marketdata-coverage` — READ-ONLY; per held/transacted ticker reports `fillable_from_dpb` (DailyPriceBar has, MarketData lacks), `missing_everywhere` (neither has, vs SPY's dpb trading calendar), and `tickers_needing_av_fetch` (held names outside the universe → `daily_price_bar` never fetches them). Params `?days=120&ticker=XXX`. (2) `/admin/backfill-marketdata-from-dailybars` — DRY-RUN by default; copies `DailyPriceBar.close` → `MarketData(ticker,date,close_price,timestamp=NULL)` for the fillable set; idempotent; `?commit=true&days=N&ticker=XXX`. **Important:** this improves AUDIT fidelity + any FUTURE snapshot recompute — it does NOT retroactively change already-stored `PortfolioSnapshot` rows (those are correct/served as-is). Tickers outside the universe still need a fresh AlphaVantage `TIME_SERIES_DAILY` pull.

**MarketData coverage — dpb backfill DONE + AV-fetch BUILT (session 9):** ran `/admin/audit-marketdata-coverage` (window 2026-02-02→today, 84 SPY trading days, 119 held/transacted tickers): **2,529 fillable-from-dpb rows** + **357 missing-everywhere rows**. **COMMITTED `/admin/backfill-marketdata-from-dailybars?commit=true`** = 2,529 daily-close rows inserted across 81 in-universe tickers (md was a strict subset of dpb, so zero dup risk). Remaining gap = **38 held tickers OUTSIDE the bot universe** (`tickers_needing_av_fetch`: BRK.B, AVGO, MU, OXY, ELV, MSTR, MELI, defense BWXT/KTOS/DRS/KBR/CACI/PSN, crypto-miners CIFR/CLSK/IREN/WULF, etc. — clearly a real user's diversified book, `daily_price_bar` has 0 rows for them) — already 74–81/84 covered on-demand, only recent 3–10 days missing each. Built **`/admin/backfill-marketdata-av-fetch`** (DRY-RUN default): targets any held ticker with calendar dates neither `market_data` nor `daily_price_bar` has, calls `PortfolioPerformanceCalculator.get_historical_price(force_fetch=True)` once per ticker (AV `TIME_SERIES_DAILY` → ~100 days cached, self-commits per ticker). Params `?commit=true&days=120&ticker=XXX&limit=50&max_seconds=45`; idempotent + resumable (re-run if `remaining_tickers_this_run` non-empty). `maxDuration=60s` so limit/budget keep it safe. **AV-fetch COMMITTED + CLOSED (session 9):** `/admin/backfill-marketdata-av-fetch?commit=true` fetched all **43 tickers (43 ok, 0 failed)** in 27.7s → **357 rows filled, 0 still-missing** (even BRK.B resolved — AV accepts the dotted symbol, no alias map needed). **MarketData coverage pass: CLOSED** — total **2,886 daily-close rows** added (2,529 dpb copy + 357 AV), every held/transacted ticker now fully covered over the 84-day window. **TODO (verify):** re-run `/admin/audit-marketdata-coverage` (`total_fillable_from_dpb_rows` + `total_missing_everywhere_rows` → ~0) and `/admin/audit-snapshot-stock-value?role=user` (stale_weekday_price flags should clear).

**Backfill COMMITTED + VERIFIED CLEAN (session 9, DONE):** ran divi51 (`user_id 9`) as pilot first (7 txns, $81,882.80; re-audit confirmed it dropped out of issues entirely), then unscoped `?commit=true` for the remaining 5 bots (apex1575, chart1658, panther3765 incl. VRTX qty_mismatch, zen1889, candle3873 = 23 txns, $398,971.80; divi51 correctly skipped as idempotent). **Total = 30 `initial` seed txns / ~$480,854.60 capital recovered across all 6 bots.** Final `/admin/audit-bot-portfolio-integrity` = **`bots_with_issues: 0`, `total_untracked_holdings: 0`, all flags zero** — every agent is now transaction-replay-validatable and `synthetic_history` cleared (first txn now == first snapshot date 2026-03-19). Chart caches purged on each commit. **Bot integrity audit pass: CLOSED.** Only remaining audit-pass item = MarketData daily-close coverage (sparse: AAPL stale 41/65, and `DXCM`/`ARKK`/`ABT`/`RSG`/`HON`/`SBUX`/`ADBE`/`CVX`/`ITW` unpriced) — separate infra backfill, pending decision.
**Holdings ~+0% gains BUG — FIXED (session 9, `c80e65a`):** USER reported every holding in both their own portfolio (bobford00) AND a subscribed portfolio (Wolff's Flagship Fund) showing ~+0% / ~+$0 gain (with tiny ~$0.50 wobble), despite correct charts. ROOT CAUSE: the profile/holdings endpoint (`@/Users/catal/CascadeProjects/stock-portfolio-app/mobile_api.py:780`) sources `current_price` from `get_batch_stock_data` (AV `REALTIME_BULK_QUOTES`); when that returns no `data` (the bulk endpoint isn't reliably served on the current AV plan — confirm via `/admin/inspect-ticker?ticker=AAPL` → `live_av_quote.status`), `current_price` fell back to `purchase_price`, so client-side gain = `current - purchase` ≈ 0 (the ~$0.50 = `round(purchase_price,2)` artifact × qty). Charts were unaffected because snapshots use a DIFFERENT price path (`calculate_portfolio_value` → `get_stock_data` → `GLOBAL_QUOTE`, which works). FIX: display-layer fallback resolves `current_price` as live bulk quote → most recent real daily close from `market_data` (just backfilled for every held ticker) → purchase_price; applied to both `portfolio_value` and per-holding `current_price`. `get_batch_stock_data` left unchanged so trade-execution never transacts on a stale close. **TODO:** consider a deeper fix for `REALTIME_BULK_QUOTES` itself (it also feeds intraday snapshot writers + trade-exec) — e.g. chunked `GLOBAL_QUOTE` fallback inside `get_batch_stock_data` — if `/admin/inspect-ticker` confirms the bulk endpoint is dead on the plan.

**MarketData coverage re-audit (session 9):** post-backfill `/admin/audit-marketdata-coverage` now shows only the **daily incremental gap** (`2026-06-03` = today's close not yet copied/fetched): `total_fillable_from_dpb_rows: 159` (all today's bars across the universe), `total_missing_everywhere: 43` (today for out-of-universe held names + a fresh `NEE` add with 0 md rows). This is EXPECTED churn, not the old historical hole — `market_data` only gets today's close on-demand and `daily_price_bar`'s daily refresh isn't auto-copied into `market_data`. Consider a small daily cron step to copy that day's dpb→md + AV-fetch out-of-universe held tickers (durable version of the two backfill endpoints). bobford00 stock-value audit now down to **2 minor drifts** (2026-04-08 +$364.68/+3.93%, 2026-05-29 −$149.88/−1.24%) + today's `stale_weekday_price` (resolves once today's close lands) — drift rows are safe to `?fix=true`.

**Email green dot — FIXED (session 9):** trade emails showed the buy/sell dot (`🟢`/`🔴`) in BOTH subject and HTML `<h2>` body heading. USER wants it only in the subject. Removed `{emoji}` from the `<h2>` in `send_trade_notification_to_subscriber` + `send_trade_confirmation_email` (`@/Users/catal/CascadeProjects/stock-portfolio-app/services/notification_utils.py:202,239`); subject keeps the dot. (NOTE: the email-reply trade path in `services/trading_email.py:336` uses 📈/📉 in its plain-text body — different indicator, left alone.)

**Email latency (~2 min Public→ApesTogether) — EXPLAINED (session 9):** the pipeline is GAS-driven, NOT Vercel-cron (vercel.json has no email-trade/process-pending-trades cron). Drivers: (1) **GAS Gmail-poll interval** — Apps Script time-driven triggers fire at most every 1 min (avg ~30s to pick up Public's email); (2) **deferred-BUY routing** — `bot_email_trade` executes only unique-holder SELLs immediately; BUYs + ambiguous sells park in `PendingTrade` and are routed by a separate `process-pending-trades` run that deliberately waits a 60s cluster window (CLUSTER_GAP_SEC) to disambiguate concurrent rebalances — so a BUY = GAS pickup + next routing run + 60s settle ≈ the ~2 min observed; (3) Public→Gmail delivery (secs–1min); (4) SendGrid send+deliver (secs–30s); (5) Vercel cold start (1–3s). SELLs notify near-immediately; the 2-min case is the deferred-BUY path. Only free win: ensure both GAS triggers run every 1 min. True real-time would need Gmail push via Pub/Sub (big build, marginal benefit). **Verdict: 2 min is reasonable; drivers make sense.**

- [x] **Display names render correctly on iOS.** Confirmed May 10 — app shows "The Grok Portfolio" and "Wolff's Flagship Fund" everywhere after iOS Build 32 was archived.
- [~] **Push notifications fire for copytrade-bot trades.** FCM infrastructure verified end-to-end May 12 via the new `/api/mobile/admin/test-push` endpoint (commit `7a86dc0`) — push delivered to both iOS Build 34 and Android with FCM `message_id` returned. The `email-trade` fan-out code path (commit `8ce748c`) still needs a real-trade smoke test: next Public.com trade-confirmation email through the GAS parser → `/api/mobile/admin/bot/email-trade` → `notify_subscribers_of_trade` should reach bobford00's iPhone within ~30s. Vercel logs for the email-trade endpoint will include a `notifications: {push_sent, email_sent, errors}` block.
- [x] **panther2585 1M chart fix verified.** ✅ Confirmed by USER this session — 1M chart no longer shows the spurious ~20% drop at the end. Other periods also visually OK.
- [x] **bobford00 performance audit.** ✅ PASS (May 31). Holds AAPL×8/NVDA×25/TSLA×10, no trades since 5/11. `max_cash_deployed` flat $10,783.56, `cash_proceeds` flat ($2.16 dividend from 5/11), 0 day-over-day jumps ≥8%, every row `total == stock+cash`, returns 1D +0.04% / 1W +1.46% / 1M +10.57% (plausible for tech holdings).

---

## A. Technical / Backend

- [x] **Bot ~60% return cliff (6 bots) — ROOT-CAUSED + FIXED (session 10).** The session-9 `/admin/backfill-bot-untracked-holdings` premise was WRONG: these bots were seeded via `set-cash` + `add-stocks`, so `max_cash_deployed` was already set to the full seed value at creation (day-0 snapshot == pre-backfill `user.max`). The backfill then ADDED `seed_value` again → `max_cash_deployed` double-counted → post-backfill EOD snapshot inflated → ~60% discontinuity on 1M/YTD. **Fix:** new `/admin/revert-bot-untracked-backfill` (commit `8439e17`): `target_max = current_user_max - seed_value` (pre-backfill peak); per daily+intraday snapshot clamp inflated → target / restore corrupted → target; resets `user.max_cash_deployed`; KEEPS the `initial` seed txns (a seed IS a buy — product decision, commit `7588b2d`); purges chart cache. Made **idempotent** + added `force_max` recovery (commit `4f25693`) after the non-idempotent first version double-subtracted on divi51 (186573→104690→22807). **DONE:** all 6 bots reverted (apex1575/chart1658/panther3765/zen1889/candle3873 clamped; divi51 recovered to flat 104690.38 via `force_max`), market-close cron re-run, CF_net=$0 + sane 1D returns fleet-wide. Charts verified correct by USER.
- [x] **avg_trades_per_week excludes dividends (session 10, commit `7588b2d`).** Dividends are income, not trades. Added `transaction_type != 'dividend'` filter in `leaderboard_utils.py` (batch trade-count) and `mobile_api.py` profile trade-count. Seeds (`initial`)/buys/sells still count.
- [x] **Trade Alerts duplication FIXED (session 10, commit `8a10115`).**
- [x] **Landmine cleanup — neutralized the two endpoints that caused the cliff (session 10).** (1) `/admin/backfill-bot-untracked-holdings` no longer mutates `max_cash_deployed` (removed `+= seed_value` double-count); it now ONLY inserts the missing `initial` seed txns to complete the ledger so the audit can replay-validate holdings — safe to run on any future add-stocks-seeded bot. (2) `/admin/fix-bot-snapshot-maxcash` (the replay-based snapshot fixer that flattened divi51) is now DEPRECATED — returns HTTP 410 unless `?allow_deprecated_replay_fix=true`, and points to `/admin/revert-bot-untracked-backfill`. The READ-ONLY `/admin/audit-bot-portfolio-integrity` is unchanged (it correctly reports `untracked_holdings`; the bug was always in the remediation, not the audit — and with the session-9 seed txns kept, it should now report `untracked_holdings: 0`).
- [x] **1D intraday-chart "prior-day points" — INVESTIGATED, NOT A BUG (session 10).** `/api/cron/collect-intraday-data` (every 15 min weekdays) is correctly gated to market hours only (9:30 AM-4 PM ET, skips weekends + `is_market_holiday`). The "0 intraday / 26 points all dated prior day" seen in a market-close log was from manually triggering `market-close` at 12:53 AM ET (off-hours, no current-day intraday data) → correct daily-snapshot fallback in `performance_calculator.py:370-386`, chart x-axis borrows prior-day SPY intraday series. Normal 4:05 PM ET run has real data. Mobile `/portfolio/<slug>/chart` shares the same fallback so users never see an empty 1D chart. Residual is cosmetic only (cache regenerated at 9:31 AM open shows prior-day shape ~15 min until next collection). No fix warranted.
- [x] Fix CoastHillBear chart spike bug (capital-deployment-aware replay)
- [x] Add `@require_admin_2fa` to `rebuild_leaderboard_cache_single`
- [x] `FOR UPDATE NOWAIT` on leaderboard_cache SELECT — fail-fast instead of 120s hang
- [x] Drift-detection System Health card in `/admin-panel`
- [x] AlphaVantage API logging from GitHub Actions — batch endpoint + HTTP fallback in `_log_av_api_call`
- [x] `display_name` column on User + serializers + iOS Models — public-facing names that bypass the username regex
- [x] **USER ACTION**: display_name values set May 10 via Supabase SQL Editor (`UPDATE "user" SET display_name = ... WHERE username = ...`) for `marblethehill72` ("The Grok Portfolio") and `CoastHillBear` ("Wolff's Flagship Fund"). Verified rendering on iOS.
- [x] Audit-endpoint tolerance buffer for trades within ±60s of cron firing (commit `34a01dc`, May 10) — eliminates Vercel-cron-jitter false positives in `/admin/audit-snapshot-cash-drift`, `/admin/audit-snapshot-max-cash-drift`, and `/api/cron/snapshot-audit`.
- [x] Push + email notification fan-out added to copytrade-bot email-trade endpoint (commit `8ce748c`, May 10). Notifications now use `public_name` so message reads "Wolff's Flagship Fund just bought…" instead of internal username.
- [x] **VERIFIED May 12**: bobford00 has 2 active `device_token` rows — `id=2` (iOS, fresh FCM token after Build 34 refresh, `updated_at=2026-05-12T16:29`) and `id=3` (Android, `created_at=2026-05-10`). Test push delivered to both.
- [x] Investigate panther2585 ~20% drop at end of 1M chart — ✅ confirmed fixed (verified by USER, see Monday checklist)
- [x] Audit bobford00 1D/1W/1M performance — ✅ PASS May 31 (clean, returns track holdings, no discontinuities)
- [x] **Audit other `mobile_api.py` admin endpoints for missing auth.** ✅ Completed this session — full sweep of all admin routes across `mobile_api.py` (51 routes), `api/index.py` (~40), `admin_interface.py`, `admin_cash_tracking.py`, `admin_phase_1-5_routes.py`, `admin_performance_test_routes.py`, `admin_snapshot_diagnostic.py`, `leaderboard_routes.py`. All decorator-protected endpoints use `admin_required`/`admin_2fa_required` (which are aliases for the SAME 2FA-enforcing function in `admin_auth.py`). Found ONE real gap: `drift_check_cash_tracking` admin-path inline auth was missing the `admin_2fa_verified` check (parallel endpoint `cron_snapshot_audit` had it). Fixed this session — admin path now requires email match (case-insensitive, with `current_user.email` fallback) AND 2FA flag, matching `cron_snapshot_audit`'s posture. Also confirmed that `admin_route_debug.py` (`/debug/check_admin_routes`), `api/admin.py` (`/admin-standalone`), and `api/debug.py` are NOT reachable on production — `api/index.py` (the Vercel entrypoint via `api/vercel.py`) does not register those blueprints, and `vercel.json` routes everything through `api/vercel.py`. They're dead code; see cleanup task below.
- [x] **POST-LAUNCH CLEANUP**: ~~delete dead admin-stub files~~ ✅ Done this session (commit `0b5ee87`) — deleted `admin_route_debug.py` (+ removed its `app.py` registration block), `api/admin.py`, `api/debug.py`. `debug_routes.py` and `admin_interface.py` are the REAL blueprints and are untouched.
- [ ] **POST-LAUNCH CLEANUP**: Delete the OLD Firebase iOS app entry (bundle `com.apestogether.app`) from Firebase Console → Project Settings → Your apps. It still exists with its APNs key attached but is no longer referenced by any plist shipped to TestFlight (Build 34+ uses the new entry `com.apestogether.ApesTogether`). Cosmetic / hygiene; not blocking launch.
- [x] **POST-LAUNCH CLEANUP**: ~~Add a "Test Push" button to the admin panel dashboard~~ ✅ Shipped this session — `/admin-panel` → System Health → Test Push Notification card wraps `/api/mobile/admin/test-push`. Username input + Send button + collapsible JSON result viewer. Replaces the old console-paste workflow.
- [x] **Fixed stale Firebase bundle ID in iOS `GoogleService-Info.plist`** ✅ **SHIPPED + VERIFIED May 12 (Build 34).** Root cause confirmed: Builds 1-33 shipped with a plist whose `BUNDLE_ID=com.apestogether.app`, while the actual app bundle is `com.apestogether.ApesTogether`. Fix: added a second iOS app entry to Firebase project `apestogether-2c749` with the correct bundle ID, uploaded APNs Auth Key `A6FBSX239G` to it (team-wide key, also still serving the old entry), downloaded the new `GoogleService-Info.plist` with new `GOOGLE_APP_ID=1:1096138595577:ios:ef605a913d1d7aa3b75628`, replaced the file in `ios/ApesTogetherApp/`, archived Build 34, installed on bobford00's iPhone. Test push from `/api/mobile/admin/test-push` delivered successfully with FCM `message_id` returned. Push delivery on iOS now works for the first time since the app's existence. **Post-launch cleanup tasks** (deferred, low priority): (a) delete the OLD Firebase iOS app entry `com.apestogether.app` from Firebase Console; (b) resolve Android Firebase SHA-1 conflict warning (same SHA-1+package combo registered in two GCP projects — Firebase auto-generated client vs. manually created "ApesTogether Android Client" in legacy GCP project); (c) keep the `/admin/test-push` endpoint as a permanent diagnostic tool.
- [x] **🚨 SECURITY (pre-launch blocker): Verify Google/Apple ID tokens properly.** ✅ **SHIPPED + VERIFIED May 11.** `mobile_api.py` now uses PyJWT's `PyJWKClient` (`_verify_google_id_token`, `_verify_apple_id_token`) to verify signature + issuer + audience + expiration against Google's and Apple's JWKS. Vercel env vars set: `STRICT_OAUTH_VERIFICATION=enforce`, `GOOGLE_ANDROID_CLIENT_ID=654567882865-4sklpa6uilpuogl30f1cnl6qqp53scc7.apps.googleusercontent.com`, `APPLE_BUNDLE_ID=com.apestogether.ApesTogether`. Smoke-tested May 11 ~9:30pm ET — Apple Sign-In works on iPhone (TestFlight build), Google Sign-In works on Android Studio Pixel 7 emulator (API 34, Google Play Store services). Audience-mismatch logs now surface the actual `aud` for fast diagnosis. **Follow-up (low priority, code hygiene only)**: delete the legacy decode-without-verification path + the `STRICT_OAUTH_VERIFICATION` shim from `mobile_api.py` once 24h of clean operation passes.
- [ ] Ping cron `*/4 * * * *` → `/api/health` to prevent Vercel cold starts (from `IMPLEMENTATION_CHECKLIST.md:69-73`) — status unclear, verify if deployed
- [ ] Verify weekly drift-detection cron fires on schedule (first scheduled run confirms; check `/admin/cash-tracking/last-drift-check` after)
- [x] **Confirmed `ADMIN_TOTP_SECRET` and `ADMIN_API_KEY` set on Vercel** (USER-confirmed, Session 16).
- [ ] Load-test `/api/mobile/feed` at 100 concurrent users
- [x] **Cash-Tracking-Drift + Snapshot-Drift cards both persist + reload correctly.** ✅ Shipped + verified this session (commit `b651b39`). Three layered bugs, all silent: (1) `flask_login.current_user` resolves to `api.index.User` which lacks the `extra_data` column, so the previous "use current_user" fix raised `AttributeError` that got swallowed by a try/except. (2) The codebase has TWO separate `SQLAlchemy(app)` instances (one in `models.py`, one created at `api/index.py:555`). `register_cash_tracking_routes(app, db)` is called with `api/index.py`'s db, so `db.session.commit()` inside `admin_cash_tracking.py` was committing a session that didn't track `models.User` rows -- the writes silently no-op'd despite returning `persisted: true`. (3) Two writers in quick succession (drift-check then snapshot-audit) had a read-modify-write race that could clobber each other's keys. **Final fix**: replaced ORM persistence in both writers with atomic raw SQL `UPDATE "user" SET metadata = jsonb_set(...)`. Raw SQL goes straight to the DB connection regardless of which SQLAlchemy instance is in scope, and `jsonb_set` is atomic per-key. Also made `cron_snapshot_audit` dual-auth so the "Run now" button works from a browser session. **Tech debt for post-launch**: consolidate the two `SQLAlchemy(app)` instances -- this footgun will keep biting until the `api/index.py:555` db is deleted in favor of `models.db.init_app(app)`.
- [x] Audit-cron email: fall back to `ADMIN_EMAIL` if `ADMIN_NOTIFY_EMAIL` is unset (commit pending May 10) — eliminates need for a duplicate Vercel env var; alerts will go to `fordutilityapps@gmail.com`.
- [x] **Backend hardening sweep (this session, commits `b4e854c` + `0b5ee87`).** (1) `notify_subscribers_via_email`: `NotificationLog.subscription_id` FKs to the legacy `subscription` table but was passed a `MobileSubscription.id` → FK violation made the batch `commit()` fail under a bare `except: pass`, silently dropping EVERY notification log in the fan-out. Now passes `None` (column is nullable) + logs commit failures loudly. (2) `bot_subscribe` (`/admin/bot/subscribe`) always 500'd — passed a non-existent `platform=` kwarg to `MobileSubscription` and omitted the NOT-NULL `in_app_purchase_id`; now creates a $0 placeholder `InAppPurchase` (platform='admin', zero price/payout/fees so it stays out of revenue/Xero) and links it. (3) `process_transaction` now normalizes `transaction_type` to lowercase at the central chokepoint. (4) Confirmed Finnhub premium gating (`FINNHUB_PREMIUM`) already implemented for `fetch_social_sentiment` + `fetch_analyst_data`; `purchase_price` price-fallback in `portfolio_performance.py` reviewed and intentionally LEFT (cost-basis is a better last resort than dropping a position).
- [ ] **KNOWN DATA-QUALITY ISSUE (non-blocking): 33 orphan uppercase transactions.** `stock_transaction` has 27 `'SELL'` + 6 `'BUY'` rows (uppercase) across 8 bot accounts (fund.finance2024, panther2585, moon-cash2021, panther59wizard, zen1889, chart1658, apex1575, panther3765), created ~Mar 27 by a since-replaced executor path. The cash-tracking replay matches lowercase only, so these are IGNORED. Verified via holdings reconciliation that they were NEVER applied to holdings OR cash — charts/cash are CORRECT and self-consistent; there is NO phantom drop. **Do NOT normalize them to lowercase** (that would make the replay count phantom trades and CREATE drift). The current `bot_execute_trade` already lowercases input, so no new orphans accrue. **Recommended (optional, post-launch): `DELETE FROM stock_transaction WHERE transaction_type IN ('SELL','BUY')`** to remove the landmine + misleading trade-history rows. No rebuild needed (live values already exclude them).
- [ ] **POST-LAUNCH: GitHub / CI security hardening (Phase F remediation — folded in from the archived `TOMORROW_GAMEPLAN.md`).** Inventory pass is effectively done (5 GH Actions secrets confirmed — `ADMIN_API_KEY`, `ALPHA_VANTAGE_API_KEY`, `CRON_SECRET`, `FINNHUB_API_KEY`, `INTRADAY_CRON_TOKEN`; all admin routes 2FA-gated per the auth sweep above). Remaining hardening, deferred to a focused post-launch session: (a) enable branch protection on `master`; (b) SHA-pin GitHub Actions instead of floating `@v4` tags; (c) move CI/deploy auth to OIDC where possible; (d) enable Dependabot + CodeQL; (e) run `git log --all -p | grep -E "(API_KEY|SECRET|password)"` historical-leak scan, BFG-clean if anything surfaces; (f) resolve any Vercel env-var warnings (preview-vs-production scoping / unused vars).
- [x] **Android legal/FAQ linkout URLs fixed (commit `608676c`).** `SettingsScreen.kt` linked to `/terms`, `/privacy`, `/faq` which 404. Real routes are `/terms-of-service`, `/privacy-policy`; FAQ is a landing anchor (`/#faq`). Also fixed the Play Console privacy-policy URL in `android/README.md` + `docs/PLAY_STORE_LISTING_GUIDE.md` (3 refs) so store submission uses a 200 URL. Android Phase E "Hide fractional shares" leaderboard toggle confirmed already fully implemented (FilterSheet + LeaderboardPreferences + ViewModel + API param).

- [x] **After-hours trades now queue as PENDING (#3, commit `9e0b4b9`).** Reused `QueuedEmailTrade` + the market-open cron's `process_queued_trades()`. `execute_trade` and `add_stocks` (new `intent='buy'`) queue when `is_market_hours()` is false; `get_portfolio` shows queued rows to the OWNER only as `status:'pending'` (null price); `DELETE /portfolio/pending-trades/<id>` cancels. Cash-on-hand-first already correct in `process_transaction`. Design notes in `PENDING_TRADES_DESIGN.md`. **Clients must rebuild** — `Trade.price` is now nullable on iOS + Android.
- [x] **ONE-TIME DATA FIX: bobford00's stuck after-hours META×5 — RESOLVED (Jun 1).** Was a live `Stock` row (`id 188`) at `purchase_price=0` with NO `stock_transaction` (the pre-#3 `$0` add_stocks path), confirmed via SQL. USER deleted it (`DELETE FROM stock WHERE id = 188`) since it was only a test. No cash unwind needed (it never created a transaction); no snapshot pollution (added 20:51 UTC, after the 20:05 EOD snapshot).
- [ ] **LATENT BUG: `add_stocks` intent='seed' records holdings at `price=0` / no transaction** → distorts cost basis and never shows in Recent Trades. This is the onboarding "declare existing holdings" path. Decide: capture cost basis in the UI, or stamp the add-date market price. (The new `intent='buy'` path already does live-price + a real txn.)

## B. iOS App

- [ ] Confirm TestFlight Build 30 includes `PerformanceChartView.swift` sparse-label change
- [ ] Visual verify CoastHillBear + your portfolio charts on every period (1D / 5D / 1M / 3M / YTD / 1Y)
- [ ] App Store Connect listing: title, subtitle, keywords, description (copy in `docs/ASO_STRATEGY.md` already drafted)
- [ ] **Replace AI-generated mockup screenshots** with real app screenshots
- [ ] App Store Connect: Privacy nutrition labels
- [ ] App Store Connect: Export Compliance answers
- [ ] Submit for App Store review (per playbook: Tue May 20)
- [ ] **VERIFY which Firebase project the live iOS app actually uses (config cleanup, Session 12 find).** Under the `fordutilityapps@gmail.com` Firebase Console there are THREE entries for this app: **"Apes Together Android"** (`com.apestogether.app` — the correct/verified Android FCM project, leave alone), **"Apes Together iOS"** (`com.apestogether.app` — suspicious: that's the *Android* package, not an iOS bundle; likely an early mistake), and **"ApesTogether iOS 2"** (`com.apestogether.ApesTogether` — matches the real iOS bundle ID, almost certainly the correct iOS one). Action: confirm which `GoogleService-Info.plist` the shipping iOS build embeds (Xcode → target → check `GOOGLE_APP_ID`/`BUNDLE_ID` in the plist), keep that one, and delete/ignore the stray. Non-blocking for Android launch; do before/around iOS submission so push + analytics point at the right project. NOTE: the ApesTogether **Firebase/GCP project for Android** is confirmed owned by `fordutilityapps` (business); the personal `bobford00@gmail.com` detour during billing-service-account setup was abandoned and cleaned up.

- [x] **Buy/Sell sheet polish (#5 + #6, commits `9d5463c` / `038f37a`).** Lighter input fields (`inputBackground` / `inputBorder`), "Quantity" labels, swipe-down indicator + close X on the Buy sheet, green "Buy" button (param'd `PrimaryButtonStyle.tint`), `banknote.fill` / `cart.fill` trade icons, fixed Sell-sheet top cut-off.
- [x] **Trade Alerts timestamp (#7)** was already on `origin/master` (`bacce6f`) — a Mac-side `git pull` gap, not a code bug.
- [ ] **Verify pending render on device:** an after-hours buy/sell should show under Recent Trades as PENDING (clock + badge, no price), NOT in Holdings, and NOT be sellable; it should settle at the next open.
- [x] **Trade buy/sell icons — DONE (Jun 7).** The old red/green directional arrows in **Recent Trades** (and the Sell sheet's `banknote.fill` / `cart.fill`) are replaced by `plus` (buy) / `minus` (sell). Applied on iOS (`PortfolioDetailView.TradeRow:758`, `TradeSheetView` `TradeType.icon`) and Android (`PortfolioDetailScreen.TradeRow`). Green-buy / red-sell color coding + PENDING clock preserved. Requires a Mac `git pull` + iOS rebuild and an Android rebuild to see.

## C. Android App — large unfinished workstream

**DECISION (May 10, 2026): Native Android in Kotlin / Jetpack Compose.** Wait for Android before public launch. No launch date set until Android is "fairly bug-free." Target: 1–2 weeks of focused Android work, then beta + parity verification, then begin marketing outreach in earnest.

### Foundation (completed May 10, session 3 — see commit + `android/README.md`)

- [x] Android project scaffolded under `/android/` (Kotlin 2.0 / Compose / Hilt / Retrofit / kotlinx.serialization / DataStore + EncryptedSharedPreferences / Vico / Firebase / Play Billing). Min SDK 26, Target SDK 34.
- [x] Networking layer (`ApiService.kt`, `AuthInterceptor.kt`, `ApiModule.kt`) — mirrors iOS `APIService.swift` 1:1. All read endpoints + auth + device registration + IAP validate + notification settings + trade execution + add stocks + tax status.
- [x] Models (`data/models/Models.kt`) — full port of iOS `Models.swift` with `display_name` → `displayName` + `publicName` extension property. snake_case fields handled via `@SerialName`.
- [x] Auth layer: `TokenStore` (encrypted) + `AuthRepository` with Google Sign-In via Credential Manager API (modern replacement for deprecated GoogleSignIn lib).
- [x] FCM push: `ApesFirebaseMessagingService` listens for trade-alert payloads, registers FCM tokens via `/device/register?platform=android`. Uses the same backend `push_notification_service.notify_subscribers_of_trade` already serving iOS.
- [x] App Links: AndroidManifest declares `https://apestogether.ai/p/<slug>` intent filter with `autoVerify=true`. `/public/.well-known/assetlinks.json` added with placeholder SHA-256 fingerprints. `vercel.json` updated to serve it as `application/json`.
- [x] Theme/Color/Type: 1:1 port of iOS `Theme.swift` palette (PrimaryAccent #00D9A5, AppBackground #0A0F0D, Gains #22C55E, Losses #EF4444).
- [x] Navigation: Compose Navigation graph with Login → MainTabs (4 tabs) + PortfolioDetail + Settings.
- [x] Stub screens for all 4 tabs + PortfolioDetail + Settings (Login + Leaderboard + Settings have real impls; the rest are placeholders that compile and route correctly).
- [x] Backend `/purchase/validate` already accepts `platform: "google"` + `purchase_token` and `iap_validation_service.py` already validates against Google Play Developer API. **No backend code changes required for Android billing**, only env var setup (see Section H).

### USER ACTIONS — required before the Android app builds locally

- [x] **Google Play Console** account paid (May 11, $25 one-time, Family Apps LLC). App listing creation with package `com.apestogether.app` still pending — needed before a real release SHA-256 fingerprint is available.
- [x] **Firebase project**: Android app `com.apestogether.app` registered (May 10). `google-services.json` downloaded and placed at `android/app/google-services.json` (gitignored). Debug SHA-1 added to Firebase.
- [x] **Google Cloud OAuth Web client** created ("ApesTogether Web Client") + Android client ("ApesTogether Android Client") with package `com.apestogether.app` + debug SHA-1. Web Client ID needs to be pasted into `android/secrets.properties`. Existing `GOOGLE_CLIENT_ID` Vercel env var ("Stock portfolio web client") is for the web app's Authlib redirect flow — leave it alone.
- [x] **Gradle wrapper generated** by Android Studio Panda on first sync (May 10).
- [ ] **Replace assetlinks.json placeholders**: `public/.well-known/assetlinks.json` has `REPLACE_WITH_RELEASE_SIGNING_CERT_SHA256` + `REPLACE_WITH_PLAY_APP_SIGNING_CERT_SHA256`. Get debug SHA-256 from `gradle :app:signingReport` (already done; save it), release SHA-256 comes from Play Console once the app listing is created and a release artifact is uploaded.
- [ ] **POST-LAUNCH CLEANUP**: Resolve the Android Firebase SHA-1 conflict warning. Same SHA-1 + `com.apestogether.app` package is registered in two GCP projects (Firebase auto-generated Android OAuth client + manually created "ApesTogether Android Client" in a legacy GCP project). Doesn't block today's work — Android sign-in + FCM are already working — but worth tidying up before launch so the Firebase Console doesn't keep flashing the warning.

### Screens still to port from iOS

- [x] LeaderboardView filter pills + sparklines (commits `ca17442`, `988e93c`)
- [x] TopInfluencersView (commit `d396ad8`)
- [x] MyPortfolioView (commit `58b0be6`)
- [x] SubscriptionsView (commit `58b0be6`, import fix `e6b7cfd`)
- [x] PortfolioDetailView (commit `58b0be6`)
- [x] PerformanceChartView via Vico (included in PortfolioDetail port `58b0be6`)
- [x] Google Play Billing on PortfolioDetailScreen Subscribe CTA (commit `feb85f2`) — BillingClient connect + querySkuDetails + launchBillingFlow + acknowledgePurchase + POST `/purchase/validate` all wired.
- [x] Onboarding flow — WelcomeCarouselView, ReferralPreviewView, EarnNudgeView, AddStocksView (commit `d1e20c3`)
- [x] CompactPlanToggle iOS port (Save 36% pill + price-prominent typography, commit `9b65fd9`)
- [x] App icon + themed-icons monochrome silhouette (commits `ca17442` through `e212107`, May 11). Photopea-flattened silhouette rescaled to align with foreground bbox.
- [ ] LegalText.swift content → assets/legal/ + SettingsScreen linkouts (status unverified, needs check)
- [ ] FeaturePollView, TradeSheetView, W9FormView, PortfolioShareCardView (lower priority, not strictly needed for v1)
- [ ] Settings TODOs deferred to v1.1: Payment History screen, Tax Info / W-9 sheet, FAQ link (iOS also hasn't shipped these; matches parity)

### Pre-launch testing

**Session 11 (2026-06-04) — code pre-flight audit done; client + backend bugs fixed. FCM display + all 4 deep-link paths VERIFIED on physical Pixel 8a. Billing still gated on Play Console setup.**

- [x] **Google Sign-In → token exchange → `/auth/user` round trip on a real device — VERIFIED on physical Pixel 8a (2026-06-09).** Root cause of the earlier on-device failure: the Android signing SHA-1 was not registered as an OAuth client, so Credential Manager rejected the request before any account picker (`google-services.json` showed `oauth_client count: 0`). **Fix (server-side config, no rebuild):** registered the **Play App Signing SHA-1** (`8F:A7:83:2D…`) as the Android client for `com.apestogether.app` in the Google Auth Platform → Clients (project `654567882865`, under `bobford00@gmail.com`) — required because the on-device build is the Play-signed internal-testing build. Today's logcat confirms the pass: `Auth.Api.Credentials [GetGoogleIdOperation] Operation succeeded` → CredentialManager `CREDENTIALS_RECEIVED` → app foregrounded; user confirmed sign-in completed. (Other registerable fingerprints if needed later: upload key `49:2E…` for a sideloaded release APK, debug `29:56…` for a debug build — not needed for the Play build.) Pre-reqs still confirmed in code: `GOOGLE_WEB_CLIENT_ID` in `secrets.properties`, Vercel `GOOGLE_ANDROID_CLIENT_ID` = the **Web** client ID + `STRICT_OAUTH_VERIFICATION=enforce`.
- [x] **Trade-alert FCM deep-link-on-tap — BUG FOUND + FIXED (session 11).** Background display always worked (notification+data, `trade_alerts` channel created in `ApesTogetherApplication` + declared as the FCM default in the manifest). But tapping a notification did NOT navigate: (1) `MainActivity` had no `onNewIntent` override, so with `launchMode=singleTask` every warm-start deep link (App Link tap or FCM tap while alive) was dropped; (2) a backgrounded FCM message is rendered by the **system tray**, which delivers the slug as the `portfolio_slug` intent **extra**, but MainActivity only read `intent.data` (a Uri). Fixed client-side only (iOS push path untouched): `MainActivity.ingestDeepLink` now resolves the slug from the data Uri **or** the `portfolio_slug` extra, on cold start **and** `onNewIntent`, and `RootApp` navigates whenever authed + a slug is pending (not just on the sign-in flip). **VERIFIED on physical Pixel 8a (2026-06-04):** admin Test Push delivered + displayed while backgrounded; deep-link nav confirmed via adb for all 4 cases — cold/warm × Uri-path/`portfolio_slug`-extra-path — each triggering the `GET /portfolio/<slug>` fetch that only fires on successful PortfolioDetail navigation (verification method needs a clean cold-launch-to-feed baseline, else same-slug response caching masks warm re-navigation as a false negative). Remaining: a real end-to-end trade-alert FCM tap (needs a live MobileSubscription, so it rides along with test #4).
- [~] App Link from `https://apestogether.ai/p/<slug>` opens PortfolioDetailScreen. Handling **VERIFIED on physical Pixel 8a (2026-06-04)** for cold **and** warm start via `adb shell am start -W -a android.intent.action.VIEW -d "https://apestogether.ai/p/<slug>" com.apestogether.app`. **`autoVerify` still reports unverified** — `public/.well-known/assetlinks.json` has placeholder SHA-256s and the debug build is signed with the debug keystore. Full verification is the `assetlinks.json` SHA-256 swap (see "USER ACTIONS" above), gated on the Play Console release/upload cert.
- [x] **Subscribe via Play Billing → backend validation → MobileSubscription row — VERIFIED on physical Pixel 8a (2026-06-09).** `bobford00` subscribed to `candle3873` from the internal-testing (Play-signed) build; the Play test-purchase sheet showed the correct product + price (`$9.00`, compressed to `/5 min` for License Testers) and the 7-day trial (compressed to a 3-min free trial), the purchase completed ("Payment successful"), `POST /api/mobile/purchase/validate` succeeded, and the trader's portfolio unlocked. `/api/mobile/health` confirms `iap.google: configured`. **This was the last Android pre-launch blocker.** (Backend detail below retained for history.) **Two backend bugs found + FIXED (session 11):** (1) the client never sent `product_id` and the backend defaulted to monthly, so the old v1 `purchases.subscriptions.get` 404'd for ANNUAL tokens; (2) annual subs were recorded at monthly pricing. **Migrated Google validation to `purchases.subscriptionsv2.get`** (token-only, future-proof for Play's new subscription model) in `iap_validation_service.py` (new `_parse_google_subscription_v2` + `_parse_rfc3339`, entitlement-first status mapping), forwarded `product_id` end-to-end (`PurchaseValidationRequest.productId` → `validate_purchase` → `validate_and_save_purchase` → validation + acknowledge), and priced via `_get_pricing(product_id)`. Unit test `test_google_response_parsing` rewritten to v2 (monthly/annual/expired/canceled) — logic verified locally. **External gates before on-device run:** set Vercel `GOOGLE_PLAY_CREDENTIALS_JSON` (Section H), publish both subscription products + base plans + the 7-day trial offer in Play Console, add a License Tester, and install from an **internal-testing track** (Play Billing won't work for a sideloaded debug APK). **Backend redeploy required.** **Session 12 (2026-06-07) progress:** Play app created (`com.apestogether.app`), merchant/payments profile set up, release signing wired into `android/app/build.gradle.kts` (gitignored `keystore.properties` + `keystores`-external `apestogether-upload.jks`, PKCS12), signed AAB built (`:app:bundleRelease`) and uploaded to the **Internal testing** track (Play App Signing auto-enabled), and **both subscriptions are Active** (`...monthly` $9 base plan `monthly` + offer `monthly-7day-free`; `...annual` $69 base plan `annual` + offer `annual-7day-free`; both trials eligibility = "Never had any subscription"). Play Developer API service account created in the **`fordutilityapps`-owned** `ApesTogether Play API` GCP project (`apes-play-billing-validator`, sole Owner = fordutilityapps; an initial `bobford00` personal-account attempt was abandoned + its key/grant cleaned up), granted in Play Console (View app info + View financial data + Manage orders and subscriptions), and `GOOGLE_PLAY_CREDENTIALS_JSON` (raw JSON) + `GOOGLE_PLAY_PACKAGE_NAME=com.apestogether.app` set on Vercel Production + **redeployed**. Remaining gate: the on-device purchase test from the internal-testing track (+ assetlinks SHA-256 swap, still pending the Play App Signing cert).
- [ ] **VERIFY during on-device billing test: multiple concurrent subscriptions on a single product ID.** Subscriptions are per-trader (`MobileSubscription.subscribed_to_id`, `mobile_api.py:462-487`) but every monthly trader-sub shares the one Play product `com.apestogether.subscription.monthly` (annual likewise). Google Play's default is **one active subscription per product per account**, so following a 2nd trader could collide / be refused at the Play purchase sheet. Confirm whether Play allows it as-is; if not, the per-trader model needs a different mapping (e.g. per-purchase obfuscatedAccountId/quantity, or multiple SKUs). Non-blocking for the first single-trader test. Also note: with the "Never had any subscription" trial eligibility, the 7-day free trial only fires on a user's first-ever sub, not per trader (intended).
- [ ] 14-day Google Play closed-testing window for Production track release. Process-only; confirm whether the Family Apps LLC **org** account is even subject to the 20-tester/14-day rule (it primarily targets personal accounts created after Nov 2023) before starting the clock.

## D. Legal / Compliance

- [!] Privacy policy final version — **blocked on attorney**
- [ ] Terms of Service final review (have `ATTermsOfService03APR2026.docx`, reconcile with `/legal/terms-of-service.md`)
- [ ] Investment disclaimers audit (per LAUNCH_PLAYBOOK §11 — "never claim guaranteed returns", etc.)
- [ ] Per-store disclaimers (App Store + Play listings must say "We do not offer real brokerage services…")

## E. Website / Landing Page

- [x] Hero CTA → "Join the Beta Waitlist"
- [x] Nav CTA → "Join the Beta"
- [x] Waitlist segmentation (follow trader / earn as trader)
- [x] Urgency text, trust bar, slider default, proof card attributions, footer socials, © 2026
- [x] **App mockup is a real screenshot** (May 10 confirmation; pricing in screenshot is dated but acceptable for now — refresh closer to launch)
- [ ] Refresh landing screenshot once iOS app is final (current one has old pricing)
- [ ] **Create social media accounts** (X, TikTok, IG, YouTube) — LAUNCH_PLAYBOOK §3. Originally Day 1 (Apr 14), now: do this weekend before Android work begins, just to lock the handles.
- [ ] Test OG image at opengraph.xyz
- [ ] Live waitlist counter ("Join N others on the waitlist")

## F. Testing

- [ ] End-to-end paid subscription flow with test iOS account (IAP → backend receipt validation → Xero sync → 1099 flow)
- [ ] End-to-end W-9 flow via Xero 1099 contractor group with a real paid subscriber
- [ ] Push notification delivery on both iOS + Android once FCM is live
- [ ] Chart rendering on every current bot portfolio, every period — no spikes, no empty periods where bots are old enough
- [ ] ~~Stripe webhook handling on subscription cancellation / refund edge cases~~ — **Stripe is disabled (`app.py:734`); cancel/refund/renewal edge cases now belong to Apple ASSN V2 + Google RTDN.** Tracked in the **💳 Subscription Lifecycle & Billing** section (incl. the in-app cancel deep-link path).

## G. Marketing / Launch Calendar

**The original Apr 14 – Jun 1 calendar in `docs/LAUNCH_PLAYBOOK.md` is now a template, not a deadline.** Sequencing reset on May 10:

1. Finish iOS stability + paid-sub E2E test + App Store assets (1–2 weeks)
2. Build native Android app (Kotlin/Compose) to feature parity (target: 1–2 weeks of focused work, plus 14-day Google Play closed-testing gate)
3. Beta both apps, fix bugs, declare "fairly bug-free"
4. **Then** set a launch date and run the social-media + outreach phases of LAUNCH_PLAYBOOK at full speed

This section only tracks **deferred / dropped items**:

- [ ] LAUNCH_PLAYBOOK Day 38 — **$2 bill stunt deferred**. Don't order bills until launch week is in sight.
- [ ] LAUNCH_PLAYBOOK Phase 1 social accounts: defer daily posting; only goal this weekend is to **register the handles** (X, TikTok, IG, YouTube, LinkedIn for the company) before someone else takes them. No posting yet.
- [ ] LAUNCH_PLAYBOOK Day 22 (Mon May 5): "Upload to TestFlight + Play Console. Invite 12+ Google Play testers (14-day clock starts)." — Google Play side gated on Section C completion.
- [ ] LAUNCH_PLAYBOOK Day 33 (Fri May 16): "Verify Google Play 14-day gate passed" — recompute target date once Section C scaffolding is done.
- [ ] LAUNCH_PLAYBOOK Day 37 (Tue May 20): "Submit App Store + Google Play listings for review" — recompute.

## H. Operations / Data

- [ ] AlphaVantage News/Movers/Prices (fallback) showing `no_calls` in admin panel — fix shipped this session, verify after next bot-trading wave runs
- [x] **`/admin-panel` → API Usage tab all-zeroes — RESOLVED (USER-confirmed, 2026-06-14). DO NOT REVISIT.** Fixed in a prior session; closed.
- [ ] Bot trade decisions cross-checked against AV sentiment data (sanity: do momentum bots actually trade differently on news-positive vs news-negative days?)
- [x] **Xero RECONNECTED (Session 16).** The 2026-06-14 harness had shown `token_valid:false` (expired 2026-03-23). USER re-authorized via `https://apestogether.ai/api/mobile/admin/xero/connect` (note the `/api/mobile` prefix — the bare `/admin/xero/connect` 404s) and `GET /api/mobile/admin/xero/accounts` then returned all three codes `true` (`4010`/`6010`/`6100`) — which requires a valid token, confirming the connection is live. **Remaining:** after the next monthly close, confirm revenue post + payout sync run cleanly against the now-valid token.
- [ ] Xero — ghost subscriber tracking matches expected month-end payouts
- [ ] Vercel env: **`GOOGLE_PLAY_CREDENTIALS_JSON`** for Google Play purchase validation (read by `iap_validation_service.py:88` — NOTE: the code reads this exact name, NOT `GOOGLE_PLAY_SERVICE_ACCOUNT_JSON`). Generate a service account in Google Cloud Console with Play Developer API access, link it in Play Console → Users & permissions, then paste the JSON key contents. `GOOGLE_PLAY_PACKAGE_NAME` is optional (defaults to `com.apestogether.app`). Without this var, `validate_google_purchase` returns `server_config_error` and every Android subscribe will fail validation.

- [ ] **#1 Verify bot creation + `/admin-panel` batch creation** follows the new rules: trades within the assigned industry/sector, the research method, and `max_cash_deployed` all wired correctly (`bot_executor.py` / `bot_strategies.py` / `bot_data_hub.py` + admin batch routes).
- [ ] **#2 Per-sector data adequacy.** Assess whether AlphaVantage News/Movers + Finnhub Insider/Analyst give bots enough signal in thinner sectors (healthcare, real estate). Recommend additional data services if there are gaps.
- [ ] **#3 AI stock-picking model API research.** Evaluate feeding bots an external AI model / signal API for stock selection vs. the current AlphaVantage + Finnhub rules-based research. Scope candidate providers, cost, latency, and how it would slot into `bot_data_hub.py` / `bot_strategies.py`. Discussed in prior sessions; never written down until now.

## I. Local Dev Hygiene (Mac)

- [ ] Delete the stale clone at `/Users/macbookair/CascadeProjects/stock-portfolio-app` on the Mac. It predates the `ios/` folder being added to the repo and was the source of confusion on 2026-05-12 when `git pull` was attempted there. The active iOS-development clone is `/Users/macbookair/Documents/ApesTogether/`.
- [ ] Commit the build-number bumps + `xcshareddata/` schemes that live as uncommitted edits on the Mac's iOS clone. Doesn't matter functionally but it'd clean up `git status` and make future iOS Build #s reproducible from Windows.

## J. Email Infrastructure

**MIGRATION COMPLETE (May 27, 2026):** apestogether.ai email is now on Google Workspace via User Alias Domain on `bob@laborofloveapp.com`. Cloudflare Email Routing decommissioned. Root MX → `smtp.google.com`; root SPF includes Google + SendGrid. SendGrid sending (notifications@) and inbound parse (trade@trade.) unchanged. Save annually: ~$118/yr (Workspace went from Squarespace reseller $201.60/yr to direct Google $84/yr). Functional addresses: `bob@`, `support@`, `privacy@`, `legal@`, `hello@`, `notifications@apestogether.ai`. Send-As identities configured for bob (default), support, privacy, notifications. "Reply from same address received to" enabled.

### Open issues / follow-ups

- [ ] **🐛 `notifications@apestogether.ai` lands in personal Gmail spam folder.** First noticed May 27 after migration but unrelated to today's DNS changes (which only affected ROOT domain MX/SPF — SendGrid sends via `em8350.apestogether.ai` subdomain, untouched). Diagnostic plan: (1) Open a recent junked email → "Show original" → capture `Authentication-Results:` to verify SPF/DKIM/DMARC verdicts. (2) SendGrid → Stats → Email Activity, check 7-day complaint rate and bounce rate. (3) Run a sample trade alert through mail-tester.com for spam-trigger scoring. (4) Inspect content for high-risk patterns (financial keywords are unavoidable but we may be able to soften certain). (5) Consider setting up DMARC aggregate reporting (`rua=mailto:dmarc-reports@apestogether.ai`) for ongoing visibility. Question to answer: is this only one personal Gmail's filter, or are real users / beta testers also missing trade alerts?
- [ ] **Investigate `Activate Gmail` link still showing on `laborofloveapp.com` in Workspace admin.** Post-migration housekeeping item, low priority, cosmetic — Gmail mail flow on the primary domain works.
- [ ] **Tighten DMARC from `p=quarantine` → `p=reject`** ONLY after the notifications@ deliverability issue is diagnosed and 2+ weeks of clean operation pass. NOT a fix for current spam-folder issue (those emails are passing DMARC; tightening only affects emails that FAIL alignment). Defer.
- [ ] **Optional: Set up Gmail filters / labels** for `support@`, `privacy@`, `legal@`, `notifications@` so each gets auto-labeled in Bob's inbox. Quality-of-life improvement.
- [ ] **Optional: Migrate internal monitoring** (`BCC_EMAIL` in `services/notification_utils.py`, `ADMIN_EMAIL` cron alerts) from `fordutilityapps@gmail.com` to dedicated `audit@apestogether.ai` / `ops@apestogether.ai` aliases with auto-archive Gmail filters. Recommended deferred — current setup keeps high-volume audit traffic out of professional inbox, which is fine.

---

## Session cadence

At the start of each session, the assistant should:

1. Read this file
2. Ask which open item(s) to work on
3. Update status as items complete

At the end of each session, the assistant should:

1. Check off items finished in the session
2. Add any newly discovered items to the relevant category
3. Commit the updated file alongside any code changes

---

## Last updated

**Session 18 (2026-06-22) — master pre-launch list rebuilt + slots store-config done + push verified:**
- Rebuilt the **MASTER PRE-LAUNCH TASK LIST** (top of file) from this file + every `docs/*.md`. Added the items the USER flagged as missing: **full security audit (SQL injection / IDOR / rate-limiting)** and an **inbound trading API for external algos/AI models**, and surfaced bot-research **#1/#2/#3** — confirming their write-ups were **never produced** (USER correctly recalled never seeing the output).
- **Slot 2–20 store products CREATED by USER** — 19 iOS groups + 38 Play products; slots item updated below.
- **Verified pushed:** Session 16 payout-integrity + 1099/W-9 code (`e630f13`) and the monthly-payout cron (`1c9eaa5`) are on `origin/master`; no uncommitted money-file changes.
- **Still gating launch:** run the two Supabase migrations; Apple ASSN sandbox test; ship iOS Build 41 + Android rebuild; E2E money tests; store assets + submit; attorney privacy policy.

**Session 16 — payout integrity + 1099/W-9 ops (code; uncommitted, tested):**
- **Track 1 (money integrity):** fixed three payout bugs — (1) renewals now book a new `InAppPurchase` row each period (`_record_renewal`), the keystone fix; (2) transaction-driven, annual-aware monthly payouts (no more count×rate); (3) idempotent refund/chargeback credit notes + sweep endpoint + carry-forward clawback. New unique index `uq_payout_user_period`; migration `scripts/migrations/2026_06_17_payout_integrity.sql`.
- **Track 2 (tax ops):** `GET /admin/tax/1099-readiness` ($600/missing-TIN dashboard) + `POST /admin/tax/w9/retry-sync` (reconciles failed W-9 pushes against Xero, releases held payouts).
- **Tests:** `tests/test_payout_integrity.py` — 9 passing (renewal cloning/idempotency, clawback netting, 1099 categorization + admin-auth). Verified locally (installed `pytest` + `Flask-SQLAlchemy` in the sandbox).
- **Remaining (USER):** run the migration; commit; reconnect Xero (Section H); finish Track 3 store/env config (started — confirm `/api/mobile/health`).

**2026-06-14 — session 14 ended with:**
- **Shipped Mac-side (commit `d5c8510`, build `1.0 (40)`):** the i1/i2/i3 fixes (holdings auto-refresh, iOS leaderboard `authenticated:true`, iOS renewal-date UTC parser) archived + uploaded. i2/i3 verified on-device; **i1 still pending** the next Wolff rebalance to confirm.
- **Subscription/billing audit (new `💳 Subscription Lifecycle & Billing` section):** standardized the CTA copy to “Try Free for 7 Days, then $X” on both apps + removed the redundant grey trial subtext (done, uncommitted). Found four open items — in-app cancel doesn’t cancel at the store / no deep-link; no Apple ASSN V2 or Google RTDN handlers; the “we’ll remind you before the trial ends” promise is unimplemented; cancel-path + accounting test harnesses to add. Confirmed the expanded bot-username generator is live + wired.
- **Session 14 (cont.) — all billing work committed + pushed (`33a903b`, rebased onto the Mac's `d5c8510`).** Built + RAN the accounting harness (`/admin/bot/simulate-subscription-lifecycle`); built Apple ASSN V2 + Google RTDN webhook handlers (`iap_webhooks.py` + routes in `mobile_api.py`); resolved the trial-reminder item by removing the promise; added in-app cancel store deep-links (iOS `showManageSubscriptions` / Android Play subscriptions). **Android debug build of the current code (new CTA, store-cancel deep-link, recent-trades-15, trade icons) installed on the physical Pixel 8a** via `gradlew installDebug`. Harness finding: **Xero token expired** (re-auth needed). **Remaining:** iOS build 41 archive; store-side ASSN/RTDN config; Xero re-auth; on-device cancel-path test.

**2026-06-01 — session 8 ended with:**
- **iOS Buy/Sell sheet polish (#5/#6)** shipped (`9d5463c`, `038f37a`): lighter inputs, "Quantity" label, swipe-down + close affordances, green Buy button, banknote/cart icons, fixed Sell top cut-off.
- **Trade Alerts (#7)** clarified — already fixed on `origin/master` (`bacce6f`); user just needed to `git pull` + rebuild iOS. Pushed 2 stranded Android commits too.
- **Pending after-hours trades (#3)** implemented end-to-end (`9e0b4b9`): backend gate on `execute_trade` + `add_stocks` (new `intent='buy'`), reusing `QueuedEmailTrade` + the existing market-open cron to settle at the open price; owner-only Pending rows in Recent Trades; iOS + Android render Pending; cancel endpoint added; cash-first confirmed already correct. Full notes in `PENDING_TRADES_DESIGN.md`.
- **New findings logged (Sections A/B/H):** (1) bobford00's pre-fix after-hours META×5 ($0-basis `Stock` id 188, no txn) — RESOLVED, USER deleted it (test); (2) `add_stocks` seed path records `price=0`/no txn (cost-basis bug); (3) #4 apes1575/divi51 open-time anomaly; (4) #1 bot-rule verification + #2 per-sector data adequacy still open; (5) on-device verification of pending render pending.

**2026-05-31 — session 7 ended with:**
- **Drift-audit crons hardened against EOD-wave false positives (commit `7549f80`).** Root cause: the audits bucketed trades with a 20:05-UTC effective-date cutoff, but the snapshot writer (`calculate_cash_proceeds_as_of_date` + historical stock-value replay) buckets by `func.date(timestamp)` = UTC calendar date. The EOD bot wave (~20:05-20:20 UTC) lands trades the same UTC day; depending on whether a snapshot was written before/after the wave (or rebuilt), it may or may not reflect them — both internally consistent (smooth `total_value`). The old cutoff flagged this timing as drift. Fix across all four functions (`cron_snapshot_audit`, `admin_audit_snapshot_cash_drift`, `admin_audit_snapshot_max_cash_drift`, `drift_check_cash_tracking`): (1) bucket trades by UTC calendar date; (2) widen the ambiguous-trade tolerance from ±60s-of-20:05 to the **full post-market-close window (>= 20:00 UTC)** — their summed |value| is added to the per-date threshold, so a snapshot that captured the wave (or not) is never falsely flagged, while INTRADAY (<20:00 UTC) drift still surfaces; (3) skip copytrade bots (`_is_copytrade_bot`) whose brokerage-screenshot migrations can't be replayed. Validated: all 7 wave false positives (fund.finance2024, panther2585, moon-cash2021, panther3765, apex1575, chart1658, divi51) cleared.
- **zen1889 $12.74 dividend double-count repaired** (data fix via direct SQL, no deploy). zen1889 is a seeded bot (5 holdings ~$91.7k seeded at creation 3/18 with no buy txns). On 5/21 a $12.74 MSFT dividend was kept as free cash AND the same-day DE buy was counted as full new capital — double-counting. Set to the canonical replay model: `cash_proceeds` $12.74→$0, `max_cash_deployed` $120,676.38→$120,663.64; fixed 6 daily + 150 intraday snapshots. Seeded baseline (~$100,484) preserved. Post-fix: 0 drift fleet-wide.
- **⚠️ DO NOT change `full-rebuild`'s `_snapshot_effective_date` (20:05 UTC cutoff) to UTC date.** It is INTENTIONAL: `_full_rebuild_for_user` reuses the stored `stock_value` (written by the EOD cron at 20:05 from live holdings, which excludes not-yet-committed post-close trades) and only recomputes cash. Using a 20:05 cash cutoff keeps cash consistent with that `stock_value`. Switching to UTC-date cash would include post-close trades while `stock_value` excludes them → inconsistent `total_value` = phantom drops. The audit's UTC-date + post-close tolerance already accepts both as-written and rebuilt snapshots, so there is nothing to reconcile.

**2026-05-27 (evening) — session 6 ended with:**
- **Email infrastructure migration complete.** Migrated apestogether.ai email from Cloudflare Email Routing → Google Workspace User Alias Domain on `bob@laborofloveapp.com`. Workspace billing also migrated Squarespace reseller → direct Google billing in same session ($201.60/yr → $84/yr annual = ~$118/yr savings). Dropped unused Catalina seat. New DNS state: root MX → `smtp.google.com` (priority 1), root SPF → `v=spf1 include:_spf.google.com include:sendgrid.net ~all`, Cloudflare-managed records (3× MX, DKIM cf2024-1, old SPF) all removed via Email Routing → "Delete and Disable". SendGrid setup untouched: outbound from `notifications@apestogether.ai` still flows via `em8350.apestogether.ai` subdomain auth (CNAMEs s1/s2._domainkey + em8350 + url4305 + 57174039 → sendgrid.net), inbound parse on `trade@trade.apestogether.ai` still routes to `/api/email/inbound`. Email-to-trade pipeline unaffected. Functional Workspace addresses: `bob@`, `support@`, `privacy@`, `legal@`, `hello@`, `notifications@apestogether.ai` (auto-mirrored from `*@laborofloveapp.com` aliases via the User Alias Domain mechanism). Gmail Send-As identities configured: `bob@apestogether.ai` (default for new outbound), `support@`, `privacy@`, `notifications@apestogether.ai`. "Reply from same address received to" enabled. Receive test from external Gmail to support@apestogether.ai delivered successfully. Send test from support@apestogether.ai displayed correct From header on recipient side.
- **Play Console contact email updated** to `support@apestogether.ai` and website ownership verified for apestogether.ai. The "Create App" button is now unblocked. Store listing copy was already drafted in `docs/PLAY_STORE_LISTING_GUIDE.md` and `docs/ASO_STRATEGY.md`.
- **Pending follow-up**: `notifications@apestogether.ai` is landing in personal Gmail's spam folder. Initially misdiagnosed as recipient-side filter learning; corrected — the address has been the SendGrid sending identity for months and today's root-domain DNS changes don't affect SendGrid's sender path (it uses `em8350` subdomain envelope sender + `s*._domainkey` DKIM, both unchanged). Real likely causes: SendGrid IP reputation, content triggers, or a Gmail engagement-signal threshold. Diagnostic plan tracked in new Section J. **Tightening DMARC from `p=quarantine` → `p=reject` will NOT fix the spam-folder issue** since legit mail is passing DMARC alignment; tightening only changes behavior for FAILING emails. Defer that change until ≥2 weeks of clean operation post-deliverability-fix.
- **Cleanup decision**: leave `BCC_EMAIL = 'fordutilityapps@gmail.com'` in `services/notification_utils.py:26` and `ADMIN_EMAIL` cron alerts in personal Gmail. Migrating high-volume audit BCC traffic into Bob's professional inbox would drown out actual support email; current separation is good design. Optional future cleanup tracked in Section J.
- **No code changes needed**: user-facing references (`templates/landing.html`, `templates/privacy_policy.html`, `templates/terms_of_service.html`, iOS `LegalText.swift` + `SettingsView.swift`, Android `SettingsScreen.kt`, `docs/PLAY_STORE_LISTING_GUIDE.md`, `docs/ASO_STRATEGY.md`) all already used `support@apestogether.ai`. The migration just delivered the actual mailbox to back the address that was already published.

**2026-05-12 (afternoon) — session 5 ended with:**
- **FCM iOS push notifications fixed and verified end-to-end.** Root cause: shipped builds 1-33 had a `GoogleService-Info.plist` with `BUNDLE_ID=com.apestogether.app` while the actual app bundle is `com.apestogether.ApesTogether`. Fix: created a second Firebase iOS app entry with the correct bundle, uploaded existing APNs Auth Key `A6FBSX239G` to it (team-wide; same `.p8` rescued from `/Users/macbookair/Downloads/`), downloaded new plist (new `GOOGLE_APP_ID=1:1096138595577:ios:ef605a913d1d7aa3b75628`), swapped into `ios/ApesTogetherApp/`, archived Build 34, installed on iPhone, verified `device_token.id=2` refreshed its token and a test push was delivered.
- **Added `/api/mobile/admin/test-push` diagnostic endpoint** (commit `7a86dc0`) protected by `@require_admin_2fa`. Reports per-token success/failure with FCM `message_id` on success or error reason on failure. Worth keeping in the codebase permanently as cheap insurance against future push regressions.
- **Saved Mac path layout to permanent memory** so future sessions don't fish for paths. Active iOS clone is `/Users/macbookair/Documents/ApesTogether/`; LaborOfLove at `/Users/macbookair/Developer/LaborOfLove/LOL_New_Design_2020/`. Apple Team `M8R8YVW472`. APNs key `A6FBSX239G` (ApesTogether). LaborOfLove APNs `53T7S48FGU`. SIWA key `Y7L9R28W87`.
- **Pending**: smoke test of push delivery on next real Public.com trade email (passive — happens whenever the next bot trade fires). Vercel logs will show `notifications: {push_sent: N}` in the `/api/mobile/admin/bot/email-trade` response.

**2026-05-11 (afternoon) — session 4 ended with:**
- **Android Section C "Screens still to port from iOS" is functionally complete.** All twelve iOS screens / flows / integrations are now on Android with parity: Leaderboard (filter pills + sparklines), TopInfluencers, MyPortfolio, Subscriptions, PortfolioDetail, PerformanceChartView (Vico), Play Billing E2E, full onboarding (Welcome / Referral / EarnNudge / AddStocks), CompactPlanToggle, and the app icon with themed-icons monochrome silhouette.
- **App icon themed-icons** finished today (commits `30266c3` → `e212107`). Hit several gotchas worth recording: (1) Image Asset Studio prepends an AOSP Apache-2.0 license comment **above** the `<?xml ?>` declaration which malforms the XML and breaks `aapt2`; fix is to delete the boilerplate so the `<?xml ?>` is back on line 1. (2) Asset Studio's "Monochrome" tab does **not** auto-flatten gradient input — it writes the source verbatim. A gradient monochrome layer renders as an empty disc under themed icons. The fix is to hand-flatten in Photopea (lock transparency → fill with black) and re-import, then rescale to match the foreground's *monkey bbox* (not the whole foreground PNG, which has a darker plate). Pillow scripts in this session's commits handle the rescale.
- **Google Play Console account paid** ($25, Family Apps LLC). App listing creation pending — required before release SHA-256 is available for `assetlinks.json`.
- **CompactPlanToggle iOS port** shipped (`9b65fd9`) — Android now matches iOS's "Save 36%" pill on the Annual chip and price-prominent typography. Earlier commit `a64ba7b` tightened vertical padding 8dp → 4dp.
- **Carousel skip race fixed** (`c67dcec`) and Image Asset Studio'd malformed XML resources fixed (`726898e`).
- Remaining Android workstream: LegalText port status (needs check), pre-launch testing on a real device (5 items), `assetlinks.json` SHA-256 swap (gated on Play Console app listing), and v1.1-deferred screens.
- **Pre-launch security blocker** (`mobile_api.py /auth/token` decoding ID tokens with `verify_signature=False`) is the next thing I'm tackling. Both iOS and Android currently rely on this code path; fix needs to preserve backward compat for both clients.

**2026-05-10 (afternoon) — session 3 ended with:**
- Audit tolerance buffer shipped (commit `34a01dc`) — Vercel cron jitter ±60s false positives no longer flagged.
- Display name values set in DB for `marblethehill72` and `CoastHillBear`. iOS Build 32 archived, names confirmed rendering.
- Copytrade-bot email-trade endpoint now fans out push + email to subscribers (commit `8ce748c`). Trade notifications switched from raw username to `public_name`.
- Pending: verify push delivery on next real Public.com trade email. Confirmed: bobford00 has 1 active `device_token` (token_id=2, ios, created Feb 27).
- ADMIN_NOTIFY_EMAIL falls back to ADMIN_EMAIL (commit `ad799d8`) — eliminates need for duplicate Vercel env var.
- **Native Android scaffold complete**: full project under `/android/` (Kotlin 2.0 / Compose / Hilt / Retrofit / Vico / Firebase / Play Billing). Auth, networking, FCM, deep-link config, Theme, Navigation graph, and 7 screens (3 real + 4 stubs) all in. iOS-equivalent feature surface enumerated in Section C "Screens still to port from iOS". `assetlinks.json` added; `vercel.json` serves it as application/json.
- Reviewed Section G launch-calendar reality: at Day 27 of 49, Phase 1 social-media foundation is unstarted. June 1 launch is officially deferred; new sequencing is iOS finalize → Android port → beta → set launch date → run playbook content.

**2026-05-09 (afternoon) — session 2 ended with:**
- Drift indicator card live, drift check returned "Clean" (13 users scanned, $1.00 threshold)
- AlphaVantage logging fix shipped (verify Mon at 9:45 AM ET wave)
- iOS Build 31 archived from local Mac
- Discovered `display_name` field never existed; the past "rename usernames" request was effectively a no-op (validation regex blocks spaces/apostrophes/uppercase). Implemented full `display_name` column + iOS Codable + 8 backend serializers + 6 Swift views in this session.
- Launch date deferred from June 1, 2026 to TBD pending Android completion.
- panther2585 ~20% 1M chart drop and bobford00 performance audit pending — diagnostic URLs queued in Monday checklist.

**2026-05-09 (morning) — session 1 ended with:**
- Chart spike fix deployed
- Leaderboard rebuild auth + lock-fast deployed (commit `8d448ab`)
- Drift indicator + AlphaVantage logging fix added
- This LAUNCH_TODO.md created
