package com.apestogether.app.ui.theme

import androidx.compose.ui.graphics.Color

/*
 * Direct port of `ios/ApesTogetherApp/Theme.swift`. Keeping the hex values
 * 1:1 ensures Android and iOS look identical at a glance, which matters for
 * App Store + Play Store screenshot consistency.
 */

// ── Backgrounds ───────────────────────────────────────────────────────────
val AppBackground = Color(0xFF0A0F0D)
val CardBackground = Color(0xFF141A17)
val CardBorder = Color(0xFF1F2A24)
val HeroBackgroundEnd = Color(0xFF0D1A14)
val CardGradientEnd = Color(0xFF0F1512)

// ── Accents ───────────────────────────────────────────────────────────────
val PrimaryAccent = Color(0xFF00D9A5)
val SecondaryAccent = Color(0xFF34D399)

// ── Semantic ──────────────────────────────────────────────────────────────
val Gains = Color(0xFF22C55E)
val Losses = Color(0xFFEF4444)

// ── Text ──────────────────────────────────────────────────────────────────
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFF9CA3AF)
val TextMuted = Color(0xFF6B7280)
