# Gradle
.gradle/
build/
local.properties

# Kotlin compiler session caches (Android Studio Panda)
.kotlin/

# Secrets (Google client IDs, signing config, etc.)
secrets.properties
keystore.properties
*.keystore
*.jks

# Firebase config (committed only AFTER adding the Android app to Firebase project)
app/google-services.json

# IDE
.idea/
*.iml
captures/
.cxx/

# OS
.DS_Store
Thumbs.db
